# Phase 4 v3 — Projection-Loss Formalization

Status: `FORMAL_THEOREM_PACKAGE_SUPPORTED_BY_FINITE_CORPUS_INSTANTIATIONS`

This package formalizes projection-loss as failure of custody factorization and separates custody-admissibility from projected quotient closure.

Run:

```bash
python scripts/phase4_v3_projection_loss_formalization.py
```

Lean workspace:

```bash
cd lean
lake build
```

Local Lean execution remains pending on a machine with Lean/Lake installed.
