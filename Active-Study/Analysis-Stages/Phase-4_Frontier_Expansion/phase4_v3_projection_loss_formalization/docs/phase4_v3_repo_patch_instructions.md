# Phase 4 v3 Repo Patch Instructions

1. Add `phase4_v3_projection_loss_formalization.md` to the Phase 4 research notes.
2. Promote `CustodyAdmissible`, `QuotientAdmissible`, and `ProjectionLossWitness` into the canon vocabulary.
3. Replace any loose use of `projection is valid` with either `custody-admissible` or `quotient-admissible`.
4. Attach `phase4_v3_projection_loss_witnesses.csv` as the finite witness table.
5. Link the Lean/Lake workspace under `lean/` for local `lake build`.
