# Phase 5 v1 Cross-Reading Patch

Adds a new sealed branch inside Phase 5 v1:

- preserves canon repair and all prior failure controls;
- derives native cross-read candidates from QBL word histories;
- freezes native tables before target comparison;
- records the boundary: current native cross-read laws are overlap/separation laws, not bilinear Fourier pairing.
