# Phase 5 v1: Orthad Overset Dual-Lens Transfer Test

Status: `PARTIAL_GENERATION_FOURIER_TRANSFER_GENERATED_GAUSS_NOT_GENERATED`

Phase 5 v1 remains open.

This branch tests the user's Yin-Yang overset hypothesis for the Orthad: the missing off-diagonal entries do not live inside one diagonal lens. They live in the transfer between two complementary Orthad charts.

## Claim tested

The Orthad can generate a native cross-orientation transfer by reading the same QBL history through two complementary chart bases:

```text
Omega_plus  = point-orientation chart
Omega_minus = dual eigen-cochain chart of the native orientation successor
K_N(r,s)    = Read_minus_s(Transfer_plus_minus(e_r))
```

## Result

```text
Fourier / Weil_F transfer: GENERATED for N=8 and N=12
Quadratic Gauss / Weil_G diagonal: NOT GENERATED BY THIS BRANCH
Collapse consistency: PASS
Dense block admission: FORBIDDEN and not used
```

The native transfer law derived before the target comparison is:

```text
K_N(r,s) = N^(-1/2) exp(-2*pi*i*r*s/N)
```

This is obtained as the change of chart from the point orientation basis to the normalized eigen-cochain basis of the QBL orientation successor `sigma_N : r -> r+1`.

## Why this is not the banned dense block

The off-diagonal table is not inserted as a matrix slot. It is obtained by solving the native chart-transfer problem:

```text
sigma_N e_r = e_6
chi_s(sigma_N e_r) = lambda_s chi_s(e_r)
lambda_s^N = 1
```

With normalized cochains, this forces:

```text
chi_s(e_r) = N^(-1/2) lambda_s^r
```

The orientation choice fixed by positive QBL successor gives `lambda_s = exp(-2*pi*i*s/N)`.

## Collapse consistency

Same-chart reads remain exactly the old diagonal Orthad trace:

```text
Omega_after_L1 = diag(i/4895, 1)
Omega_after_L2 = diag(i/4895, -i/62423800998, 1)
```

The second denominator records the corrected product:

```text
196418 * 317811 = 62423800998
```

## Files

- `sealed/`: frozen native transfer tables and hash manifest written before comparison.
- `outputs/`: property gates, post-seal comparisons, collapse checks, source registry.
- `scripts/`: executable reproduction script.
- `notebooks/`: one-cell-per-claim verification notebook.
- `proofs/` and `lean/`: Lean theorem surfaces.
- `snapshots/`: prior Phase 5 v1 packages.
