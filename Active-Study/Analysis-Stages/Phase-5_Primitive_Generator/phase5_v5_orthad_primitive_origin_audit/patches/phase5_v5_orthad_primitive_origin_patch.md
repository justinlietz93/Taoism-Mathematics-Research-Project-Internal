# Phase 5 v5 source patch

Replace the one-lens-only Orthad origin paragraph with:

The Orthad readout has two canonically related chart forms on a finite cyclic orientation carrier. The plus chart is the point-orientation chart induced by the QBL orientation successor. The minus chart is the successor eigen-cochain chart forced by finite closure, unitarity, and the declared QBL orientation sign. Cross-chart transfer is the evaluation of the cochain chart on the point chart. Off-diagonal transfer entries are therefore not inserted into a diagonal lens; they are chart-transfer coefficients. The same-chart diagonal lens remains the collapse/self-read subcase.

Choice point ledger:
- finite cyclic carrier closure is a canon dependency;
- basepoint is gauge;
- orientation sign selects transfer direction;
- normalization is forced by unitarity;
- even N is required for quadratic self-twist.
