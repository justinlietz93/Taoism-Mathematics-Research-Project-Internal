# Phase 5 v2 Canon Patch

Replace the old one-lens-only Orthad statement with:

```text
The Orthad readout is an overset dual-lens structure.
The old diagonal lens is the same-chart self-read subcase.
A complementary dual eigen-cochain chart is induced by the native cyclic orientation successor.
Cross-chart transfer gives the orientation-pairing matrix.
The reverse transfer pairing has a same-chart quadratic refinement, the Orthad self-twist.
```

Do not reintroduce macro symbols S/T for Phase Calculus operators. External modular comparison targets are named Weil_F_N and Weil_G_N.
