# Phase 5 v6: Regression Against Failed Branches and Stale Canon Repair

## Claim

The corrected overset Orthad canon explains every failed Phase 5 branch, preserves the corrected positive result, and identifies stale documentation patterns that would recreate the old failures.

## Latest corrected result

The Orthad is canonically an overset dual-lens readout.

```text
same-chart self-read:
  old diagonal retained trace

cross-chart transfer:
  K_N(r,s) = N^(-1/2) exp(-2πi r s/N)

reverse-transfer quadratic self-twist:
  G_N(r) = exp(πi r²/N)
```

The one-lens diagonal Orthad is not deleted. It is demoted to its correct role: the same-chart collapse/subcase.

## Regression result

Every earlier Phase 5 v1 failure is now classified as one of:

1. banned-method control,
2. source-canon repair,
3. insufficient one-chart subcase,
4. wrong growth-channel control,
5. wrong scale-observable control,
6. one-chart overlap control,
7. forbidden false pass,
8. corrected overset generation branch.

The failure trail is preserved and made protective. The old mistakes are now explicit regression tests.

## Stale-documentation result

The package defines stale phrase patterns and repair rules. A local scan of available Phase/Shadow package text found stale or historical language occurrences. These are not automatically errors because some occur inside negative-control/history documents. They are patch-review targets.

## Gate

A future document is current-canon compatible only if:

- it does not define the Orthad as diagonal-only,
- it does not revive `S=Q` or `T=L` macro collision,
- it does not treat scalar Shadow projection failure as non-closure,
- it does not say Fourier/Gauss are B/L or Q by name alone,
- it preserves sealed comparison discipline.
