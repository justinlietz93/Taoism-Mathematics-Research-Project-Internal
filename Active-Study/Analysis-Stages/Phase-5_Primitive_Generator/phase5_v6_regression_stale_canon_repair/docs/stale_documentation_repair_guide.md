# Stale Documentation Repair Guide

## Patch rules

### Diagonal-only Orthad

STALE:

```text
The Orthad lens is diagonal.
```

CORRECTED:

```text
The diagonal lens is the same-chart collapse of the Orthad. The full Orthad is overset dual-lens.
```

### Shadow externality

STALE:

```text
The Shadow Residual remains external / never loaded into the lift.
```

CORRECTED:

```text
Scalar mock-theta coefficient/readout remains external. Retained residue-orientation vector and Orthad overset transfer/self-twist structure are carriable state.
```

### Macro collision

STALE:

```text
R = Q∘B, S = Q, T = L
```

CORRECTED:

```text
Deprecated selector shorthands. Do not use S/T for Phase macros when Weil generators are in scope. Use non-colliding labels and derive earned Macro_QBL separately.
```

### Fourier/Gauss naming contamination

STALE:

```text
B/L dual seating equals Fourier.
Q square seating equals Gauss.
```

CORRECTED:

```text
Fourier is generated by Orthad cross-chart transfer.
Gauss is generated by reverse-transfer quadratic self-twist.
```

### Scalar projection failure

STALE:

```text
Scalar projection failure means non-closure.
```

CORRECTED:

```text
Scalar projection failure is a projection-loss gate. It may mean the retained orientation carrier is required.
```
