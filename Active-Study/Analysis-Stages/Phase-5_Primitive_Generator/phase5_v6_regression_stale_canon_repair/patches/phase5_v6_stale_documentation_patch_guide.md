# Phase 5 v6 stale documentation patch guide

Apply these changes to stale canon files:

1. Replace diagonal-only Orthad definitions with overset dual-lens canon.
2. Preserve diagonal lens only as same-chart collapse.
3. Remove or mark deprecated `R=Q∘B, S=Q, T=L` macro grammar.
4. Replace `B/L dual seating equals Fourier` with `Fourier is generated by Orthad cross-chart transfer`.
5. Replace `Q square seating equals Gauss` with `Gauss is generated by reverse-transfer quadratic self-twist`.
6. Split Shadow externality: scalar coefficient/readout external; retained orientation-residue vector carriable.
7. Require sealed native-object derivation before external target comparison.
