# Phase 5 v1 Canon Repair and Corrected Protocol

STATUS: REOPENED_CANON_CONTAMINATION_REPAIRED_CORRECTED_GENERATION_TEST_OPEN

This package preserves the original Phase 5 v1 and reopened Phase 5 v1 evidence, then adds the source-level correction demanded before any further generation verdict.

## Final state

- Phase 5 v1 is open.
- The old bare-primitive residuals are retained as method-control evidence.
- The degenerate macro grammar `R=Q∘B, S=Q, T=L` is suspended as macro grammar.
- PC macro/selector symbols are renamed to avoid collision with Weil `S/T`.
- The no-shadow-in-lift law is split into scalar-readout externality vs retained-orientation carriability.
- The Orthad lens is allowed to carry orientation-index pairing blocks.
- No corrected falsifier is triggered because explicit QBL words and terminal Orthad projection are not yet derived.

## Corrected target

Find explicit finite QBL words `W_G_N` and `W_F_N`, read them through Orthad with retained orientation pairing, and compare the resulting readout-level operators to `Weil_G_N` and `Weil_F_N` without retuning.
