# Results

Generated by:

```bash
PYTHONPATH=src python3 -m orthad_voidwalker_overset.run_smoke
PYTHONPATH=src python3 -m pytest -q
python3 formal/cocycle_sympy_check.py
```

## Gate card

```json
{
  "package": "orthad_voidwalker_overset",
  "claim_class": "bounded smoke/adversarial harness, not theorem",
  "base_gates": {
    "legal_rewrite_invariance": true,
    "same_projection_different_retained_separation": true,
    "illegal_cocycle_rejected_by_walkers": true
  },
  "random_stress": {
    "legal_cases": 40,
    "illegal_cases": 20,
    "legal_pass_rate": 1.0,
    "illegal_pass_rate": 1.0,
    "min_legal_coupling_echo_gain": 1.0,
    "median_legal_coupling_echo_gain": 1.0,
    "max_good_topology_spikes": 0,
    "min_bad_topology_spikes": 485
  },
  "global_pass": true
}
```

## What passed

1. H1 and H2 are legal rewrite variants. They produced the same visible projection and the same retained coupling signature.
2. H1 and H3 have the same visible projection but different retained coupling signatures.
3. H4 corrupts one overlap latch. Void walkers discovered nonzero cycle residuals and emitted topology spikes without being handed the final residual.
4. Random legal histories had no topology spikes and extracted the correct coupling matrix.
5. Random illegal histories produced topology spikes and failed admission.
6. Legal discovered coupling achieved positive echo recovery against the blind no-coupling baseline.

## What this does not prove

This does not prove the arbitrary Orthad theorem. The harness currently uses potential-derived legal histories, triangle-cycle cocycle checks, and a small finite modulus. It does not yet implement a true gauge quotient, all Q/B/L primitive details, product-module Weil-compatible K/G operators, or arbitrary high-depth lifted histories.

## Decision

This is worth one hardened follow-up pass. The idea should not be sidelined solely because the earlier CEG scaffold was incomplete. This version shows that the missing VDM topology layer can operate on the overset graph shape and can separate legal retained coupling from same-projection/different-history and illegal-cocycle cases.
