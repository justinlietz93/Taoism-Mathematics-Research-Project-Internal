# 27 - Liu 2022 QBL bridge perspective pass

Source added by user:

`Pasted text.txt`

Related dossier files:

- `25_LIU_2022_DATA_CONSTRAINED_MHD_SQUEEZE_DRY.md`
- `26_RIGOR_CORRECTION_LIU_BRIDGE_NOT_EXHAUSTED.md`

## Correction to prior posture

Do not classify Liu 2022 as weak on Q/B/L because the direct symbols do not match.

Correct posture:

```text
LIU_2022_BRIDGE_OPEN_AND_HIGH_VALUE
```

The user-provided perspective should not be accepted as proven identity, but it must be treated as a structured hypothesis set with named bridge claims and falsification gates.

## Main reframing

The useful comparison is not:

```text
solar squashing-factor Q == primitive Phase Q
```

The useful comparison is:

```text
retained 3D magnetic topology
-> horizontal/field-line mapping distortion
-> squashing-factor Q diagnostic
-> high-Q support footprints / QSLs / HFT ribbons
-> reconnection/lift event
-> synthetic AIA projection
```

This makes solar `Q` a possible **derived diagnostic / support-channel readout**, not a primitive operator.

The primitive-operator bridge must be searched one layer deeper:

```text
primitive Q candidate:
  accumulated twist / oriented transfer / phase-like field-line winding

primitive B candidate:
  gradient/current-sheet sharpening at HFT/QSL until topology can no longer remain in the prior connectivity class

primitive L candidate:
  topological re-articulation/promotion: P1 -> P3 footpoint change, upper/lower structure separation, CME lift

projection candidate:
  synthetic AIA 94/304 line-of-sight images after retained MHD evolution
```

## Claim ladder

### Claim 1 - Retained state before projection

Status:

```text
STRONG_CONFIRMED
```

Liu 2022 is strongest here. Observed AIA images constrain and validate the model, but the simulation evolves a retained 3D MHD state. Synthetic AIA images are produced after the retained state evolves.

Phase bridge:

```text
Compute in lift / retained state first.
Project at boundary / observation channel afterward.
```

This is a true structural bridge and should remain locked.

### Claim 2 - Squashing-factor Q as derived support-channel diagnostic

Status:

```text
HIGH_VALUE_BRIDGE_OPEN
```

Solar squashing-factor Q should be treated as a scalar diagnostic of field-line mapping distortion and boundary support, not as primitive Phase Q.

Bridge target:

```text
QSL / HFT high-Q footprint
~ support-channel / lens-readout diagnostic over retained topology
```

Needed next:

```text
Extract Liu's exact definition/source convention for squashing factor Q.
Compare it to field-line map Jacobian / horizontal variation.
Then compare that map-level tensor to CF00 QGT g/Omega split.
```

Do not claim exact derivation until the map-Jacobian relation is explicitly written.

### Claim 3 - Q primitive bridge through twist/oriented transfer

Status:

```text
BRIDGE_OPEN_NOT_PROVEN
```

The primitive Q bridge is not the squashing-factor name. It is the physical accumulation of orientation/twist/shear before reconnection.

Candidate bridge:

```text
primitive Q:
  quarter-continuation / oriented transfer / phase accumulation

Liu analogue:
  field-line twist, shear, footpoint migration, sigmoid rotation/connectivity deformation
```

Needed next:

```text
Locate Liu evidence for twist, shear, field-line rotation, writhe, or footpoint displacement.
Separate ideal-MHD twist accumulation from reconnection-driven connectivity change.
```

### Claim 4 - B bridge through HFT/QSL current-sheet sharpening

Status:

```text
BRIDGE_OPEN_NOT_PROVEN
```

The B bridge should not be searched as a visible Fibonacci floor. Search as admitted refinement/stress concentration under a retained topology.

Candidate bridge:

```text
primitive B:
  balanced refinement until next refinement exceeds phase-admitted capacity

Liu analogue:
  current-density / squashing / HFT compression grows until reconnection is triggered
```

Needed next:

```text
Extract current density evolution, HFT/current-sheet formation, decay-index contour, and reconnection timings.
Determine whether there is a thresholded admissibility structure:
  allowed stress accumulation -> blocked same-connectivity continuation -> reconnection.
```

Do not claim the B floor-anchor is present until a capacity/threshold variable is identified.

### Claim 5 - L bridge through topological re-articulation

Status:

```text
HIGH_VALUE_BRIDGE_OPEN
```

The user text correctly shifts attention to ORS/topological re-articulation. Liu contains a strong physical re-articulation pattern:

```text
null-point reconnection changes high-channel footpoint P1 -> P3
HFT tether-cutting separates upper sigmoid from lower filament
upper channel erupts
lower filament remains stable
```

This is not pure Yin-Yang-grid re-chart, but it is a real topological custody/lift candidate.

Needed next:

```text
Identify what is preserved across reconnection.
Identify what is cut/reconnected.
Identify which structure is promoted/lifted and which remains retained.
```

### Claim 6 - J+M split / reversible-irreversible dynamics

Status:

```text
PLAUSIBLE_HIGH_VALUE_BRIDGE_OPEN
```

The bridge is plausible because ideal MHD instability and reconnection/dissipation are different dynamical regimes.

But the strong statement must be earned by extracting the paper's equations and terms:

```text
ideal transport / Lorentz-force evolution / energy terms
nonideal/numerical diffusion / heat conduction / gamma=1.1 / reconnection heating
```

Then compare to:

```text
conservative channel
+ dissipative/entropy-producing channel
```

Do not say Liu proves VDM's J+M split. Say Liu is a high-value physical case for testing the split.

### Claim 7 - Missing slow-rise phase as skipped prehistory

Status:

```text
BRIDGE_OPEN_WITH_STRONG_CAUTION
```

The user's pasted text proposes that Liu's unstable initial state has skipped the slow-rise accumulation and that Phase Calculus may quantify the missing prehistory.

This is valuable, but the exact count must not be asserted from stale macro grammar.

Correct status:

```text
SLOW_RISE_PREHISTORY_BRIDGE_OPEN
EXACT_R9S54T_COUNT_NOT_LOCKED
```

Needed next:

```text
Extract Liu's statement about unstable initial condition and absent slow-rise phase.
Extract decay-index threshold and initial MFR stability details.
Compare to Phase capacity/floor mechanics only after latest Phase source is available or independently reconstructed.
```

## Corrected classification table

| Layer | Correct classification | Why |
|---|---|---|
| Retained state / projection | `STRONG_CONFIRMED` | Liu evolves 3D MHD state before synthetic AIA projection. |
| Squashing-factor Q | `DERIVED_SUPPORT_DIAGNOSTIC_BRIDGE_OPEN` | Not primitive Q, but likely a boundary/topology readout. |
| Primitive Q | `TWIST_ORIENTED_TRANSFER_BRIDGE_OPEN` | Search twist/shear/phase accumulation, not name match. |
| Primitive B | `HFT_REFINEMENT_THRESHOLD_BRIDGE_OPEN` | Search stress/current-sheet/squashing concentration under admissibility. |
| Primitive L | `TOPOLOGICAL_REARTICULATION_BRIDGE_OPEN` | P1->P3 and upper/lower separation are strong candidates. |
| J+M dynamics | `PLAUSIBLE_HIGH_VALUE_BRIDGE_OPEN` | Need equation-level comparison. |
| Slow-rise gap | `PREHISTORY_BRIDGE_OPEN` | Do not assert exact macro count yet. |

## Updated rigor rule

```text
Never collapse a physics bridge to WEAK because the first direct symbol match fails.

For physical papers, separate:
  name match,
  diagnostic match,
  operator match,
  threshold match,
  projection match,
  invariant/custody match.
```

## Next Liu-specific work order

1. Extract exact squashing-factor Q definition/convention from Liu or cited Titov source.
2. Build a table of Liu mechanisms:
   - twist/shear/orientation accumulation,
   - HFT/QSL/current-sheet concentration,
   - decay-index/torus threshold,
   - null reconnection P1->P3,
   - tether-cutting separation,
   - synthetic AIA projection.
3. For each mechanism, mark:
   - retained variable,
   - diagnostic/readout,
   - threshold/blockage,
   - transition/lift,
   - invariant/leakage risk.
4. Only then assign stronger or weaker status.

## Reunified understanding

Liu 2022 should be treated as a physics bridge resource, not just a projection-side example.

The correct integrated hypothesis is:

```text
Phase Calculus may describe the discrete custody grammar behind a class of continuous topological MHD transitions.

Liu 2022 is a candidate continuous physical shadow:
  retained state -> topology diagnostics -> thresholded reconnection -> selective lift -> projected observations.
```

This is not proven identity. It is a strong, nontrivial bridge target.
