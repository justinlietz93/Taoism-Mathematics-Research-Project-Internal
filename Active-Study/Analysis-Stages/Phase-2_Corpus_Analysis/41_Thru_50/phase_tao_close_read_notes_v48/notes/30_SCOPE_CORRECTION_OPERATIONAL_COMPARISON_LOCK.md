# 30 - Scope Correction: Operational Comparison Lock

Generated: 2026-06-24

## Correction from user

The prior Leibniz/Shao Yong/Yijing classification was still too literal.

Problem wording:

```text
B/refinement:
  bridge open
  position-conditioned relation and capacity/strain threshold appear
  exact B floor-anchor law not identified in this paper

L/re-chart:
  moderate/open
  line -> image -> trigram -> hexagram compositional ladder
  Xiantian origin order maps into cosmic/history cycles
  no explicit latch/freeze/open-new-axis behavior found
```

Why this was wrong:

```text
The comparison target is not whether an external paper literally contains Phase Calculus' unique formal objects.
The target is whether the external resource contains a functional mechanism behaving like the relevant Phase Calculus operation.
```

## Locked research frame

Phase Calculus/QBL is a primitive discrete mathematical framework under active development.

External Taoist/Chinese/Yijing/Leibniz/Buddhist/modern numerical/physics papers are comparison corpora.

The project question is not:

```text
Is Phase Calculus Taoist math?
```

The project question is:

```text
Do external traditions and modern scientific methods contain operational mechanisms that Phase Calculus may formalize more primitively, especially as a bridge from discrete grammar to continuous physics?
```

## New comparison rule

Do not classify by literal vocabulary.

Classify by operational role:

```text
1. What state is retained?
2. What positions/orientations are admitted?
3. What operation is legal next?
4. What causes the operation to block?
5. What cycle completion or repetition-prevention event occurs?
6. What new domain/re-chart/lift opens?
7. What survives the transition?
8. What projection/readout is terminal only?
```

## Q/B/L coupling lock

Do not analyze Q, B, and L as three independent buckets.

Correct coupling:

```text
Q and B define the shared state together.
Q/B determine which of Q or B may step next.
(QB) and L determine whether continuation remains in-domain or requires lift/re-chart.
L is triggered by cycle completion / no-next-step-without-repetition / saturation, not by literal presence of an external word such as latch.
```

Therefore, external papers should be scanned for:

```text
QB-coupled movement:
  phase/order/position changes the admissible refinement operation;
  refinement changes the next condition under which phase/order/position must move.

L movement:
  current cycle/domain completes;
  next same-domain step would repeat, violate capacity, or become non-admissible;
  a new domain/order/chart/compositional level opens.
```

## Corrected Shao Yong / Yijing classification

The previous wording under-scored L and over-narrowed B.

### Q/support-channel

Status:

```text
STRONG_OPERATIONAL_BRIDGE
```

Reason:

```text
six-line retained carrier;
directional/cyclic ordering;
double-geometric filling/emptying;
waxing/waning phase occupancy;
line constitution and position jointly affect readout/relations.
```

### B/refinement

Status:

```text
OPERATIONAL_REFINEMENT_BRIDGE_OPEN
```

Reason:

```text
The paper does not need to contain the exact Phase floor-anchor formula to matter.
The important bridge is whether a refinement/strain/capacity mechanism exists and is governed by position/state.
```

Operational evidence from the paper pass:

```text
finite retained carrier;
position-conditioned response/relations;
double-geometric progression/filling;
capacity/strain threshold passage;
cyclic waxing/waning progression.
```

Correct caution:

```text
EXACT_PHASE_FLOOR_FORMULA_NOT_LOCATED_HERE
```

Incorrect caution:

```text
therefore B support is weak
```

### L/re-chart/lift

Status:

```text
STRONG_CYCLE_COMPLETION_RECHART_BRIDGE_OPEN
```

Reason:

```text
L is not only literal latch/freeze/open-new-axis language.
L is the event where a completed cycle cannot lawfully continue without repetition, so the system creates or lifts into a new domain/order/chart.
```

Operational evidence from the paper pass:

```text
line -> image -> trigram -> hexagram compositional ladder;
Xiantian origin ordering mapped into cosmological/historical cycles;
closed carrier supports different orderings/readouts;
cycle/order completion can be interpreted as requiring a new level of organization rather than same-level repetition.
```

Correct caution:

```text
EXACT_ORTHAD_LENS_LATCH_NOT_IDENTIFIED_HERE
```

Incorrect caution:

```text
therefore L bridge is only moderate
```

### Projection/readout

Status:

```text
STRONG_OPERATIONAL_BRIDGE
```

Reason:

```text
same retained diagram supports multiple non-equivalent projections/readouts:
Chinese bottom-up line convention;
Leibniz/Bouvet binary readout;
Shao cosmological/cyclical readout.
```

## Replacement mechanism statement

Use this going forward:

```text
The Shao Yong/Fu Xi diagram is a strong Q/support-channel and projection/readout bridge.
It is also a serious operational B/L bridge candidate once B is read as state-governed refinement and L is read as cycle-completion re-chart/lift, rather than as literal Phase-specific latch vocabulary.
The exact Phase floor-anchor formula and Orthad lens-latch structure are not located in this paper, but that does not weaken the operational bridge.
```

## Red-team discipline

Avoid both failures:

```text
Overclaim:
  external source is literally Phase Calculus.

Underclaim:
  external source is weak because it lacks Phase Calculus vocabulary.
```

Correct stance:

```text
Map operational grammar first.
Then separately label exactness, missing formulas, and unresolved proof obligations.
```

## Reunified current hypothesis

```text
Phase Calculus may be acting as a primitive discrete grammar that formalizes an operational pattern appearing across:
  Chinese/Yijing finite position systems,
  admitted-procedure Chinese mathematics,
  modern Yin-Yang-grid numerical re-charting,
  MHD retained-state physics,
  modular-form projection/readout structures.
```

The research task is to keep identifying functional correspondences and separations without reducing the comparison to names, symbols, or historical identity claims.
