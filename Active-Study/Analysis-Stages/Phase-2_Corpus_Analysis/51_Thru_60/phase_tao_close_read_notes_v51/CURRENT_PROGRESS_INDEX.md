# Current progress index

## Latest package

`phase_tao_close_read_notes_v51.zip`

## Latest pass

`notes/57_MONODROMY_MEMORY_STATE_COMPLETENESS_PASS.md`

## Latest revealing finding

Monodromy as Memory supplies the clean internal negative control for projection/readout drift:

```text
same stripped visible projection
  does not imply
same full lifted-register transition
```

The double-cover witness has the same visible projection for the empty loop and a two-turn loop, but the retained register changes:

```text
empty:    sheet=0, kappa=0, history=[]
two-turn: sheet=0, kappa=2, history=[g,g]
```

The quintic-like branch witness adds a nonidentity commutator:

```text
[a,b] = a b a^-1 b^-1
permutation = [0,2,4,3,1]
cycle = (1 2 4)
```

Therefore sheet, kappa/winding, history, and generator/action registry are carried state, not metadata.

## Current bridge spine

```text
retained carrier/state
-> admissible operation
-> carrier-specific refinement/update arithmetic
-> noncommutative hidden order / action registry
-> monodromy/history register
-> completion / blockage / threshold
-> carry / lift / re-chart
-> boundary readout
-> scalar only as terminal projection
-> invariant/custody preservation
-> state-completeness negative controls
-> chart-basis / interpolation-registry completeness
-> stale-canon layer separation
```

## Current progress estimate

```text
paper/resource first-pass coverage: ~98.8%
code/data bridge coverage: ~98.2%
overall project research pass: ~96.4%
```

## Highest-value open blockers

1. Ancient Yi full translation and exact line-order proof.
2. Latest active Phase selector/floor source from HDD.
3. Liu2022 QSL/current/connectivity time series.
4. Local Lean execution unavailable; proof surfaces indexed but not executed here.
5. Optional exhaustive semantic audit of all 384 Wilhelm line readings.
6. Optional full build/test audit of xi/farey assembly surfaces.
