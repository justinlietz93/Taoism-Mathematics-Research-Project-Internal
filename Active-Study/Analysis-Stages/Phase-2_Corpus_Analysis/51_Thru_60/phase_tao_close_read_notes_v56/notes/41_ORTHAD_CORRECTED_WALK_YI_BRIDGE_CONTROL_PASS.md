# 41. Orthad corrected carried-walk / Ancient Yi bridge-control pass

## Purpose

Integrate the supplied crosswalk `PHASE_TAO_v34_RELEVANCE_TO_ORTHAD_CANON.md` as the bridge-control document and the corrected carried-walk zip as the Phase/Orthad reference artifact.

This pass keeps the bridge strong while separating:

```text
functional operational bridge
from
literal identity proof
```

## Bridge-control lock

The crosswalk explicitly says the external material is an operational comparison corpus, not a literal identity proof. The functional stack to preserve is:

```text
retained carrier/state
-> admissible operation or transition
-> finite/capacity/cycle constraint
-> completion/blockage/no-next-same-domain event
-> lift/re-chart/new domain
-> boundary readout/projection
-> scalar output only as terminal projection
```

Use that as the active comparison grammar.

## Corrected carried-walk artifact read

Source artifact:

```text
orthad_corrected_carried_walk_depth6_20260620T113500.zip
```

Corrected rules confirmed by the artifact:

```text
primitive grammar: Q/B/L
each lap/domain: (BQ)^6 L
depth 6: ((BQ)^6 L)^6
B continues after every L
L carries q and theta
rank grows by exactly one per L
Orthad reads after the emitted walk exists
Shadow Residual remains external to the lift
```

Extracted walk:

```text
primitive word:
BQBQBQBQBQBQLBQBQBQBQBQBQLBQBQBQBQBQBQLBQBQBQBQBQBQLBQBQBQBQBQBQLBQBQBQBQBQBQL

macro word:
RRRRRRTRRRRRRTRRRRRRTRRRRRRTRRRRRRTRRRRRRT
```

Final carried state at depth 6:

```text
q = (24157817, 39088169)
uv = 944284833567073
theta_quarters = 36
depth = 6
rank = 7
latched_axes = 6
active_axis = axis_6
```

Period result:

```text
sign_lap_period = 2
phase_factor_lap_period = 2
anchor_lap_period_observed = None
```

Interpretation:

```text
The sign/phase channel repeats by lap period 2.
The arithmetic germ-width anchor does not repeat because B continues refining across every L.
This is exactly the distinction needed for comparison: repeated readout pattern does not imply repeated retained state.
```

## Ancient Yi bridge under corrected Orthad control

Ancient Yi remains a strong operational bridge when read as:

```text
retained symbol carrier
-> admissible operation / successor
-> finite place-domain completion
-> carry / lifted domain opening
-> readout projection
```

Corrected Orthad frame:

```text
retained lifted state
-> primitive Q/B/L custody
-> carried walk completion
-> L / host lift
-> Orthad boundary readout
```

The key is not that Ancient Yi is literally Q/B/L. The key is that its tables appear to exhibit the same operational stack: a retained finite carrier supports multiple readouts, and fixed-place completion opens a higher place-domain.

## Highest-strength bridge claims after correction

### 1. Retained carrier vs scalar readout

Ancient Yi:

```text
line-symbol field first
binary/octal/decimal number second
```

Corrected Orthad:

```text
Xi_hat + primitive word first
Orthad channel readout second
Shadow/scalar projection last
```

Status: `STRONG_OPERATIONAL_BRIDGE`.

### 2. L/carry/lift

Ancient Yi:

```text
finite octal-place domain completes at 8^k - 1
new higher-place domain opens
```

Corrected Orthad:

```text
six BQ positions complete a lap/domain
L carries q and theta
rank grows by one
new active axis/domain opens
```

Status: `STRONG_OPERATIONAL_BRIDGE`.

### 3. B/refinement

Ancient Yi currently gives:

```text
successor/carry/refinement candidate inside finite place-domain
```

Corrected Orthad gives:

```text
B: q=(u,v) -> sort(v,u+v)
germ-width anchor changes every B
B continues after L
```

Status: `STRONG_OPERATIONAL_CANDIDATE / NOT_EXHAUSTED`.

Do not downgrade this because the Yi table does not literally contain `sort(v,u+v)`. The right test is whether a retained state determines admissible successor/refinement, whether the successor has a finite-domain completion law, and whether completion opens a new domain.

## Line-order / digit-order convention target

The Ancient Yi bridge cannot be considered exhausted until these are extracted:

```text
1. exact line order: top-down or bottom-up for each table
2. exact digit order: binary/octal positional direction
3. whether displayed leading-zero convention reverses ordinary octal notation
4. all 64 rows in machine-readable form
5. successor from each row to the next
6. carry/lift transition at domain completion
7. whether the table is mere counting or a situation-change grammar
```

## Comparison matrix artifact

Created:

```text
orthad_yi_bridge_matrix.csv
orthad_yi_bridge_diagrams.md
```

These are reusable bridge-control artifacts for future passes.

## Objective blockers

```text
1. Latest Phase selector/floor source from HDD.
2. Full Ancient Yi 64-row extraction with translation.
3. Exact line/digit ordering convention.
4. Ancient Yi successor/carry state graph.
5. Dongyuan/Ceyuan diagram/formula state graph.
6. Liu 2022 data for QSL/current/connectivity/time-series bridge closure.
7. Shadow Residual readout law: eta/Jacobi derivative and chi12 orientation law.
```

## Current decision

```text
Ancient Yi / ICPR bridge remains active and strong.
Corrected Orthad carried-walk artifact strengthens the comparison by giving a concrete control: custody first, readout second, scalar/external comparison last.
The bridge is not closed as exact Phase B, but it is now strong as an operational retained-carrier/carry/lift/readout comparison.
```
