# Results

## Verdict

```text
CORRECTED_CARRIED_WALK_DEPTH6_COMPLETED
PREVIOUS_DEPTH6_PACKAGE_INVALIDATED
```

## What was changed from the broken package

```text
removed uv > 4096 cutoff
removed fabricated capacity ladder
removed claim that clean negation breaks at depth 3
removed flat germ-width conclusion
used BQ interleaving in every domain
used L as rank +1 only
kept θ and q carried across L
```

## Correct walk rule used

For each lap/domain:

```text
(BQ)^6 L
```

For depth 6:

```text
((BQ)^6 L)^6
```

## State carry

Final carried state after depth 6:

```text
q = (24157817, 39088169)
uv = 944284833567073
theta_quarters = 36
depth = 6
latched_axes = 6
```

## Sign lap period

The signed position list is period 2 by lap:

```text
lap 0 = + - - + + -
lap 1 = - + + - - + = -lap 0
lap 2 = + - - + + - = lap 0
```

## Channel status

- `sign`: emitted by carried phase `i^k`; period 2 by lap.
- `support_seats`: emitted by carried Q-seat order; increases continuously.
- `germ_width_anchor`: emitted by B; continues refining after every L.
- `inter_term_phase`: emitted by successive Q ticks; local ratio remains `i`.
- `external_exponent_spacing`: only computed during final external q-series comparison. It is not emitted by the lift before comparison.

## First new channel resolution

The corrected run does not show a new threshold at depth 3. It shows that the arithmetic anchor was incorrectly flattened before. With B restored, the germ-width anchor continues immediately after the first L and is visible by depth 2.

## External χ12 check

The external reference is checked only after the emitted walk exists. Under the raw carried phase-sign extractor, the run resolves the n=7 collision but does not match χ12 on every supported residue. This means full orientation law is not closed by this run.
