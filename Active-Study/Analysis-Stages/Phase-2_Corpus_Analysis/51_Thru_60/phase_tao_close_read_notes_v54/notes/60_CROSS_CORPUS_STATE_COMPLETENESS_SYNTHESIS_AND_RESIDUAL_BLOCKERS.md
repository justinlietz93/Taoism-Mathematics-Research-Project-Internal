# 60 — Cross-corpus state-completeness synthesis and residual blocker pass

## Status

`v54` synthesis and gate-tightening pass.

This is genuine progress beyond a status update: it consolidates all internal and external bridge evidence into a single state-completeness kernel, projection-loss taxonomy, and residual blocker matrix.

## 1. Current thesis

Phase Calculus is being evaluated as a retained-state transition framework:

```text
retained carrier/state
-> admissible operation
-> carrier-specific refinement/update arithmetic
-> completion / blockage / threshold
-> carry / lift / re-chart
-> boundary readout
-> scalar only as terminal projection
-> invariant/custody preservation
-> state-completeness negative controls
```

The central research rule is now sharper:

```text
A projection is bridge-admissible only when it preserves enough retained state to determine the next admissible transition under the relevant operator family.
```

This rule covers all current corpora without forcing identity claims.

## 2. New files

| File | Purpose |
|---|---|
| `cross_corpus_minimal_retained_state_kernel_v54.csv` | minimal retained-state requirements for all major internal/external corpora |
| `projection_loss_collision_taxonomy_v54.csv` | taxonomy of projection-loss classes and concrete witnesses |
| `bridge_gate_master_status_v54.csv` | unified pass/partial/open status by gate |
| `bridge_residual_blockers_v54.csv` | remaining blockers split by internal/external track |
| `internal_external_progress_split_v54.csv` | separated progress percentages |
| `cross_corpus_state_completeness_synthesis_v54.json` | machine-readable synthesis specification |
| `scripts/cross_corpus_state_completeness_attack_v54.py` | executable negative-control witness attack |
| `proofs/CrossCorpusStateCompletenessV54.lean` | Lean companion proof surface |
| `notebooks/cross_corpus_state_completeness_attack_v54.ipynb` | notebook companion attack surface |

## 3. Latest revealing synthesis

The prior passes are now unified by projection-loss class:

```text
order loss:          noncommutative hidden central charge
history loss:        monodromy sheet/kappa/history
registry loss:       branch grammar generator/action registry
basis loss:          Pencil Yin-Yang chart vector basis
width/order loss:    Ancient Yi place width and LSD-first convention
semantic loss:       Wilhelm line text/category vs retained transition carrier
diagnostic loss:     MFE/Liu output channels vs full MHD custody
stale selector loss: Phase R/S/T/Tdelta witness layer vs corrected QBL/L carry
```

This is the strongest common bridge so far. The external corpora are not being used as identity proofs. They are now used as independent stress tests of the same retained-state rule.

## 4. Internal track update

Internal research is now effectively complete for the current bridge-research pass except for two hard blockers:

```text
latest active Phase selector/floor source from HDD;
local Lean/lake execution of proof surfaces.
```

The internal spine is stable:

```text
QBL primitive core
-> B as carried-state refinement, not one rigid origin-only sequence
-> corrected L carries q/theta and opens rank/domain
-> Orthad reads after emitted walk exists
-> stale RST/Tdelta reset layer demoted
-> projector hygiene enforced by state-completeness gates
```

## 5. External track update

External research is now near-complete for current bridge use.

Strong external bridges:

```text
Ancient Yi: retained symbol carrier, LSD-first octal carry/lift, scalar-terminal readout
Wilhelm: retained six-line event automaton, 384 selected-line emissions, semantic readout gate
Pencil/Yin-Yang: hard re-chart implementation with chart-basis state-completeness
MFE/Liu: retained field custody with boundary callbacks and terminal diagnostic channels
Chinese logic/automata/category: normalization language for state, operation, word, boundary, projection
```

Remaining external blockers:

```text
Ancient Yi full Chinese translation and exact line-order philological proof;
Liu2022 QSL/current/connectivity time-series data;
optional full Pencil/MFE build tests;
Shadow residual remains external reference, not closed inside the lift.
```

## 6. Progress

```text
Internal Research: 99.1%
External Research: 98.6%
Overall project research pass: 98.85%
```

Not 100%: the remaining blockers are real, not cosmetic.

## 7. Decision

```text
CONTINUE only if new source/data becomes available or final retrospective is requested.
Current package reaches terminal synthesis for available bridge-research evidence.
```
