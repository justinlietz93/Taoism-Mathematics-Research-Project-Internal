# 62 — Bridge claim dependency graph and terminal source boundary

## Status

`v56` bridge-dependency pass.

This pass converts the accumulated research into a machine-readable claim graph, with explicit dependencies, kill tests, and required missing artifacts. It does not declare 100% completion.

## 1. Current understanding

Phase Calculus is being tested as a retained-state transition framework:

```text
retained carrier/state
-> admissible operation
-> carrier-specific refinement/update arithmetic
-> completion / blockage / threshold
-> carry / lift / re-chart
-> boundary readout
-> scalar only as terminal projection
-> invariant/custody preservation
-> state-completeness negative controls
-> claim-dependency and source-boundary accounting
```

The strongest current bridge rule is:

```text
Projection equality is not state completeness.
A projection is only bridge-admissible when it preserves enough retained state
for the next admissible transition under the relevant operator family.
```

## 2. Genuine progress in v56

The previous packages had many strong results, but they were still distributed across notes and CSVs. v56 adds a dependency graph that tells us which results support which later claims.

The new graph has ten claim nodes:

```text
C01 carrier before readout
C02 state-completeness projection gate
C03 corrected Orthad custody
C04 B refinement scope correction
C05 modular-B operator-family research path
C06 L/carry/re-chart bridge
C07 Wilhelm event automata
C08 physical re-chart bridge: Pencil/MFE/Liu
C09 Shadow as external reference
C10 available-source terminal boundary
```

This gives the project a stable research spine. It also prevents the recurring failure mode where a terminal projection, scalar label, semantic reading, diagnostic image, or primitive word gets mistaken for the carried object.

## 3. Latest revealing findings

### 3.1 The bridge is now claim-dependent, not resemblance-dependent

The bridge does not rest on visual or semantic resemblance. It rests on dependency preservation:

```text
Does the external system preserve retained state?
Does it have an admissible transition/update operator?
Does completion/blockage open carry/lift/re-chart?
Does readout happen after custody?
Does the projection preserve enough state for the next transition?
```

### 3.2 Modular-B is correctly contained

Ancient Yi base-8 carry is now classified as:

```text
OPEN_RESEARCH_CANDIDATE_STRONG
```

not as exact Phase B and not as weak analogy.

The dependency graph preserves the strongest version:

```text
B-like refinement may be carrier/domain-selected arithmetic.
Canonical Phase-origin B follows the Fibonacci/Farey corridor.
Non-origin/asymmetric B starts are valid carried-state refinements.
Ancient Yi base-8 successor/carry may be a different carrier-selected arithmetic.
```

### 3.3 100% is blocked by exact artifacts, not by ordinary reading backlog

The hard blockers are now explicit:

```text
latest active Phase selector/floor source from HDD;
working Lean/lake proof environment;
Ancient Yi full table extraction/translation and exact line-order proof;
Liu2022 QSL/current/connectivity time-series data;
Shadow residual Jacobi/unary-theta transformation proof.
```

These blockers affect final proof, data validation, or philological certainty. They do not erase the current bridge.

## 4. Files added in v56

| File | Purpose |
|---|---|
| `bridge_claim_dependency_graph_v56.csv` | C01-C10 claim dependency graph |
| `bridge_dependency_closure_audit_v56.csv` | dependency/risk/closure status per claim |
| `internal_external_terminal_evidence_closure_v56.csv` | internal/external terminal evidence status |
| `claim_readiness_ladder_v56.csv` | safe claims, open claims, forbidden overreach, kill tests |
| `hard_blocker_decision_matrix_v56.csv` | exact missing artifacts blocking honest 100% |
| `retained_transition_normal_form_v56.csv` | minimal formal normal form: S, O, W, P, K |
| `bridge_research_progress_summary_v56.json` | machine-readable summary |
| `scripts/bridge_dependency_gate_attack_v56.py` | executable dependency/kill-test audit |
| `proofs/BridgeDependencyGateV56.lean` | Lean companion proof surface |
| `notebooks/bridge_dependency_gate_attack_v56.ipynb` | notebook companion attack |

## 5. Attack result

The Python attack checks that every claim has valid dependencies, kill tests, and explicit 100% requirements.

```text
PASS
claims=10
closed_or_current_locked=5
open_or_data_pending=5
hard_100_blockers=5
```

The companion Lean surface attacks the core logic:

```text
same projection does not imply same retained state;
same projection does not imply same next-transition code.
```

## 6. Progress

```text
Internal Research: 99.6%
External Research: 99.2%
Overall project research pass: 99.4%
```

## 7. Decision

Do not declare 100%.

The available-source bridge research is now terminalized into a claim-dependency graph. Further real movement requires at least one of the hard missing artifacts or a final retrospective request.
