# 56. Phase Formalisation Stale-Canon Split and Projector Hygiene Pass

**Status:** v50 deep internal close-read / corrected-canon reconciliation pass  
**Targets:** `Phase_Calculus_Complete_Formalisation.pdf`, `CF000_Primitive_Distinguishability.pdf`, `CF19_The_Full_Lifted_Object.pdf`, `Farey_Remainder_Recursion_and_Orthogonality.pdf`, corrected Orthad carried-walk artifact  
**Classification:** internal Phase source reconciliation / projector hygiene master gate / stale executable selector demotion / CF000-CF19-Farey bridge synthesis

## 1. Result

v50 resolves the highest-value remaining internal source blocker: the old complete-formalisation text must be split into three layers, not read as one canon surface.

```text
Layer 1 - primitive operator core:
  Q/B/L operator core remains useful and canon-compatible.

Layer 2 - executable macro/witness layer:
  R/S/T and Sigma_{W,Delta} are branch-local witness machinery.
  They are not corrected Orthad canon.

Layer 3 - projector hygiene / branch-memory layer:
  This remains strongly useful as a master bridge gate.
```

The important correction is direct:

```text
Formalisation Ch7 T:
  T(A,(u,v),t,...) -> (A+1,(1,A+1),t+1,...)

Corrected Orthad L:
  L carries q and theta;
  rank grows by exactly one per L;
  B continues after L;
  Orthad reads after the emitted walk exists.
```

Therefore Chapter 7 `T` is now explicitly marked:

```text
WITNESS_LAYER_ONLY / STALE_OR_PARTIAL_PHASE_SOURCE_DELTA
```

This preserves the strongest Phase/Orthad bridge by not allowing the older selector macro layer to overwrite the corrected carried-walk result.

## 2. Internal source split

| Surface | v50 status | Use |
|---|---:|---|
| Formalisation Chapter 5 Q/B/L core | CANON_COMPATIBLE_CORE_SURFACE | Keep as primitive operator-core source with stale-source caution. |
| Formalisation Chapter 7 R/S/T macro calculus | WITNESS_LAYER_ONLY | Use as historical executable witness, not native Orthad canon. |
| Formalisation Chapter 7 selector `Sigma_{W,Delta}` | STALE_OR_PARTIAL_PHASE_SOURCE_DELTA | Do not use as current operation-authoring law. |
| Formalisation Chapter 8 projector hygiene | STRONG_INTERNAL_GATE | Use as master state-completeness/projection-loss gate. |
| Formalisation no-later-domain-selector principle | SUPPORTS_DEMOTION | Prevents rigid selector universalization. |

## 3. Projector hygiene becomes the master bridge gate

The complete-formalisation projector hygiene clauses now unify the repeated findings from v41-v49:

```text
readout is admissible only as readout;
projection is state-complete only if it preserves the state needed for the next admissible transition;
branch certificates must report retained coordinates, action law, history, and quotient/projection loss.
```

This is now the common validation gate for:

| Corpus | Projection-loss target |
|---|---|
| Ancient Yi | decimal/octal/binary labels do not replace line/place carrier and digit-order convention |
| Wilhelm | ordinal/name/text does not replace six-line carrier plus R/C/RC operation registry |
| MFE/Liu | diagnostic output does not replace full field state plus boundary/nonideal channels |
| Pencil Yin-Yang | component values do not replace chart basis and interpolation registry |
| TDAHE | scalar `h_z` does not replace retained branch register |
| Branch grammar | primitive word does not replace generator/action registry |
| Noncommutative geometry | visible origin does not replace hidden central/order coordinate |
| Corrected Orthad | boundary readout does not replace q/theta/rank/axis/latches and emitted word |

## 4. CF000 / Farey / CF19 synthesis

v50 also closes the current CF000-CF19-Farey synthesis pass.

```text
CF000:
  same-axis saturation is not termination;
  exhaustion of lawful same-domain articulation forces orthogonal re-articulation.

Farey:
  canonical origin-path B has exact Fibonacci/Farey denominator-pair refinement;
  finite floor truncation is lawful orthogonal transfer and phase-sector replacement.

CF19:
  visible witness is not state-complete;
  exact return belongs to the completed lifted object, not the visible carrier alone.
```

This creates a clean internal spine:

```text
CF000 same-axis saturation
-> Farey finite-floor B/refinement and orthogonal transfer
-> CF19 completed lifted object and visible-witness obstruction
-> Formalisation projector hygiene
-> corrected Orthad carried-walk canon
```

## 5. SymPy / notebook / Lean attack surfaces

v50 adds a new companion attack set:

| Artifact | Status |
|---|---:|
| `scripts/projector_hygiene_selector_demote_attack_v50.py` | PASS locally |
| `projector_hygiene_selector_demote_attack_v50_output.json` | all gates pass |
| `notebooks/projector_hygiene_selector_demote_attack_v50.ipynb` | included with executed PASS output |
| `proofs/ProjectorHygieneSelectorDemoteV50.lean` | included; Lean not locally runnable in this container |

The executable attack verifies:

```text
corrected L carries q: PASS
old T does not carry q on non-origin state: PASS
visible phase projection collision exists: PASS
visible phase is not state-complete for next B: PASS
Ancient Yi 77 -> 001 LSD-first carry: PASS
SymPy non-injective hidden-coordinate projection collision: PASS
```

## 6. Latest revealing finding

The revealing finding is not just that older material is stale. It is sharper:

```text
The complete formalisation already contains the projector-hygiene machinery needed to demote its own older branch-local selector layer.
```

So the correct action is not rejection. The correct action is layer separation:

```text
keep Q/B/L operator core;
keep projector hygiene;
keep CF000/Farey/CF19 mechanism spine;
demote R/S/T selector closure to historical witness layer;
use corrected carried-walk artifact for Orthad canon.
```

## 7. New files from v50

```text
phase_core_cf000_cf19_farey_evidence_excerpts_v50.csv
phase_formalisation_stale_canon_demotions_v50.csv
phase_projector_hygiene_clause_inventory_v50.csv
phase_internal_core_bridge_synthesis_v50.csv
projector_hygiene_selector_demote_gate_results_v50.csv
projector_hygiene_selector_demote_attack_v50_output.json
scripts/projector_hygiene_selector_demote_attack_v50.py
notebooks/projector_hygiene_selector_demote_attack_v50.ipynb
proofs/ProjectorHygieneSelectorDemoteV50.lean
```

## 8. Updated status

```text
Paper/resource first-pass coverage:
  97.0% -> 98.2%

Code/data bridge coverage:
  97.0% -> 97.5%

Overall project research pass:
  93.4% -> 95.0%
```

## 9. Remaining blockers

```text
1. Ancient Yi full translation and exact line-order proof.
2. Latest active Phase selector/floor source from HDD.
3. Liu 2022 QSL/current/connectivity time series.
4. Full line-by-line proof execution in Lean, blocked by local Lean unavailability.
5. Optional full semantic audit of all 384 Wilhelm line readings.
```
