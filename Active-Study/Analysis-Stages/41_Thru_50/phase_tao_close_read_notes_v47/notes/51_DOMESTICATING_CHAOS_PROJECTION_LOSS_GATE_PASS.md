# 51. Domesticating Chaos projection-loss state-completeness gate pass

## Scope

This pass makes new research progress by integrating `Domesticating_Chaos` as the internal negative-control surface for the whole Phase/Tao/Yi/Orthad bridge program.

The goal is not to add another analogy. The goal is to sharpen the bridge gate:

```text
A projection, readout, scalar, ordinal, diagnostic, or event word is not the carried object unless it is state-complete for the next admissible transition.
```

This directly protects the current bridge from semantic drift while preserving its strongest operational form.

## New artifacts

- `domesticating_chaos_projection_loss_gate_audit_v45.csv`
- `domesticating_chaos_key_metrics_v45.csv`
- `projection_loss_state_completeness_bridge_matrix_v45.csv`
- `projection_loss_kill_tests_v45.csv`
- updated `coverage_percent_status.csv`
- updated `CURRENT_PROGRESS_INDEX.md`
- updated `01_LIVE_NOTES.md`
- updated `04_MECHANISM_LEDGER.md`

## Main finding

`Domesticating Chaos` supplies the missing internal negative-control template for all external bridge work.

It does two things at once:

1. It confirms that retained/lifted state can carry predictive structure that visible projection loses.
2. It logs where reduced projections fail, rather than hiding the failure.

That makes it a gate for the Yi/Wilhelm/MFE/Orthad comparison.

## Load-bearing evidence extracted

### Double-pendulum projection disproof

Lean encodes a finite counterexample:

```text
two states can share the same visible projected angles
while omitted momentum changes the next visible motion.
```

The JSON final verdict records the same obstruction as:

```text
projection that omits canonical momenta cannot be an exact one-step branch map.
```

Operational bridge consequence:

```text
same readout does not imply same carried state.
```

### Pendulum lifted trace vs visible-only projection

Extracted metrics:

```text
lifted event forecast max time error: 7.105427357601002e-15
visible-only mean event error: 3.681666666666666
reconstruction max abs error: 1.4210854715202004e-14
```

Interpretation:

```text
lifted trace carries event/turn memory;
visible wrapped angles lose it.
```

### N-body event word vs event word plus anchor

Extracted metrics:

```text
event-word-only continuous state error: 1.8923112444399534e-11
event-word-only passes 1e-14 gate: False
event-word-plus-anchor continuous state error: 0.0
event-word-plus-anchor passes 1e-14 gate: True
```

Interpretation:

```text
event word = branch-memory accounting;
event word + lifted anchor = state-complete replay carrier.
```

This is directly relevant to Orthad:

```text
primitive word alone is not automatically the entire carried object;
Xi_hat includes q, theta, rank/latches, and accrued word before readout.
```

## New cross-corpus state-completeness rule

The bridge gate is now:

```text
For any candidate bridge, ask:

1. What is the retained carrier?
2. What projection/readout/scalar could collide?
3. What additional anchor/context makes the state transition-complete?
4. Does the source preserve that anchor before readout?
5. Can a negative control show projection-only failure?
```

## Reunified application

| Corpus | Retained object | Insufficient projection | State-complete requirement |
|---|---|---|---|
| Corrected Orthad | Xi_hat + q/theta/rank/word | Shadow/scalar before walk | emitted walk plus carried q/theta/rank |
| Ancient Yi | line/place carrier | decimal or octal label alone | carrier plus digit-order/successor convention |
| Wilhelm | six-line carrier | ordinal/name/text alone | carrier plus R/C/RC or line-flip operation |
| MFE/Liu | full 3D field + boundary state | diagnostic image/output | field + ghost/boundary + RHS/update history |
| Domesticating Chaos | lifted trace/anchor + event word | visible angles or word alone | lifted anchor plus event/branch word |

## Why this is genuine progress

Before v45, the project had strong bridges for carrier/readout and lift/carry, but the kill test for projection loss was not applied uniformly across the bridge corpus.

After v45, the bridge framework has a reusable falsifier:

```text
If two distinct retained states share a readout but admit different next transitions, the readout is not state-complete.
```

This now controls:

```text
Ancient Yi scalar decimal readouts,
Wilhelm King Wen ordinals,
MFE/Liu diagnostic projections,
Orthad boundary channels,
and Phase internal event words.
```

## Updated bridge statuses

```text
Corrected Orthad retained-state discipline:
  PASS_REFERENCE_ANCHOR

Ancient Yi retained carrier/readout:
  STRONG_OPERATIONAL_BRIDGE

Ancient Yi scalar-terminal discipline:
  STRENGTHENED_BY_PROJECTION_LOSS_GATE

Wilhelm carrier-operation algebra:
  STRONG_OPERATIONAL_BRIDGE

Wilhelm ordinal-as-transition-law:
  REJECTED_BY_CARRIER_OPERATION_EVIDENCE

MFE/Liu retained field custody:
  STRONG_OPERATIONAL_BRIDGE

MFE/Liu diagnostic-as-state:
  DATA_DEPENDENT_NEGATIVE_CONTROL_PENDING

Modular-B research path:
  OPEN_AND_NOW_STATE_COMPLETENESS_GATED
```

## Current understanding

Phase Calculus is being tested as a primitive discrete framework that may normalize external systems by function:

```text
retained state
-> admissible operation
-> carrier-specific refinement arithmetic
-> completion / blockage / threshold
-> carry / lift / re-chart
-> boundary readout
-> scalar only as terminal projection
-> invariant/custody preservation
-> state-completeness negative controls
```

## Latest revealing findings

1. Domesticating Chaos supplies the missing internal red-team gate: projection-only and word-only reductions fail where lifted anchor/state succeeds.
2. This strengthens the Ancient Yi bridge because decimal/octal/binary scalar readouts are now clearly terminal projections from a retained carrier, not the carried object.
3. This strengthens the Wilhelm bridge because King Wen ordinal adjacency is now explicitly separated from carrier-operation law.
4. This sharpens MFE/Liu: AIA/output diagnostics cannot be treated as the dynamical state; boundary-complete 3D field custody remains the bridge target.
5. This sharpens Orthad: Orthad reads after the emitted walk exists; the Shadow Residual remains external and cannot be loaded into custody.

## Progress estimate

```text
paper/resource first-pass coverage: ~94.5%
code/data bridge coverage: ~91.5%
overall project research pass: ~88.6%
```

## Next blockers

```text
1. Full Ancient Yi table translation and line-order proof.
2. Latest active Phase selector/floor source from HDD.
3. Dongyuan/Ceyuan diagram-to-formula state graph.
4. Liu2022 QSL/current/connectivity data.
5. Formal modular-B operator-family theorem with explicit projection-loss controls.
```
