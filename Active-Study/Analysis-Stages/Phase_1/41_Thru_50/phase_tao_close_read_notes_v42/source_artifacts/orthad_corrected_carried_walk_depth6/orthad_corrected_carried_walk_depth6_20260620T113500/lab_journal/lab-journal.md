# Lab Journal

## Entry 1: reset

The previous depth-6 package is not used as evidence. It used a hard cutoff and then interpreted the artifact as structure. I treated it as failed work.

## Entry 2: canon lock

I used the current v0.6.7 canon and lens-matrix v3. The gates I enforced were: no candidates, no scalar, no reset, B and Q mutate active axis in place, L is the only rank extension, rank grows by one per L, carried phase continues through L.

## Entry 3: selector repair

I removed the `uv > 4096` cutoff. The corrected carried-phase proof uses the six-seat lap rule: six `BQ` ticks, then `L`. That means B fires in every domain.

## Entry 4: run observation

The sign lap list does not become a growing block structure. It has period 2. Lap 1 is the negation of lap 0. Lap 2 returns to lap 0. This matches the half-turn story.

## Entry 5: arithmetic channel

The germ-width anchor no longer stays flat. It refines every B tick across every boundary. That is the main structural correction from the failed package.

## Entry 6: external reference separation

I kept χ12 out of emission. The external comparison shows that the raw carried phase-sign extractor resolves the n=7 collision but does not match full χ12 orientation across all supported residues. I am not closing 15.4 from this run.

## Entry 7: challenge

The hard remaining problem is not position unfolding. It is the exact orientation law: how, if at all, Ω internally seats `(1, 11)` positive and `(5, 7)` negative without importing χ12.
