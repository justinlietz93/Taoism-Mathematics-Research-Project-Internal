# Orthad / Ancient Yi bridge diagrams

## Corrected Orthad custody/readout

```mermaid
flowchart LR
  X[Retained Xi_hat + word] --> C[Primitive custody: BQ ticks]
  C --> D[(BQ)^6 domain completion]
  D --> L[L host lift: carry q/theta, rank +1]
  L --> X2[New active axis/domain]
  L --> O[Orthad boundary readout]
  O --> S[Terminal scalar/external comparison only]
```

## Ancient Yi carrier/readout/carry bridge

```mermaid
flowchart LR
  Y[Retained line-symbol carrier] --> R[Readout convention]
  R --> B[Binary / octal / decimal projection]
  Y --> P[Fixed place-domain succession]
  P --> F[Finite domain completion: 8^k - 1]
  F --> N[Higher-place domain opens]
  N --> R2[New readout domain]
```

## Normalized operational stack

```mermaid
flowchart TD
  A[Retained carrier/state] --> B[Admissible transition / operation]
  B --> C[Finite/capacity/cycle constraint]
  C --> D[Completion / no next same-domain step]
  D --> E[Carry / lift / re-chart / new domain]
  E --> F[Boundary readout / projection]
  F --> G[Scalar output only as terminal projection]
```
