# Phase 3 v2 Repo Patch Instructions

Apply these status edits to the Phase-Tao research notes.

## Replace status

```text
Can B be generalized as a refinement operator schema over a retained carrier?
```

with:

```text
B generalizes as a retained-carrier refinement schema. Phase/Farey is the native instance. Ancient Yi base-8 place carry is an external full instance under the LSD-first carry model. Wilhelm supplies retained local transition and projection-loss evidence, with intrinsic completion/lift marked as the next source target.
```

## Update Modular-B row

```text
status = CONDITIONAL_CLASS_CLAIM_SUPPORTED
full_instances = Phase/Farey; Ancient Yi octal-place
boundary_instances = Wilhelm six-line mutation
next_target = Projection-Loss Theorem Across Corpus
```

## Add falsifier

```text
A counterexample is any claimed Modular-B domain where terminal projection determines the next admissible transition without retained carrier data.
```
