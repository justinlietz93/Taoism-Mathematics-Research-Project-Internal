# Lab Journal

## 2026-07-11: v7 standard-follower closure pass

1. Accepted the v6 affine ceiling, half-open partition, `J`, `P`, forbidden words, complexity, entropy, mixing, and finite boundary certificate.
2. Replaced the open-interior follower definition with the standard word follower set.
3. Proved the exact endpoint-preserving identity `C(wv)=C(w) intersect D^-|w|(C(v))`.
4. Derived `Fol(w)={v:H_w intersect C(v) is nonempty}` for the actual half-open follower region.
5. Proved that equality of these intersection languages forces equality of region interiors.
6. Expanded the boundary-adjacent construction inside one `D^-n(p)` fundamental gap.
7. Replaced the unnecessary unique-common-boundary claim with the stronger robust invariant actually needed: the ordered minus-to-plus handoff at `D^n(p)`.
8. Proved infinitely many distinct ordered standard follower-set pairs, hence non-soficity.
9. Restored the global no-finite-Markov-order conclusion only after the follower bridge closed.
10. Kept the mixing proof separate.
11. Added finite exact follower-region geometry, a focused no-I/O notebook claim, and a Lean endpoint bridge surface.
12. Retained the threshold, equidistribution, and gauge/FQM holds unchanged.
