# p5-b2-v2 Global Threshold Bridge

This package proves globally that the exact Fibonacci threshold count equals the affine ceiling law.

## Status

```text
GLOBAL T_A=ceil(y_A) BRIDGE: PROVED
GAUGE/FQM MAP FROM d_A=±1: NOT YET DERIVED
```

## Rebuild

From the extracted package root:

```bash
python scripts/20260711T110131_build_package.py --package-root . --verify
```

The builder regenerates outputs, traces, source and executed notebooks, figures, proof logs, and `MANIFEST.json`.

## Primary document

`docs/QBL_GLOBAL_EXACT_THRESHOLD_BRIDGE_v1.md`
