# p5-b2-v3 Results

The accepted theorem is preserved:

\[
T_A=\lceil y_Aceil\qquad(A\ge0).
\]

The proof-companion layer now distinguishes exact symbolic obligations from finite regression checks. The package builder creates and hashes the final deterministic ZIP and performs a byte-identity test using two clean copies.

```text
GLOBAL T_A=ceil(y_A) BRIDGE: PROVED
SPECIFIC-ORBIT EQUIDISTRIBUTION: NOT PROVED
GAUGE/FQM MAP FROM d_A=±1: NOT YET DERIVED
p5-b2 BRANCH STATUS: CLOSED
```
