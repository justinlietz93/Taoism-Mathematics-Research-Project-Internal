# Single Course Correction

**Purpose:** Apply one narrow correction before the next `p5-b3` task. Preserve the current pairing work. Prevent three known category errors. Do not open a new research branch inside this task.

## [CURRENT STATE]

The `p5-b3` branch remains open.

The next interaction is `p5-b3-v5`.

The current primary task is still the primary Orthad pairing:

\[
P_t \xrightarrow{B,Q,L} P_{t+1}.
\]

The source-forced two-slot pairing interface is the current lawful starting point.

The exact pairing type, value law, seed law, chart maps, transfer recurrences, and all-depth recurrence remain `NOT YET DERIVED`.

The affine `7/8/9` boundary-return system is a separate descriptive layer.

Its exact cocycle is proved.

Its full-language invariants are proved for the ambient affine system.

Their exact transfer to the canonical QBL boundary orbit remains open.

## [STRATEGIC QUESTION]

Determine whether the effective-invariant correction supplies constraints that simplify the primary-pairing problem.

Keep the primary-pairing derivation as the main task.

Treat grammar-level effective invariants as a source of constraints, not as a replacement for the pairing derivation.

Propose a simpler path only if the effective-invariant layer yields a concrete lawful reduction.

## [REASONING TASK]

1. Work from the source-forced two-slot pairing interface.

2. Determine the weakest lawful signatures of `B`, `Q`, and `L` on that interface.

3. Keep the Orthad-level and descriptive-level meanings of `L` separate.

   - Orthad-level `L` concerns retained structure, axis extension, chart restrictions, and directed transfers.
   - Descriptive-level `L` concerns the emergence of a new irreducible descriptive domain with its own effective invariant.

4. Apply the CF000 effective-invariant law.

   A newly admitted domain:

   - inherits the primitive invariant;
   - retains the prior effective-invariant stack;
   - adds a new effective invariant proper to that domain.

   Do not require the new effective invariant to reproduce the geometry or algebraic form of the prior layer.

5. Test grammar-level candidates only in their native category.

   Consider:

   - the exact boundary-return cocycle;
   - the three-symbol branch partition;
   - language complexity;
   - entropy;
   - follower-set structure;
   - non-sofic memory;
   - mixing.

   Determine which of these are intrinsic to the canonical QBL boundary layer.

6. Keep the canonical-orbit scope exact.

   Report one of these cases:

   - the canonical orbit closure equals the full affine system;
   - the canonical orbit closure is smaller but has its own intrinsic invariants;
   - only the arithmetic cocycle transfers.

7. Determine whether any proven effective invariant constrains the pairing recurrence.

   Look for:

   - a commuting quotient or factor diagram;
   - a domain-return law on pairing classes;
   - a smaller sufficient state;
   - an obstruction to finite symbolic memory;
   - a lawful meaning for the new `L` block;
   - a reduction from raw matrix entries to invariant data.

8. Preserve the retained-state scalar boundary.

   Algebraic representations may use scalar coefficients.

   A static scalar is not an independent component of the retained lifted state.

   A scalar is not the complete retained object.

   Local scalar traces such as

   \[
   a_t=\frac{i^{\#Q(W_t)}}{u_tv_t}
   \]

   and

   \[
   a_{\mathrm{completed},0}=\frac{i}{4895}
   \]

   are state-incomplete descendants.

   They may test a candidate recurrence.

   They may not replace the primary pairing, chart restrictions, directed transfers, or retained history.

9. Treat coefficient systems as representation data only.

   Determine whether a coefficient system is needed to express a candidate algebraic realization.

   Do not add it as a new retained-state coordinate.

   Do not assume the primary pairing is scalar-valued unless that is derived.

10. Keep CG000, CF03, and the October 2025 axioms as historical context.

    Use them to generate tests.

    Do not use them as current authority.

    Record any match as one of:

    - historical structural echo;
    - endogenous factor;
    - role-preserving self-description;
    - branch-relative origin-resolution candidate.

11. Determine whether the affine layer merely records QBL output or reveals finer structure about its own QBL antecedent.

    Test whether it:

    - distinguishes lower-layer histories previously equivalent;
    - reveals a hidden partition or role in QBL;
    - constrains the primary-pairing recurrence;
    - reconstructs domain information unavailable in primitive custody;
    - supports another saturation and re-articulation step.

12. Continue the primary-pairing derivation even if the effective-invariant test remains open.

    Do not make proof of CG000, A8, or higher-order descriptive `L` a prerequisite for completing the pairing boundary.

## REPORT

Report these statuses separately:

```text
SOURCE-FORCED TWO-SLOT PAIRING INTERFACE:
ORTHAD-LEVEL B/Q/L SIGNATURES:
EXACT PRIMARY PAIRING RECURRENCE:
STATIC SCALAR IN RETAINED STATE:
ALGEBRAIC REPRESENTATION WITH SCALAR COEFFICIENTS:
SCALAR-VALUED PRIMARY PAIRING:
CANONICAL QBL BOUNDARY EFFECTIVE INVARIANT:
FULL AFFINE INVARIANT TRANSFER:
HIGHER-ORDER DESCRIPTIVE L:
CG000 INTERFACE-MIRROR STATUS:
PAIRING SIMPLIFICATION FROM EFFECTIVE INVARIANTS:
```

For every `DERIVED` claim, give the exact bridge.

For every `NOT YET DERIVED` claim, name the first missing datum.

For every candidate, state what would falsify it.

## DELIVERABLE

Produce one corrected research document and one reproducible package for `p5-b3-v5`.

Keep the package centered on the primary-pairing task.

Include the effective-invariant results as a separate section.

Do not open a new branch unless a concrete mathematical dependency is proved.

## COURSE-CORRECTION SUMMARY

```text
PRIMARY TASK:
    derive or delimit the B/Q/L action on the primary pairing

SECONDARY TEST:
    determine whether emergent effective invariants constrain or simplify it

ORTHAD GEOMETRY:
    required for Orthad-level L

GRAMMAR-LEVEL EFFECTIVE INVARIANT:
    may be non-geometric

STATIC SCALAR AS RETAINED STATE:
    not licensed

SCALAR COEFFICIENTS IN REPRESENTATION:
    allowed

HISTORICAL DOCUMENTS:
    test-generating context, not current authority

BRANCH STATUS:
    p5-b3 remains open

NEXT INTERACTION:
    p5-b3-v5
```
