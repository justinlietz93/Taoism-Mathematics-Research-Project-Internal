# p5-b3-v5 Audit Results

## Verdict

```text
REVISE

CANONICAL QBL-TO-AFFINE BOUNDARY-ORBIT SEMICONJUGACY: ADOPT
CANONICAL DOUBLING-ORBIT REDUCTION: ADOPT
CANONICAL ITINERARY APERIODIC: ADOPT
CANONICAL ORBIT CLOSURE INFINITE AND TRANSITIVE: ADOPT
CANONICAL COMPLEXITY AND ENTROPY BOUNDS: ADOPT
CASE 3, ONLY ARITHMETIC IS INTRINSIC: REJECTED, ADOPT

D1 INDUCED RETURN INVARIANT: ADOPT
D1 DOMAIN-PROPER EFFECTIVE INVARIANT UNDER CF000: NOT YET DERIVED
HIGHER-ORDER DESCRIPTIVE L: NOT YET DERIVED

p5-b3 BRANCH STATUS: OPEN
NEXT INTERACTION: p5-b3-v6
```

## Integrity and reproducibility

The uploaded archive is internally coherent and deterministically rebuildable.

```text
Actual uploaded ZIP SHA-256:
6c5109dac6bde39687142a05c474db16f19d698f9d6040c5a07d92a7a0784ac2

Reported ZIP SHA-256:
6c5109dac6bde39687142a05c474db16f19d698f9d6040c5a07d92a7a0784ac
```

The reported value is malformed: it contains 63 hexadecimal characters and omits the final `2`.

```text
Document SHA-256:                  PASS
Manifest entries:                  49/49
Manifest hashes and byte counts:   PASS
Notebook cells:                    12/12 PASS
Figures:                           12
Clean rebuild:                     PASS
Release vs clean rebuild:          BYTE-IDENTICAL
```

The included verifier expects a sibling `.zip.sha256` file. That file was not part of the uploaded archive, so the verifier cannot validate the delivered response hash by itself. The audit computed the archive hash directly.

## Adopted mathematical results

### 1. The canonical orbit reduction is correct

The affine boundary orbit is conjugated to

\[
z_A=2^A\alpha\pmod 1,
\qquad
\alpha=12\frac{\log2}{\log\varphi}.
\]

The Galois-conjugation argument proves `alpha` irrational. Irrationality excludes preperiodicity but does not prove density. The remaining closure fork is correctly reduced to base-2 disjunctivity.

### 2. The canonical symbolic package is intrinsic

The exact carry itinerary, its finite-word language, and its shift-orbit closure are generated by the canonical boundary-return system itself. The package correctly proves:

```text
canonical itinerary: not eventually periodic
canonical orbit closure: infinite
canonical orbit closure: topologically transitive
n+1 <= p_can(n) <= 2^(n+1)-1
0 <= h(X_can) <= log 2
```

Therefore the weak claim that only the scalar arithmetic cocycle is intrinsic is false.

### 3. Finite coverage is correctly bounded

The accepted trace contains 10,000 carry symbols and 9,999 adjacent edges. It realizes every ambient affine word through length seven. The package correctly leaves density and language equality open.

## Load-bearing defects

### Defect 1: an induced return map is not yet a proved new CF000 domain

The package proves that D1 is a lawful induced system:

```text
objects: saturated pre-L states and return edges
map: exact first-return map
label: c_A in {7,8,9}
```

That establishes a new derived description and a genuine return invariant.

It does not by itself establish admission of a new irreducible descriptive domain. By construction, every D1 edge is the exact D0 path from one boundary state to the next, and every D1 label is computed from retained D0 data. The package does not define the boundary between:

```text
same-layer induced recoding inside D0 path dynamics
and
new descriptive-domain admission under CF000
```

CF000 requires more than the existence of a new name or presentation. A genuinely higher articulation must add a determination rather than merely rename the lower one, and a new domain must be irreducible to the exhausted current domain.

Correct status:

```text
D1 INDUCED RETURN INVARIANT: PROVED
D1 DOMAIN-PROPER EFFECTIVE INVARIANT: NOT YET DERIVED
```

### Defect 2: failure of a symbolwise map is too weak

The package uses:

```text
all closing primitive letters are L
return labels include 9, 7, and 8
```

This proves there is no one-letter map from the closing `L` to the return alphabet.

It does not prove irreducibility to D0. Variable-length block codes, Poincare return maps, and induced systems routinely create new alphabets without creating a new articulation class. The audit needs a criterion that excludes all lawful same-layer recodings, not only one-block renaming.

### Defect 3: the saturation scope changes mid-proof

At each `S_A^-`, the current QBL domain is locally saturated:

```text
B blocked
Q blocked
L forced
```

But D0 was defined as the complete all-domain custody system. That full system is not shown to saturate; it already contains `L` and continues to the next domain.

The package moves from local Domain-A saturation to a claim that the entire D0 descriptive layer has undergone a higher-order lift. The indexing must be made explicit:

```text
Domain A -> Domain A+1 under primitive L
or
D0 primitive system -> D1 induced return system under descriptive L
```

Those are not the same statement.

### Defect 4: a saturation definition is counted as a proved premise

Section 12 gives a useful falsifiable definition of D1 same-layer saturation. It does not prove that D1 is saturated.

The generated CSV nevertheless marks this row as `PASS`:

```text
same-layer saturation criterion independent
certificate: criterion stated
```

Stating a criterion is not satisfying it. Nor does the package prove that D1 saturation forces a further descriptive domain.

Correct status:

```text
D1 SATURATION CRITERION: DEFINED
D1 SAME-LAYER SATURATION: NOT PROVED
D1-TO-NEXT-DESCRIPTIVE-DOMAIN RE-ARTICULATION: NOT YET DERIVED
```

### Defect 5: the notebook structurally certifies assigned verdicts

Notebook Cell 11 reads the five `PASS` values emitted by the derivation script and prints `PASS` when all five equal that assigned value.

The derivation script itself writes:

```text
new descriptive domain lawfully admitted: PASS
new domain-proper effective invariant: PASS
same-layer saturation criterion independent: PASS
```

The code does not derive CF000 irreducibility or saturation. It serializes the document's disputed classification and then verifies that serialization.

This does not affect the exact symbolic algebra or finite-language computations. It does mean the notebook cannot certify the two structural theorem labels.

## Corrected disposition

```text
EXACT BOUNDARY-RETURN COCYCLE: PROVED
CANONICAL RETURN SYMBOLIC SYSTEM: PROVED
CANONICAL ORBIT CLOSURE: NOT YET DETERMINED
CASE 1 VERSUS CASE 2: BASE-2 DISJUNCTIVITY OPEN
CASE 3: FALSE

D1 INDUCED RETURN INVARIANT: PROVED
D1 DOMAIN-PROPER EFFECTIVE INVARIANT: NOT YET DERIVED
HIGHER-ORDER DESCRIPTIVE L: NOT YET DERIVED
ORTHAD-LEVEL HIGHER-ORDER L: NOT YET DERIVED
```
