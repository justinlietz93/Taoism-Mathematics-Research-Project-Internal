# Pairing Layer Separation

## Layer 1: local ordered two-slot restriction interface — PROVED

The primary authority names one primary object and four ordered descendants. The placements `++`, `--`, `+-`, and `-+` are source-forced local roles. Exact word dependence and pairing-first descent are source-forced.

## Layer 2: global Pair(-,-) bifunctor — OPEN

No category of argument objects, morphisms, identity/composition law, variance, or global pullback functor is defined. The four local placements do not imply global bifunctoriality.

## Layer 3: algebraic realization — OPEN

A represented duality morphism, bilinear form, conjugate-sesquilinear form, operator-valued pairing, quadratic object with polarization, or another realization may instantiate Layer 1. No realization is selected.

## Promotion errors

- Layer 1 is not a complete value law.
- A local `ι*Pι` display is not a global bifunctor theorem.
- Repeated `*` is not a typed star theorem.
- A candidate realization is not a runtime choice available to the evolving state.
