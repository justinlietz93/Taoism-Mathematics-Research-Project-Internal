[CURRENT STATE]

Use `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md` as the primary authority.

Use the State-Forced Derivation Principle as a binding constraint.

Use the exact included convergence package:

```text
inputs/convergence/p5_v8y_primary-pairing-star-phase-compatibility_20260711_162758.zip
SHA-256: 491ecfffd78ce5ab11e82794381e7579168027892dce7986fb9d4d3507395d27
```

Treat it as an alternate research route, not as higher authority.

Keep these accepted results:

```text
LOCAL ORDERED TWO-SLOT RESTRICTION ARCHITECTURE: DERIVED
ONE PRIMARY OBJECT GENERATES FOUR DESCENDANTS: DERIVED
EXACT WORD DEPENDENCE: DERIVED
B/Q FIX ARCHITECTURAL RANK: DERIVED
L RETAINS OLD ARCHITECTURE: DERIVED
L PRESERVES TWO-SLOT ARITY: DERIVED
L ADDS ONE ARCHITECTURAL AXIS: DERIVED
FIRST-DOMAIN COMPLETED LOCAL DESCENDANT i/4895: DERIVED
GLOBAL ALL-DOMAIN ACTIVE-SPINE FORMULA: NOT YET DERIVED
HERMITIAN DIAGONAL PROMOTION OF i/(uv): REJECTED
```

Keep these boundaries:

```text
GLOBAL Pair(-,-) BIFUNCTOR: NOT YET DERIVED
EXPLICIT PRIMARY PAIRING TYPE: NOT YET DERIVED
EXACT PRIMARY PAIRING SEED: NOT YET DERIVED
STAR SEMANTICS: NOT YET DERIVED
Q ACTION TYPE: NOT YET DERIVED
ORDER BETWEEN STAR AND Q GAPS: NOT YET DERIVED
EXACT B/Q/L VALUE RECURRENCE: NOT YET DERIVED
EXACT CHART MAPS: NOT YET DERIVED
EXACT DIRECTED TRANSFERS: NOT YET DERIVED
```

Do not preselect `STAR_SEMANTICS_AXIOM`.

Candidate actions are possible representations of one intrinsic retained-state update. They are not choices made by the evolving system.

[STRATEGIC QUESTION]

Determine whether the complete retained state forces one intrinsic B/Q/L action on the primary pairing.

Test these contenders head to head:

```text
A. source-forced local ordered two-slot architecture
B. genuine star-semantics independence
C. state-forced derivation of one intrinsic action
```

For each rejected contender, give the exact contradiction certificate.

For each surviving contender, determine whether it is:

```text
an incomplete description
a coordinate-equivalent representation
a genuinely inequivalent complete intrinsic law
```

[REASONING TASK]

1. Separate the three pairing layers.

   Distinguish:

   ```text
   local ordered two-slot restriction interface
   globally defined Pair(-,-) bifunctor
   algebraic realization of the intrinsic pairing law
   ```

   State which source clauses support each layer.

   Report: `PAIRING_LAYER_SEPARATION.md`.

2. Define an autonomous complete intrinsic transition.

   Work with

   \[
   \widehat X_t=
   (X_t,P_t,\Omega_t^+,\Omega_t^-,T_t^{+\to-},T_t^{-\to+}).
   \]

   Define the intrinsic custody selector

   \[
   p_t=\Sigma_{\mathrm{cust}}(X_t).
   \]

   Define \(\Sigma_{\mathrm{cust}}\) by the strict custody priority:

   ```text
   B when B is admissible.
   Q when B is blocked and another lawful phase position remains.
   L when B is blocked and no lawful phase position remains.
   ```

   Write the coupled autonomous evolution law as

   \[
   \widehat X_{t+1}=
   \mathcal U_{\Sigma_{\mathrm{cust}}(X_t)}(\widehat X_t).
   \]

   Keep \(\mathcal U_B\), \(\mathcal U_Q\), and \(\mathcal U_L\) only as case maps of this autonomous law.

   A complete model witness may not receive `B`, `Q`, or `L` as an external runtime input.

   When \(\Sigma(\widehat X_t)\) is used as shorthand, require it to factor through the custody projection:

   \[
   \Sigma(\widehat X_t)=
   \Sigma_{\mathrm{cust}}\!\left(\pi_X(\widehat X_t)\right).
   \]

   The pairing, both chart restrictions, and both directed transfers may mutate after the primitive is selected. They may not select, permit, block, or veto the primitive.

   Treat \(\Sigma\) only as notation for the custody predicates. Do not add a retained selector object to the state.

   Require custody evolution, exact-word retention, pairing mutation, latched-sector retention, and no projection.

   Require all four chart and transfer descendants to be induced from the updated primary pairing.

   Let

   \[
   D_{++,t}=\Omega_t^+,
   \qquad
   D_{--,t}=\Omega_t^-,
   \qquad
   D_{+-,t}=T_t^{+\to-},
   \qquad
   D_{-+,t}=T_t^{-\to+}.
   \]

   Introduce abstract restriction maps \(R_{ab,t}\) where needed and require

   \[
   D_{ab,t+1}=R_{ab,t+1}(P_{t+1})
   \]

   for all four plus/minus placements.

   The complete retained state must fix each restriction map. If it does not, report the exact missing datum that would fix it.

   Do not permit independent recurrences for the primary pairing, the two chart restrictions, and the two directed transfers.

   Report: `COMPLETE_INTRINSIC_TRANSITION_INTERFACE.md`.

3. Define action equivalence.

   Determine when two algebraic presentations represent the same intrinsic update.

   Require the comparison to commute with custody, exact word, B/Q/L transitions, chart restrictions, and directed transfers.

   Use the weakest representation-isomorphism criterion available. Name any missing gauge datum.

   Report: `INTRINSIC_ACTION_EQUIVALENCE.md`.

4. Audit the repeated-star evidence before typing it.

   Locate every exact source passage containing the relevant `*` notation.

   Separate:

   ```text
   ratified source expression
   intended algebraic shape
   task-level candidate
   historical construction
   ```

   Determine whether one typed star operation is actually required across those passages.

   Test whether the star law is:

   ```text
   forced directly
   induced by the pairing realization
   constrained jointly with Q
   genuinely independent
   only schematic notation
   ```

   Do not use `C_t*` or historical `H=M+iJ` as current authority.

   Report: `STAR_Q_DEPENDENCY.md`.

5. Scope the local descendant before using it.

   On the exact Domain-0 path before the first `L`, derive

   \[
   a_t=\frac{i^{\#Q(W_t)}}{u_tv_t}.
   \]

   Prove its `B` and `Q` updates on that path.

   At the first `L`, classify `i/4895` as the completed retained old-axis descendant.

   Keep the newly opened active axis distinct. It begins as the new unmutated axis required by the authority.

   Determine whether the retained state forces an axis-indexed recurrence for later domains.

   Contender A is an adopted interface. Test whether it is sufficient to force the intrinsic law; do not retest whether the local interface exists.

   Do not apply the total global `Q` count to the newborn active axis without deriving that identification.

   State exactly which pairing component each scalar descendant constrains.

   Report: `LOCAL_DESCENDANT_SCOPE.md`.

6. Resolve B using complete actions.

   Test pullback, value mutation, regeneration, and any other lawful presentation only as complete coupled updates.

   Reject metadata-only descriptions.

   Determine whether surviving presentations are equivalent under Step 3.

   Report the intrinsic B law or the first missing retained datum.

7. Resolve Q and star together.

   Build complete deterministic candidates only where lawful.

   Test them against:

   ```text
   exact +i Domain-0 active descendant
   any later-axis +i law requires a derived axis-indexed recurrence
   retained arithmetic anchor
   both orientation hands
   exact word history
   four-turn visible repetition without lifted-state identity
   chart and transfer co-evolution
   no projection
   ```

   Determine whether Q forces the star law, star forces Q, both arise from a prior pairing type, or a genuine independence fork remains.

8. Resolve L while preserving two-slot arity.

   Preserve two-slot pairing arity.

   Model `L` as an extension of each argument object's ambient architecture into old and new sectors.

   Do not treat the new architectural axis as a third pairing argument.

   Use the component schema:

   ```text
   old-old
   old-new
   new-old
   new-new
   ```

   Require:

   ```text
   old-old = retained embedded prior pairing
   architectural axis count +1
   exact history retained
   charts and transfers extended
   no projection
   ```

   Every old-new, new-old, and new-new component must be a deterministic function of retained state.

   Test zero and nonzero laws as complete coupled transitions. A standalone matrix is not a model.

9. Test contender A.

   Determine whether the local ordered two-slot architecture is:

   ```text
   sufficient to force the intrinsic law
   an incomplete source interface
   coordinate-equivalent to a broader Pair(-,-) realization
   contradicted as a complete law
   ```

   Report the exact certificate.

10. Test contender B.

    Determine whether genuine star-semantics independence survives the complete retained-state constraints.

    Independence requires two complete lawful recurrences with inequivalent star laws and identical accepted descendants.

    Otherwise reject the independence claim or leave it open with the exact missing datum.

11. Test contender C.

    Determine whether one intrinsic action survives after the complete state, exact word, Q cycle, four descendants, and first-L retention are enforced.

    When one law survives, state every forcing constraint.

12. Produce a valid independence certificate only when needed.

    If several intrinsic laws survive, construct at least two complete all-prefix recurrences.

    Prove every Phase-Calculus obligation and reproduce all accepted descendants.

    Prove inequivalence under Step 3.

    Otherwise do not claim type underdetermination or independence.

13. Retest the boundary-return cocycle.

    Determine whether it induces a lawful return on pairing classes after the intrinsic-action analysis.

    Do not use arbitrary class sequences.

    Report the commuting law or first missing bridge.

14. Replace notebook labels with executable models.

    Every `COUNTERMODEL` or `MODEL WITNESS` must instantiate a complete coupled-state recurrence and check its obligations.

    Candidate lists, toy matrices, and editorial compatibility scores do not qualify.

15. Strengthen the formal companion.

    Encode the complete transition interface, action equivalence, complete model witnesses, and inequivalence theorem if claimed.

    Compile when Lean is available.

    Otherwise report:

    ```text
    LEAN SOURCE PRESENT; PROOF AND COMPILATION NOT VERIFIED
    ```

16. Preserve the descriptive boundary.

    Keep:

    ```text
    D1 DOMAIN-PROPER EFFECTIVE INVARIANT: NOT YET DERIVED
    HIGHER-ORDER DESCRIPTIVE L: NOT YET DERIVED
    ```

    Do not reopen the corrective descriptive pass.

17. Package the work.

    Write:

    ```text
    QBL_PRIMARY_PAIRING_STATE_FORCED_ACTION_v1.md
    ```

    Build:

    ```text
    p5-b3-v8_state-forced-pairing-action_<YYYYMMDDTHHMMSS>.zip
    ```

    Include the complete experiment-package structure, this audit package, the included parallel `p5_v8y` package, and a deterministic rebuild.

    Report every conclusion as `PROVED`, `CERTIFIED FINITELY`, `COUNTERMODEL`, `MODEL WITNESS`, `OBSERVED`, or `OPEN`.

    Use `COUNTERMODEL` and `MODEL WITNESS` only for complete instantiated models.
