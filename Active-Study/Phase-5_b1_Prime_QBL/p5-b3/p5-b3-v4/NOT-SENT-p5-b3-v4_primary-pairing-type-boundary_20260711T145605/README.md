# p5-b3-v4 Primary Pairing Type Boundary

This package determines the boundary between architecture forced by the current QBL/Orthad law and algebra that requires a chosen realization.

## Rebuild

From the package root:

```bash
python scripts/20260711T145605_build_package.py --package-root . --archive ../p5-b3-v4_primary-pairing-type-boundary_20260711T145605.zip
```

The command regenerates derivation outputs, traces, the executed notebook, figures, manifest, deterministic ZIP, and ZIP SHA-256.

## Main result

No existing controlling authority fixes the complete primary-pairing realization or two-sided orthogonality. The next ratification target is the full `PRIMARY_PAIRING_REALIZATION_AXIOM` defined in the research document.
