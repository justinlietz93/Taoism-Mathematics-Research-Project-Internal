# Assumption Lock

## Primary authority

`QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md` controls primitive custody and Orthad architecture.

## Accepted results

- Carry indexing correction.
- Local active-axis laws `B: a -> a*u/(u+v)` and `Q: a -> i*a`.
- First completed local witness `i/4895`.
- Exact boundary-orbit semiconjugacy and global threshold theorem from prior accepted branches.

## Contextual sources

- The supplied diagram gives the dependency order and visual architecture. The written law controls notation and status.
- CF00 and CF13 may constrain downstream geometry and the quarter-turn carrier, but may not be promoted into a primary-pairing axiom without an explicit bridge.
- Older diagonal Orthad documents are provenance only.

## Hard exclusions

This pass does not choose a bilinear, sesquilinear, quadratic, Hermitian, operator-valued, or diagonal realization for convenience. It does not infer two-sided orthogonality from the word `orthogonal`. It does not treat a local active-axis scalar as the complete primary pairing.

## Status discipline

- Architecture can be forced while algebraic realization remains open.
- A candidate model is labeled `MODEL WITNESS`.
- A theorem requiring declared hypotheses is labeled `CONDITIONAL CHECK`.
- Only algebra derived from stated inputs is labeled `PASS`.
