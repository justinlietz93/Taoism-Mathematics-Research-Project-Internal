# p5-b3-v7 Primary Pairing B/Q/L Action Boundary

This package derives the weakest exact B/Q/L signatures forced on the source-level two-slot primary-pairing interface.

## Rebuild

From the package root:

```bash
python scripts/20260711T194326_build_package.py --package-root . --archive ../p5-b3-v7_primary-pairing-bql-action_20260711T194326.zip
```

The command regenerates exact outputs, traces, the executed notebook, figures, manifest, deterministic ZIP, and ZIP SHA-256.

## Main boundary

The old-old pairing structure is retained across `L`, but no mixed component is forced to vanish until pairing-orthogonality is typed. `B` and `Q` mutate active data at fixed architectural rank, but no slot or value action is fixed.
