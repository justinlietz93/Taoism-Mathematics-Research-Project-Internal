# QBL Descriptive Articulation Boundary

**Step:** `p5-b3-v6`  
**Primary structural authority:** `CF000_Primitive_Distinguishability`  
**Primary custody authority:** `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md`  
**Scope:** decide whether the canonical pre-`L` return construction is a new CF000 descriptive domain or a same-layer induced presentation of primitive QBL path dynamics  
**Excluded lane:** primary-pairing, chart-map, transfer, gauge, FQM, and Weil recurrences

---

## 1. Disposition

### PROVED

```text
CANONICAL QBL-TO-AFFINE BOUNDARY-ORBIT SEMICONJUGACY: PROVED
EXACT BOUNDARY-RETURN COCYCLE: PROVED
CANONICAL CARRY ITINERARY APERIODIC: PROVED
CANONICAL SYMBOLIC ORBIT CLOSURE INFINITE: PROVED
CANONICAL SYMBOLIC ORBIT CLOSURE TRANSITIVE: PROVED

D1 INDUCED RETURN INVARIANT: PROVED
D1 IS A SAME-LAYER INDUCED RECODING OF D0: PROVED
D1 IS A LAWFUL INDUCED DESCRIPTION, NOT A FORCED RE-ARTICULATION: PROVED
HIGHER-ORDER DESCRIPTIVE L: FALSE FOR D0-TO-D1
```

The load-bearing conclusion is not that the carry grammar is trivial. It is an exact, intrinsic path invariant of QBL custody. The correction is that an intrinsic induced invariant does not automatically constitute a new irreducible CF000 descriptive domain.

The canonical enriched return system is isomorphic to the first-return category of the D0 custody system on the pre-`L` boundary section. Its states are existing D0 states, its edges are complete lawful D0 paths, and its carry labels are exact D0 path observables. It therefore adds no determination outside the D0 path articulation class.

### CERTIFIED FINITELY

- exact primitive custody simulation through Domain 8;
- exact recovery of the first nine canonical boundary states and return paths;
- finite agreement of derived carries with the accepted trace;
- accepted `A=0..10000` trace validation;
- full ambient affine word coverage through length seven;
- direct verification of the corrected prior archive SHA-256.

### OBSERVED

- the finite canonical itinerary continues to display broad ambient-language coverage;
- finite state and edge frequencies remain close to the previously recorded Lebesgue benchmark.

Neither observation is used in the descriptive-equivalence theorem.

### OPEN

```text
CANONICAL ORBIT CLOSURE = FULL AFFINE SYSTEM: NOT YET PROVED
CANONICAL ORBIT CLOSURE IS PROPER: NOT YET PROVED
SPECIFIC-ORBIT EQUIDISTRIBUTION: NOT PROVED
D1 SAME-LAYER SATURATION: NOT YET DERIVED
D1 NEXT RE-ARTICULATION: NOT YET DERIVED
EXACT PRIMARY PAIRING RECURRENCE: NOT YET DERIVED
ORTHAD-LEVEL HIGHER-ORDER L: NOT YET DERIVED
```

---

## 2. Release-integrity correction

The exact SHA-256 of the previously uploaded `p5-b3-v5` archive is

```text
6c5109dac6bde39687142a05c474db16f19d698f9d6040c5a07d92a7a0784ac2
```

It contains 64 hexadecimal characters. The prior response omitted the final `2`. The current package includes the exact prior archive and a corrected `.sha256` input. Its verifier hashes that archive directly and requires equality with the corrected value.

---

## 3. Authorities and decision rule

### 3.1 CF000 criterion

CF000 distinguishes a genuinely higher articulation from a renaming or reparameterization. A genuinely higher determination adds an internal determination for which the lower degree is no longer sufficient as sole determination. A new domain is irreducible to the exhausted current domain. Same-axis saturation means that no genuinely new lawful invariant-bearing articulation remains available within the current domain.

CF000 does not require a metric, geometric axis, matrix rank, or Orthad realization for descriptive novelty. It does require irreducibility to the prior articulation class.

### 3.2 Custody criterion

The current QBL authority defines

\[
X_t=(A_t,q_t,\theta_t,k_t,j_t,W_t)
\]

and a deterministic one-step law with strict priority

\[
B>Q>L.
\]

The exact ordered word prefix `W_t` is native retained state. `L` is already one of the native transitions of the complete all-domain custody system. A Domain-A boundary is defined internally by `B` blocked and `Q` blocked.

### 3.3 Stronger formulation of the A/B/C fork

The three proposed cases conflate two different constructions:

1. the **canonical enriched return system**, whose states and edges are actual QBL boundary states and QBL paths;
2. the **symbolic orbit closure**, which adds limit sequences of the carry code and need not consist of realized QBL boundary states.

For the enriched return system, Case A is proved. For the added closure points, QBL-domain admission is not licensed. They remain a mathematical completion of the code unless an enlarged lawful QBL family is constructed.

---

## 4. D0 articulation class

Let

\[
D_0=(\mathcal X_0,\operatorname{Step})
\]

be the complete reachable QBL custody system. Its states are all states reachable from the primitive seed by the current `B>Q>L` law. Its elementary transitions are the primitive `B`, `Q`, and `L` steps.

Define `Path(D0)` to have:

- objects: reachable custody states;
- morphisms: finite lawful QBL paths;
- composition: path concatenation.

Because `W` is retained, a state reached later on the same orbit contains the exact prefix needed to identify the path suffix from an earlier prefix.

### 4.1 Native and derived D0 constructions

| Construction | Classification | Certificate |
|---|---|---|
| one-step primitive transitions | **NATIVE** | `Step` emits exactly one of `B,Q,L` |
| exact word prefixes | **NATIVE** | `W_t` is a retained custody coordinate |
| finite QBL paths | **DERIVED INSIDE D0** | finite composition of native steps |
| stopping times | **DERIVED INSIDE D0** | first hitting index of a state predicate along the deterministic path |
| pre-`L` boundary section | **DERIVED INSIDE D0** | predicate `B blocked` and `Q blocked` |
| first-return maps | **DERIVED INSIDE D0** | iterate `Step` from one section hit to the next |
| path observables | **DERIVED INSIDE D0** | functions of retained states, word suffixes, or finite paths |
| induced symbolic codes | **DERIVED INSIDE D0** | labels applied to section states or return paths |
| topological closure points not realized by a QBL path | **NOT YET LICENSED** as QBL states | no enlarged lawful custody family is derived |

Thus D0 is not restricted to isolated one-step names. Its retained exact word makes finite paths and path-derived constructions lawful internal objects.

---

## 5. Descriptive articulation equivalence

The companion document `DESCRIPTIVE_ARTICULATION_EQUIVALENCE.md` gives the full criterion. The essential statement is:

A presentation remains in the same articulation class when every new state is an existing old state or path-derived tuple, every new edge is a finite lawful old path, every new relation is an old path observable, and no two elements with identical complete old interpretation acquire different new determinations.

This distinguishes the following cases.

| Construction | Articulation status |
|---|---|
| renaming | same class |
| invertible fixed-length block code | same class |
| lossy block code | factor/quotient, not a new domain by loss alone |
| variable-length first-return code | induced same-layer presentation unless an irreducible new determination is proved |
| induced subsystem | same-layer subsystem |
| orbit closure | descriptive completion; not a QBL state domain without a lawful state construction |
| CF000 new domain | must add a determination unavailable in the complete prior state/path/relation class |

Computability from D0 alone is not the only test. The decisive test is whether the new structure splits a fiber of the **complete** D0 description. D1 does not.

---

## 6. Exact D1 construction

### 6.1 Boundary section

For each `A>=0`, let

\[
S_A^-=(A,q_A,\theta_A,N_A-1,j_A,W_A^-)
\]

be the canonical state immediately before the `L` closing Domain A. Define

\[
\Sigma^-:=\{S_A^-:A\ge0\}.
\]

Each state is an actual D0 custody state.

### 6.2 First-return edge

Let `tau(x)` be the least positive number of primitive steps required to return from `x in Sigma^-` to `Sigma^-`. Determinism and the current domain law give a unique next boundary hit. Define

\[
\mathcal R(x)=\operatorname{Step}^{\tau(x)}(x).
\]

For the canonical points,

\[
\mathcal R(S_A^-)=S_{A+1}^-.
\]

Let

\[
r_A:S_A^-\longrightarrow S_{A+1}^-
\]

be the complete primitive path, beginning with the closing `L` and ending at the next pre-`L` boundary.

### 6.3 Enriched return category

Let `Ret(D0,Sigma-)` be the category generated by:

- objects `S_A^-`;
- elementary return arrows `r_A`;
- finite compositions `r_{B-1}...r_A`.

This is the enriched D1 return system before symbolic projection.

---

## 7. Exact D1-to-D0 interpretation

Define

\[
J:\operatorname{Ret}(D_0,\Sigma^-)\longrightarrow \operatorname{Path}(D_0)
\]

by

\[
J(S_A^-)=S_A^-,
\qquad
J(r_A)=\text{the complete primitive path from }S_A^-\text{ to }S_{A+1}^-.
\]

### Theorem 7.1: state faithfulness

`J` is injective on objects.

**Proof.** Each object is the actual retained custody state `S_A^-`. Distinct domain indices give distinct states, and `J` is the identity on those states. □

### Theorem 7.2: edge faithfulness

`J` is faithful.

**Proof.** A return path is the exact word suffix between two retained prefixes. If two return arrows had the same complete D0 path, they would have the same source, target, and word suffix, hence would be the same arrow. Composition is preserved by concatenation. □

### Theorem 7.3: fullness onto the boundary-return subcategory

`J` is full onto the forward D0 path subcategory whose objects are `Sigma-` and whose arrows are complete paths from one boundary hit to a later boundary hit.

**Proof.** Any D0 path from `S_A^-` to `S_B^-` with `B>A` crosses the unique successive boundary hits

\[
S_A^-,S_{A+1}^-,\ldots,S_B^-.
\]

Determinism forces the path to equal

\[
J(r_{B-1}\circ\cdots\circ r_A).
\]

Therefore every arrow of the stated subcategory lies in the image. □

### Theorem 7.4: invertible enriched presentation

The enriched D1 return system is isomorphic to the induced boundary-return subcategory of D0.

**Proof.** The inverse presentation keeps the boundary objects and segments any boundary-to-boundary D0 path at its internally defined pre-`L` section hits. The boundary predicate and exact word determine that segmentation uniquely. □

This is stronger than saying that D1 is computable from D0. It is a lossless interpretation of the enriched return dynamics as D0 paths.

---

## 8. Carry projection and information loss

At a boundary state, let

\[
\nu(q_A)=T_A
\]

be the cumulative primitive `B` count, recovered from the exact word or from the Fibonacci pair index. For `A>=1`, define

\[
c_A=\nu(q_A)-2\nu(q_{A-1})\in\{7,8,9\}.
\]

This gives a label map on return edges:

\[
\ell(r_{A-1})=c_A.
\]

The label is a D0 path observable because it is a function of the two D0 boundary endpoints and their retained histories.

The map

\[
\ell:\operatorname{Ret}(D_0,\Sigma^-)_1\to\{7,8,9\}
\]

is generally information-losing: many distinct return edges may share one carry. The carry itinerary is therefore a symbolic factor of the enriched return system, not a more state-complete object than D0.

The accepted arithmetic factor remains exact:

\[
\pi(S_A^-)=\lambda j_A+\beta-\nu(q_A)=E_A
\]

and

\[
E_A=2E_{A-1}+\gamma-c_A.
\]

This proves an exact boundary-return cocycle while preserving its same-layer status.

---

## 9. Irreducibility test

CF000 genuineness requires a new internal determination that is not available in the lower articulation class.

For D1, every proposed determination is covered as follows:

| D1 item | D0 representation |
|---|---|
| boundary state `S_A^-` | actual D0 state satisfying the boundary predicate |
| return edge `r_A` | exact finite D0 path |
| return time `tau(S_A^-)` | first-hitting path length in D0 |
| cumulative B count | count of `B` in retained D0 word or pair index |
| carry `c_A` | function of two retained D0 boundary counts |
| affine error `E_A` | function of retained `j_A` and `nu(q_A)` |
| canonical carry itinerary | label sequence of D0 return edges |
| finite carry words | finite blocks of the D0 path-observable sequence |

There is no pair of enriched D1 elements with identical complete D0 state/path interpretation and different D1 determination. The CF000 fiber-splitting certificate is absent because the interpretation is faithful.

### Theorem 9.1

```text
D1 IS A SAME-LAYER INDUCED RECODING OF D0: PROVED
```

**Proof.** The enriched D1 system is isomorphic to a D0 induced path subcategory by Theorem 7.4. Every symbolic D1 observable factors through that interpretation by Section 8. Therefore D1 adds no relation or determination irreducible to the complete D0 path articulation class. Under the descriptive-equivalence criterion, it is a lawful same-layer induced recoding. □

### Corollary 9.2

```text
D1 DOMAIN-PROPER EFFECTIVE INVARIANT: FALSE FOR THE INDUCED ORBIT PRESENTATION
```

The exact return cocycle is an intrinsic induced invariant, but not a domain-proper invariant of a newly admitted CF000 domain.

---

## 10. Negative controls

### 10.1 Variable-length return code that remains same-layer

Consider

\[
n\mapsto n+1
\]

on `N`, with section

\[
s_m=\frac{m(m+1)}2.
\]

The first-return path from `s_m` to `s_{m+1}` has length `m+1`. Label it by parity, producing a two-letter return alphabet. The section points are old states, return edges are old successor paths, and labels are path-length observables. The exact same interpretation theorem applies. The new alphabet is a same-layer induced recoding.

This control rejects the invalid rule “variable return length plus a new alphabet implies a new domain.”

### 10.2 Genuine new relation by fiber splitting

Let the old description be

\[
D(x,b)=x
\]

on `N x {0,1}` and let the proposed new coordinate be

\[
\xi(x,b)=b.
\]

Then

\[
D(x,0)=D(x,1),
\qquad
\xi(x,0)\ne\xi(x,1).
\]

No function of `x` recovers `b`. The new coordinate splits an old-description fiber and is a genuine additional determination relative to the declared old class.

D1 behaves like Control A, not Control B, because complete D0 path interpretation already distinguishes every enriched D1 element.

---

## 11. Saturation indexing

Three saturation questions must remain separate.

### 11.1 Local Domain-A saturation

At `S_A^-`:

\[
B\text{ is blocked},
\qquad
Q\text{ is blocked}.
\]

Therefore the current local Domain A is saturated and primitive `L` is forced.

```text
LOCAL DOMAIN-A SATURATION AT S_A^-: PROVED
```

This certificate concerns the current domain-local `B/Q` capacity.

### 11.2 Saturation of complete D0

D0 includes `L` and all subsequent domain evolution as native same-system transitions. From every `S_A^-`, the law supplies a next state `S_A^+` and then a lawful path to `S_{A+1}^-`. Hence the complete all-domain custody layer has not exhausted its lawful articulation.

```text
COMPLETE D0 ALL-DOMAIN SATURATION: FALSE ON THE CANONICAL INFINITE CUSTODY ORBIT
```

Local saturation triggers a native D0 transition. It does not force departure from the D0 articulation class.

### 11.3 D1 saturation

Retain the prior falsifiable definition. For a proposed D1 description `D:Y->Z`, same-layer saturation requires:

1. equality under `D` implies equality of follower languages and all future cocycle values;
2. every lawful return observable factors through `D`;
3. no strictly finer lawful same-D1 equivalence remains that separates future behavior.

```text
D1 SATURATION CRITERION: DEFINED
D1 SAME-LAYER SATURATION: NOT YET DERIVED
D1 NEXT RE-ARTICULATION: NOT YET DERIVED
```

No current proof identifies a nontrivial descriptor satisfying all three conditions, and no contradiction forces departure from D1.

---

## 12. Admission mechanism

D1 is constructed by observing the repeated section hits of an already continuing D0 orbit. No contradiction prevents continued articulation inside D0; continued articulation is exactly what produces the next return.

```text
D1 IS A LAWFUL INDUCED DESCRIPTION, NOT A FORCED RE-ARTICULATION: PROVED
```

The admission mechanism is induction on a section, not CF000 orthogonal/domain re-articulation. The boundary-return construction is useful because it exposes a renormalized law, not because D0 lacks a lawful continuation.

---

## 13. Higher-order descriptive L

The phrase must be indexed by the transition it claims.

| Claimed transition | Status | Reason |
|---|---|---|
| Domain A -> Domain A+1 | primitive `L` is proved | local saturation forces the native custody extension |
| D0 complete custody -> D1 return presentation | **not a descriptive L** | D1 is an induced same-layer path presentation and D0 is not saturated |
| D1 -> a later descriptive domain | open | D1 saturation and a forced irreducible next determination are not proved |

Therefore:

```text
HIGHER-ORDER DESCRIPTIVE L: FALSE FOR D0-TO-D1
```

This does not refute the general possibility of a higher-order descriptive `L`. It rejects this particular D0-to-D1 identification under the current authorities.

---

## 14. Canonical orbit results retained at exact strength

Let `c=(c_1,c_2,...)` be the canonical carry itinerary and

\[
X_{\mathrm{can}}=\overline{\{\sigma^n c:n\ge0\}}.
\]

The accepted results remain:

```text
canonical itinerary aperiodic: PROVED
X_can infinite: PROVED
X_can topologically transitive: PROVED
n+1 <= p_can(n) <= 2^(n+1)-1
0 <= h(X_can) <= log 2
```

The finite accepted trace realizes every ambient affine word through length seven.

The following remain open:

```text
X_can = full affine coding
X_can proper
exact h(X_can)
non-soficity of X_can
mixing of X_can
finite Markov order of X_can
specific-orbit equidistribution
```

Density is equivalent to base-2 disjunctivity of

\[
\alpha=12\frac{\log2}{\log\varphi}.
\]

None of these closure questions is needed for the same-layer recoding theorem, which is decided at the enriched state/path level.

### 14.1 Closure points are not automatically QBL states

`X_can` may contain limit sequences not equal to a shift of the canonical itinerary. The current custody law does not construct a corresponding boundary state for each such sequence. Therefore the orbit closure is an intrinsic symbolic completion of the canonical code, but it is not presently an enlarged QBL state domain.

---

## 15. Invariant stacking after correction

The accepted stack is

```text
primitive invariant and prior effective invariants
    retained through D0 custody
        -> exact D0 finite paths and boundary states
            -> induced return cocycle and canonical symbolic factor
```

The last arrow is derivation inside the D0 path articulation class. It adds an invariant of the induced presentation, not a newly admitted CF000 domain-proper invariant.

This distinction preserves both facts:

1. Case 3, “only scalar arithmetic is intrinsic,” is false. The canonical symbolic return system is intrinsic.
2. Intrinsic symbolic structure alone does not prove a new irreducible descriptive domain.

---

## 16. Orthad lane

The descriptive correction does not use or settle:

- the exact primary-pairing type or recurrence;
- exact chart restriction maps;
- exact bidirectional transfer recurrences;
- Orthad rank extension at all depths;
- gauge, holonomy, FQM, or Weil data.

```text
ORTHAD-LEVEL HIGHER-ORDER L: NOT YET DERIVED
EXACT PRIMARY PAIRING RECURRENCE: NOT YET DERIVED
```

The written custody/Orthad law remains primary. Its open pairing-first dependency is unaffected by the same-layer return result.

---

## 17. Evidence classification

### PROVED

- D0 includes finite paths, boundary sections, stopping times, first-return maps, and path observables as native or derived same-system constructions.
- enriched D1 is isomorphic to the induced D0 boundary-return subcategory.
- the carry code is a D0 path-observable factor.
- D1 is a same-layer induced recoding of D0.
- D1 is lawful but not saturation-forced.
- higher-order descriptive `L` is false for D0-to-D1.
- the previously accepted canonical semiconjugacy, cocycle, aperiodicity, infinitude, transitivity, and complexity/entropy bounds remain valid.

### CERTIFIED FINITELY

- custody simulation and boundary segmentation through Domain 8;
- carry agreement with the accepted trace over the simulated range;
- accepted trace integrity through `A=10000`;
- full ambient word coverage through length seven;
- negative-control calculations;
- corrected prior release hash.

### OBSERVED

- broad finite language coverage;
- finite frequency agreement with ambient metric benchmarks.

### OPEN

- canonical orbit closure equality or properness;
- disjunctivity and equidistribution;
- D1 same-layer saturation;
- a forced D1-to-later descriptive re-articulation;
- all open Orthad recurrence obligations.

---

## 18. Final status

```text
D1 INDUCED RETURN INVARIANT: PROVED
D1 IS A SAME-LAYER INDUCED RECODING OF D0: PROVED
D1 DOMAIN-PROPER EFFECTIVE INVARIANT: FALSE FOR THE INDUCED ORBIT PRESENTATION
D1 IS A LAWFUL INDUCED DESCRIPTION, NOT A FORCED RE-ARTICULATION: PROVED

LOCAL DOMAIN-A SATURATION AT S_A^-: PROVED
COMPLETE D0 ALL-DOMAIN SATURATION: FALSE ON THE CANONICAL INFINITE CUSTODY ORBIT
D1 SATURATION CRITERION: DEFINED
D1 SAME-LAYER SATURATION: NOT YET DERIVED
D1 NEXT RE-ARTICULATION: NOT YET DERIVED

HIGHER-ORDER DESCRIPTIVE L: FALSE FOR D0-TO-D1
ORTHAD-LEVEL HIGHER-ORDER L: NOT YET DERIVED
EXACT PRIMARY PAIRING RECURRENCE: NOT YET DERIVED

p5-b3 BRANCH STATUS: OPEN
```
