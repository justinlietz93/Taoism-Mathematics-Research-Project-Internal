# Assumption Lock — p5-b3-v8 Unified State-Forced Task

## Binding authority

1. `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md` is the primary custody and Orthad authority.
2. The State-Forced Derivation Principle is binding.
3. The included `p5_v8y` package is a convergence input, not an authority override.

## Bound convergence input

```text
path:
inputs/convergence/p5_v8y_primary-pairing-star-phase-compatibility_20260711_162758.zip

sha256:
491ecfffd78ce5ab11e82794381e7579168027892dce7986fb9d4d3507395d27

internal_document_sha256:
bbba649dcbeff1593afb6d81ca6753253533e6dbe4e2bd8057a7eab1caa5bd46

internal_manifest:
61/61 verified
```

## Retained-state law

Every lawful candidate action must:

- encode custody-only primitive selection by \(p_t=\Sigma_{\mathrm{cust}}(X_t)\);
- apply the strict custody priority `B > Q > L`: `B` when admissible, `Q` when `B` is blocked and a lawful phase position remains, and `L` when `B` is blocked and none remains;
- evolve autonomously by \(\widehat X_{t+1}=\mathcal U_{\Sigma_{\mathrm{cust}}(X_t)}(\widehat X_t)\);
- require any shorthand \(\Sigma(\widehat X_t)\) to factor as \(\Sigma_{\mathrm{cust}}(\pi_X(\widehat X_t))\);
- use `B`, `Q`, and `L` only as case maps, never as external runtime inputs;
- allow the pairing, charts, and transfers to mutate only after custody selects the primitive; they may not select, permit, block, or veto it;
- treat \(\Sigma\) as notation for custody predicates, never as a new retained selector object;
- be determined by the complete retained state and exact word;
- mutate the primary pairing and induce all four descendants from the updated primary pairing;
- satisfy \(D_{ab,t+1}=R_{ab,t+1}(P_{t+1})\) for all four plus/minus placements;
- fix each restriction map from retained state or report it as part of the exact missing datum;
- preserve latched structure;
- perform no projection during evolution;
- use no external scheduler, convention, or runtime choice;
- reproduce every accepted primitive and local descendant;
- preserve two-slot pairing arity under architectural extension;
- keep the completed old-axis descendant distinct from the newborn active axis;
- treat `i/4895` as a first-domain completed local descendant only, unless an all-domain axis-indexed recurrence is derived.

## Forbidden imports

The task may not:

- select a candidate because it is familiar, simple, or elegant;
- promote repeated `*` notation to `STAR_SEMANTICS_AXIOM` before eliminating state-forced derivation;
- assume a global `Pair(-,-)` bifunctor from four local descendant obligations;
- conflate a local two-slot restriction interface with a global bifunctor;
- conflate an abstract architecture with one algebraic realization;
- assume representability, scalar variance, Hermitianity, self-adjointness, or a coefficient involution;
- use historical `H=M+iJ` as current type evidence;
- use `C_t*` as ratified authority when it arose only in a candidate presentation;
- treat a dictionary, candidate list, toy matrix, or editorial compatibility score as a complete model;
- claim independence from arbitrary pairing-class sequences;
- introduce free `L` components not fixed by retained state;
- globalize the Domain-0 scalar descendant to later active axes without an axis-indexed recurrence;
- apply the Domain-0 `+i` law to any later axis without a derived axis-indexed recurrence;
- accept a model that receives `B`, `Q`, or `L` as an external runtime input;
- allow \(P_t\), either chart restriction, or either directed transfer to select, permit, block, or veto `B`, `Q`, or `L`;
- add \(\Sigma\) as a retained state component or independent selector object;
- use a lifted-state selector that does not factor through the custody projection;
- assign independent recurrences to the primary pairing, chart restrictions, or directed transfers;
- apply the total global `Q` count to the newborn active axis without derivation;
- reopen the corrective descriptive branch.

## Evidence classes

`COUNTERMODEL` and `MODEL WITNESS` require a complete instantiated coupled-state recurrence.

A rejected contender requires an exact contradiction certificate naming the failed Phase-Calculus obligation.

An independence claim requires two complete lawful recurrences and an inequivalence proof under the declared action-equivalence relation.
