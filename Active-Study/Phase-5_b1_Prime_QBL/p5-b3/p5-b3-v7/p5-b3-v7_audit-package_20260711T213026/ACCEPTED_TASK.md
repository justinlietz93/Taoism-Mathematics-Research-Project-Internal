# Accepted Task — p5-b3-v8

The active task is the unified state-forced primary-pairing analysis in `OUTGOING_INSTRUCTIONS.md`.

It incorporates:

- the `p5-b3-v7` audit;
- the State-Forced Derivation Principle;
- the exact included parallel package
  `inputs/convergence/p5_v8y_primary-pairing-star-phase-compatibility_20260711_162758.zip`;
- the parallel package's star/phase route, subject to the audit corrections below.

The parallel package is bound by SHA-256:

```text
491ecfffd78ce5ab11e82794381e7579168027892dce7986fb9d4d3507395d27
```

The verdict remains `REVISE`. The branch remains `p5-b3`. The next interaction remains `p5-b3-v8`.

The task must compare these contenders head to head:

```text
A. local ordered two-slot restriction architecture
B. genuine independent star-semantics gap
C. one intrinsic action forced by complete retained state
```

It must distinguish:

```text
1. local ordered two-slot restriction interface
2. global Pair(-,-) bifunctor
3. algebraic realization of the intrinsic pairing law
```

No contender may be accepted without a complete retained-state certificate.

A complete transition must preserve custody-only primitive selection:

\[
p_t=\Sigma_{\mathrm{cust}}(X_t),
\qquad
\widehat X_{t+1}=\mathcal U_{\Sigma_{\mathrm{cust}}(X_t)}(\widehat X_t).
\]

The custody selector uses the strict priority:

```text
B when B is admissible.
Q when B is blocked and another lawful phase position remains.
L when B is blocked and no lawful phase position remains.
```

When \(\Sigma(\widehat X_t)\) is used as shorthand, it must factor through the custody projection:

\[
\Sigma(\widehat X_t)=\Sigma_{\mathrm{cust}}\!\left(\pi_X(\widehat X_t)\right).
\]

`B`, `Q`, and `L` may appear only as case maps. A model witness may not receive the primitive letter as an external runtime input.

The pairing, both chart restrictions, and both directed transfers may mutate after custody selects the primitive. They may not select, permit, block, or veto it. \(\Sigma\) is notation for the custody predicates, not a new retained selector object.

All four descendants must remain induced from the updated primary pairing:

\[
D_{ab,t+1}=R_{ab,t+1}(P_{t+1}).
\]

The retained state must fix the restriction maps, or the task must report the exact missing datum. Independent recurrences for the primary pairing, two chart restrictions, and two directed transfers are not admissible.

The local ordered two-slot restriction interface is already adopted. The task tests whether that interface is sufficient to force the intrinsic law; it does not retest whether the interface exists.

The scalar descendant scope is fixed as follows:

```text
FIRST-DOMAIN COMPLETED LOCAL DESCENDANT i/4895: DERIVED
GLOBAL ALL-DOMAIN ACTIVE-SPINE FORMULA: NOT YET DERIVED
```

The newborn active axis after the first `L` remains distinct from the completed retained old-axis descendant. No total-global-`Q` formula may be assigned to that newborn axis without derivation.

`STAR_SEMANTICS_AXIOM` is not preaccepted. The task must first determine whether star semantics are forced by the retained state, exact word, Q cycle, four descendants, L retention, or the pairing realization itself.

The exact `+i` witness is scoped to the Domain-0 active descendant. Any `+i` law for a later axis requires a derived axis-indexed recurrence.
