# Lab Journal

## 2026-07-11

- Loaded the accepted `p5-b3-v2` audit and current primitive custody/Orthad law.
- Split the carry statement by index: the completed `c_A` is recoverable at `S_A^-`; the future `c_{A+1}` is not appended by the closing `L`.
- Tested the primary-pairing type against the chart pullback equations, directed mixed blocks, quarter-turn phase, and rank extension.
- Derived the minimal duality-morphism interface and isolated ordinary versus conjugate scalar variance as the earliest type fork.
- Proved raw presentation nonuniqueness using bilinear and sesquilinear seeds sharing the same local normalization.
- Derived exact local active-axis `B/Q` recurrences and the conditional orthogonal `L` block signature.
- Replayed the first-domain custody word and recovered `i/4895` without instantiating a full pairing.
- Kept chart maps, transfers, descriptive-layer independence, gauge/FQM, and terminal projection open.
