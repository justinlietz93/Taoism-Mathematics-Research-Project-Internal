# Pairing-first prior-result extract

**Classification:** contextual prior analysis, not primitive authority.

The pairing-first architecture supports the following minimal interface:

```text
P_t : H_t -> D(H_t)
```

where `D` is a contravariant duality. The chart blocks then have the common form

```text
D(iota_a) o P_t o iota_b.
```

The current authority does not decide whether scalar variance is ordinary or conjugate. The surviving realizations include an ordinary-dual bilinear pairing and a conjugate-dual sesquilinear pairing. Hermitian self-adjointness is an additional specialization, not a derived property.

The first missing type axiom is therefore:

```text
SCALAR_VARIANCE_AXIOM
```

It must decide whether scalar multiplication in the duality is ordinary or conjugated. Seed admissibility, normalization, and gauge class remain separate obligations.

At the first `L`, the architectural orthogonality requirement forces the old/new mixed birth blocks to vanish. The old block is retained. The new-axis rank-one block is not fixed until the pairing type and seed law are fixed.
