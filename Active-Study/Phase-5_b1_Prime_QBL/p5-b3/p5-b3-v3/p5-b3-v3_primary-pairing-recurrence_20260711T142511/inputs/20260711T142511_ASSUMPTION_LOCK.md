# Assumption Lock

## Primary authority

`QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md` controls primitive custody and Orthad architecture.

## Accepted inputs

- The canonical pre-`L` QBL orbit and affine error orbit semiconjugacy.
- The global threshold theorem and boundary carry sequence.
- The carry-indexing correction from the `p5-b3-v2` audit.

## Context only

- The supplied Orthad diagram visualizes dependency order. The written law controls notation and status.
- Pairing-first prior analyses may identify candidate interfaces but cannot override the primary authority.

## Hard exclusions

This pass does not import a constant diagonal matrix, post-`L` lens, finished FQM form, Weil matrix, `Z/12Z` successor, or independently seeded chart blocks as the primary pairing.

## Status discipline

A local active-axis scalar is not the full pairing. A type signature is not a value recurrence. An architectural rank law is not a completed module construction. Open items remain `NOT YET DERIVED`.
