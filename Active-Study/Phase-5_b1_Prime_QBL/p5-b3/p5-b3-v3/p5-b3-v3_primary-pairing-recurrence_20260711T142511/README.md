# p5-b3-v3 Primary Pairing Recurrence

This package executes the hard-stop analysis requested after the accepted `p5-b3-v2` audit.

## Rebuild

From the package root:

```bash
python scripts/20260711T142511_build_package.py \
  --package-root . \
  --archive-out ../p5-b3-v3_primary-pairing-recurrence_20260711T142511.zip
```

The command regenerates scientific outputs, traces, the executed notebook, figures, manifest, deterministic ZIP, and ZIP SHA-256.

## Mathematical result

The minimal primary-pairing interface is an abstract duality morphism `P_t:H_t->D(H_t)`. The current authority does not choose ordinary versus conjugate scalar variance, a unique seed, or exact value-level primitive mutations.

`p5-b3` remains open.
