# Findings

## Decisive boundary

`PPGRL` is retained as the complete research target, but its first unresolved datum is the exact state-indexed argument-and-placement realization. The current authority fixes only the architectural skeleton.

The repaired descendant dependency is the pointwise restriction law

\[
D_{ab}(x,y)=P(\iota_a x,\iota_b y).
\]

This is non-vacuous because it excludes independently chosen descendant targets. A constant map can satisfy the bare equation `D=R(P)` but fails pointwise fidelity when primary objects differ on the placed arguments.

## Resulting status

```text
NON-VACUOUS RESTRICTION FACTORIZATION INTERFACE: PROVED
EXACT QBL FACTORIZATION REALIZATION: NOT YET DERIVED
STATE-FORCED INTRINSIC ACTION: NOT YET DERIVED
```

The source-forced `B/Q/L` architectural signatures survive. Exact value laws remain open because the argument/placement realization, pairing carrier/codomain, generator, seed, and action category are not yet fixed.
