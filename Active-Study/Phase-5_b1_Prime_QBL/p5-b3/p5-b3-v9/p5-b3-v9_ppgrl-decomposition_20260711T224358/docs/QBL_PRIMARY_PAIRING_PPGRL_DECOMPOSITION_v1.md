# QBL Primary Pairing: PPGRL Dependency Decomposition

**Version:** v1  
**Research interaction:** `p5-b3-v9`  
**Primary authority:** `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md`  
**Binding method:** State-Forced Derivation Principle

## Executive result

The current authority forces a nontrivial architectural skeleton but not a complete primary-pairing recurrence. The repaired dependency analysis closes two points that were previously conflated:

1. `PPGRL` is the umbrella target, not the first missing datum.
2. The equation `D=R(P)` becomes a genuine dependency certificate only when `R` is a realization-fixed restriction operation satisfying pointwise fidelity through state-fixed placements.

The first unresolved datum is therefore not scalar variance, star semantics, or the whole recurrence. It is:

```text
STATE-INDEXED ARGUMENT-AND-PLACEMENT REALIZATION
```

This datum must fix the ambient argument architecture, the two chart argument objects, the two placements, their `B/Q` transport, and their `L` old/new extension at every exact word prefix.

The strongest current result is:

```text
NON-VACUOUS FACTORIZATION INTERFACE: PROVED
QBL INSTANTIATION OF THAT INTERFACE: NOT YET DERIVED
STATE-FORCED INTRINSIC B/Q/L VALUE ACTION: NOT YET DERIVED
```

---

## 1. Accepted incoming boundary

The following are retained without reopening them:

```text
CUSTODY-ONLY PRIMITIVE SELECTION: PROVED
LOCAL ORDERED TWO-SLOT ARCHITECTURE: PROVED
ONE PRIMARY OBJECT GENERATES FOUR DESCENDANTS: SOURCE-FORCED DIRECTION
B/Q FIX ARCHITECTURAL RANK: PROVED
L RETAINS OLD ARCHITECTURE: PROVED
L PRESERVES TWO-SLOT ARITY: PROVED
L ADDS ONE ARCHITECTURAL AXIS: PROVED
DOMAIN-0 COMPLETED OLD-AXIS DESCENDANT i/4895: PROVED
TYPED STAR LAW: NOT YET DERIVED
STATE-FORCED INTRINSIC ACTION: NOT YET DERIVED
```

The primary authority gives the coupled order:

```text
custody selects primitive
-> custody state advances
-> primary pairing mutates from advanced state and exact word
-> both chart restrictions derive from the primary pairing
-> both directed transfers derive from the primary pairing
-> complete state retained
-> no projection
```

It also labels `iota_a^* P iota_b` as an intended algebraic shape and leaves the exact chart-map and all-depth primary recurrence as formalization obligations. Therefore the repeated star cannot be typed before the realization below is fixed.

---

## 2. PPGRL is a dependency stack

`PPGRL` denotes the complete unresolved deliverable. It decomposes as follows.

| Order | Layer | Authority-forced content | Open content |
|---:|---|---|---|
| 0 | Custody | Exact `X_t`, exact word, strict `B>Q>L`, no Orthad veto | none in this pass |
| 1 | Architectural skeleton | Two ordered slots, two orientation hands, four ordered placements, fixed rank under `B/Q`, old/new extension under `L` | exact objects and maps |
| 2 | Argument and placement realization | Existence and role of two placements are forced | `H_t`, `C_{±,t}`, `iota_{±,t}`, their prefix transport, and `L` embeddings |
| 3 | Primary carrier/codomain | One primary object exists and is generative | exact pairing object class, codomain, evaluation notion |
| 4 | Restriction constructor | Descendants must be restrictions, not independent seeds | exact realization of restriction and its compatibility laws |
| 5 | Seed | A primary object exists before the first primitive | exact seed |
| 6 | `B` action | fixed architecture/rank; latched sectors retained; active arithmetic data changes | exact primary update |
| 7 | `Q` action | fixed architecture/rank; active orientation advances by a quarter turn | exact action on slots, pairing, codomain, and placements |
| 8 | `L` action | two-slot arity retained; old architecture retained; one new axis appended | mixed and new-new values, codomain transport, exact placements |
| 9 | Equivalence | coordinate presentations must preserve the complete coupled evolution | exact representation/gauge category |

### Earliest missing datum

The earliest unresolved object is the exact state-indexed argument-and-placement family. It precedes pairing type because it supplies the arguments on which a pairing could act. It precedes chart restriction because it supplies the placements through which restriction is defined. It precedes `L` components because it supplies the old/new inclusions.

```text
EARLIEST MISSING DATUM:
STATE-INDEXED ARGUMENT-AND-PLACEMENT REALIZATION
```

This is narrower than `PPGRL` and earlier in dependency order.

---

## 3. Minimal non-vacuous primary-to-descendant factorization

The weakest useful typed realization at one prefix has:

\[
\iota_{+,t}:C_{+,t}\to H_t,
\qquad
\iota_{-,t}:C_{-,t}\to H_t,
\]

and a two-slot primary evaluator

\[
P_t:H_t\times H_t\to K_t.
\]

No scalar structure is assumed on `K_t`. For `a,b in {+,-}`, define

\[
D_{ab,t}(x,y)
=
P_t\bigl(\iota_{a,t}(x),\iota_{b,t}(y)\bigr).
\tag{3.1}
\]

This yields the four descendants:

\[
D_{++,t}=\Omega_t^+,
\quad
D_{--,t}=\Omega_t^-,
\quad
D_{+-,t}=T_t^{+\to-},
\quad
D_{-+,t}=T_t^{-\to+}.
\]

Equation (3.1) defines a realization-fixed restriction constructor

\[
D_{ab,t}=\rho_t(P_t,\iota_{a,t},\iota_{b,t}).
\]

Its load-bearing property is pointwise fidelity, not the mere existence of a function named `rho`.

### 3.1 Constant-map smuggling theorem

For any arbitrary target `d`, the constant map

\[
R_d(P)=d
\]

satisfies `d=R_d(P)` for every `P`. Therefore `D=R(P)` alone is vacuous.

The construction is rejected by the restriction identity. If `P_0` and `P_1` differ on the images of fixed placements, then their lawful restrictions differ on the corresponding chart arguments. A constant map cannot equal both. Thus arbitrary target encoding is excluded by:

```text
REALIZATION-FIXED RULE
+ NO TARGET-DESCENDANT INPUT
+ POINTWISE FIDELITY ON PLACED ARGUMENTS
```

If a constant presentation happens to agree with the primary object on every placed argument in the entire admissible family, it is extensionally the same restriction on that family. It is not evidence of an independently selected descendant.

### 3.2 Status

```text
NON-VACUOUS RESTRICTION FACTORIZATION INTERFACE: PROVED
EXACT QBL PLACEMENT MAPS: NOT YET DERIVED
EXACT QBL RESTRICTION CONSTRUCTOR REALIZATION: NOT YET DERIVED
```

---

## 4. What the retained state forces about argument architecture

The current law forces the following skeleton.

1. The primary object has two ordered argument roles.
2. Each role admits a `+` or `-` chart placement, giving four ordered descendants.
3. `B` and `Q` preserve architectural rank and every latched sector.
4. `L` preserves the old architecture and appends one new active axis.
5. `L` extends each argument role into old and new sectors; it does not add a third pairing argument.
6. Exact word history is load-bearing.

It does not force:

- exact ambient or chart objects;
- whether the two hands are subobjects, covers, fibers, labels, or another realization;
- exact `iota_+` and `iota_-` maps;
- how `B/Q` transport the placements;
- exact old/new inclusions at `L`;
- a unique argument family up to an as-yet undefined action equivalence.

Therefore:

```text
ARGUMENT-OBJECT ARCHITECTURAL SKELETON: PROVED
EXACT ARGUMENT-OBJECT FAMILY: NOT YET DERIVED
CHART PLACEMENT MAPS: NOT YET DERIVED
```

The first missing retained distinction is the one that tells which state-indexed argument/placement realization is the intrinsic one.

---

## 5. Input signature of the primary generator

The authority says that the primary pairing mutates from the advanced retained custody state and exact word prefix. Since the exact word belongs to `X_t`, the source-forced generative direction is

\[
P_t=G(\mathcal A_t,X_t),
\tag{5.1}
\]

where `A_t` denotes the state-indexed argument-and-placement architecture. At the next step,

\[
P_{t+1}=G(\mathcal A_{t+1},X_{t+1}).
\tag{5.2}
\]

The prior `P_t` may be used as an incremental cache only if an implementation proves

\[
U_p(P_t,X_t)=G(\mathcal A_{t+1},X_{t+1}).
\]

It is not an independent source of choice. The four descendants cannot be inputs to `G`; that would reverse the source-forced dependency.

```text
PRIMARY GENERATION INPUT DIRECTION: PROVED
EXACT GENERATOR G: NOT YET DERIVED
```

---

## 6. Intrinsic `B` boundary

Under `B`, custody performs

\[
(u,v)\mapsto(v,u+v).
\]

The authority forces:

```text
architectural rank fixed
all latched sectors retained
phase retained
active arithmetic data updated
primary object regenerated from the advanced retained state
four descendants restricted from that updated primary object
no projection
```

The accepted Domain-0 local descendant obeys

\[
a\mapsto a\frac{u}{u+v}.
\]

This constrains one local active descendant. It does not determine the full primary object.

Pullback, regeneration, and direct value mutation are not yet distinct intrinsic laws. They are possible presentation forms. To prove two are equivalent requires the action-equivalence diagram of Section 10. To prove one is intrinsic requires the missing generator `G` and argument/placement realization.

```text
INTRINSIC B ARCHITECTURAL SIGNATURE: PROVED
INTRINSIC B VALUE ACTION: NOT YET DERIVED
FIRST B-SPECIFIC MISSING BRIDGE:
B-equivariance/update law for G on the exact argument-and-placement realization
```

---

## 7. Intrinsic `Q` boundary

Custody advances

\[
\theta\mapsto\theta+\frac\pi2
\]

without changing the pair or architectural rank. The weakest exact orientation statement is an order-four action on the active orientation projection:

\[
J^4=\mathrm{id}
\]

at the visible orientation level. Four `Q` steps do not identify the full lifted state because `k`, `j`, the exact word, and retained history differ.

The Domain-0 local descendant obeys

\[
a\mapsto ia.
\]

This does not determine whether `J` acts on the first slot, second slot, both slots, the codomain, the primary object as a whole, or the placements. The two orientation hands and four ordered descendants require compatible co-evolution, but they do not select one of those representations without the missing argument action and restriction realization.

```text
ORDER-FOUR ACTIVE ORIENTATION QUOTIENT: PROVED
INTRINSIC Q ACTION ON PRIMARY PAIRING: NOT YET DERIVED
FIRST Q-SPECIFIC MISSING BRIDGE:
state-forced action of the quarter-turn generator on arguments, placements, primary object, and codomain
```

The typed star remains downstream of that bridge.

---

## 8. Intrinsic `L` boundary

At `L`, each argument architecture acquires old and new sectors. Write abstract inclusions

\[
e_{\mathrm{old},t}:H_t\to H_{t+1},
\qquad
e_{\mathrm{new},t}:N_{t+1}\to H_{t+1}.
\]

The primary object at `t+1` has four two-slot components:

```text
old-old
old-new
new-old
new-new
```

Old-old retention is not a reflexive statement. In an evaluation realization it requires a codomain transport `kappa_t:K_t -> K_{t+1}` and the pointwise law

\[
P_{t+1}(e_{\mathrm{old},t}x,e_{\mathrm{old},t}y)
=
\kappa_t(P_t(x,y)).
\tag{8.1}
\]

Equation (8.1) is the exact shape of retained old architecture. The authority does not determine `kappa_t`, the mixed components, or the new-new component.

```text
L OLD-OLD RETENTION SHAPE: PROVED
L OLD-NEW: NOT YET DERIVED
L NEW-OLD: NOT YET DERIVED
L NEW-NEW: NOT YET DERIVED
ZERO MIXED COMPONENTS: NOT DERIVED
```

The newborn axis is distinct and unmutated. The completed Domain-0 old-axis descendant is

\[
\frac{i}{4895},
\]

but this value does not initialize the newborn axis and does not determine any mixed component.

---

## 9. Star semantics after the factorization repair

The non-vacuous restriction formula uses ordinary precomposition and requires no star. A star appears only in a represented algebraic realization where one placement action is encoded contravariantly.

Therefore:

- the repaired restriction interface does not force a star;
- `Q` does not force a star before its argument/codomain action is typed;
- a typed star and the `Q` action may arise from one prior realization law;
- genuine star independence requires two complete lawful all-prefix models with inequivalent stars, which do not yet exist.

```text
STAR LAW: NOT YET DERIVED
STAR-SEMANTICS INDEPENDENCE: NOT YET DERIVED
```

---

## 10. Action equivalence

Two algebraic presentations are coordinate-equivalent only if a prefix-indexed family of isomorphisms preserves:

```text
custody and exact word
all B/Q/L case maps
ambient and chart argument objects
plus/minus placements
primary object and codomain evaluation
non-vacuous restriction constructor
all four descendants
old/new sector structure
seed and every reachable prefix
no projection
```

Agreement on `i/4895`, operation counts, or raw matrices is insufficient.

```text
ABSTRACT ACTION-EQUIVALENCE CRITERION: PROVED
EXACT ACTION-EQUIVALENCE CATEGORY: NOT YET DERIVED
```

No pair of complete lawful models is available, so no independence or inequivalence theorem is claimed.

---

## 11. Domain-0 local verification

For the exact word

```text
B Q Q B B B Q B Q B B Q B B L
```

the local active descendant before `L` is

\[
a_t=\frac{i^{\#Q(W_t)}}{u_tv_t}.
\]

`B` changes only the denominator pair and gives

\[
\frac{i^q}{uv}
\mapsto
\frac{i^q}{v(u+v)}
=
\frac{i^q}{uv}\frac{u}{u+v}.
\]

`Q` increments `q` and gives multiplication by `i`. The endpoint is

\[
q=(55,89),
\qquad
\#Q=5,
\qquad
\frac{i^5}{55\cdot89}
=
\frac{i}{4895}.
\]

This is **CERTIFIED EXACTLY** by the companion script and notebook. It remains a local descendant, not the primary pairing.

---

## 12. Constant-map negative control

The computational control instantiates two primary evaluators that differ on the same placed arguments. A constant descendant constructor returns the same target for both and therefore satisfies the vacuous equation `D=R(P)`. It fails the pointwise restriction identity for at least one primary evaluator and is rejected.

```text
CONSTANT-MAP SMUGGLING UNDER D=R(P): COUNTERMODEL TO SUFFICIENCY
CONSTANT MAP AS A LAWFUL QBL MODEL: REJECTED
```

This is a dependency negative control, not an alternate intrinsic action.

---

## 13. Status ledger

```text
PPGRL UMBRELLA TARGET:
    ADOPTED

EARLIEST MISSING DATUM:
    STATE-INDEXED ARGUMENT-AND-PLACEMENT REALIZATION

ARGUMENT-OBJECT ARCHITECTURE:
    SOURCE-FORCED SKELETON: PROVED
    EXACT FAMILY: NOT YET DERIVED

CHART PLACEMENT MAPS:
    EXISTENCE AND ROLE: SOURCE-FORCED
    EXACT MAPS AND PREFIX RECURRENCE: NOT YET DERIVED

NON-VACUOUS RESTRICTION FACTORIZATION:
    ABSTRACT POINTWISE INTERFACE: PROVED
    EXACT QBL REALIZATION: NOT YET DERIVED

PRIMARY PAIRING SEED:
    NOT YET DERIVED

INTRINSIC B ACTION:
    ARCHITECTURAL SIGNATURE: PROVED
    PRIMARY VALUE ACTION: NOT YET DERIVED

INTRINSIC Q ACTION:
    ORDER-FOUR ORIENTATION QUOTIENT: PROVED
    PRIMARY ACTION TYPE: NOT YET DERIVED

INTRINSIC L ACTION:
    OLD-OLD RETENTION SHAPE: PROVED
    MIXED AND NEW-NEW COMPONENTS: NOT YET DERIVED

STAR LAW:
    NOT YET DERIVED

ACTION-EQUIVALENCE CATEGORY:
    ABSTRACT COMMUTING CRITERION: PROVED
    EXACT REPRESENTATION CATEGORY: NOT YET DERIVED

COMPLETE-MODEL INDEPENDENCE:
    NOT YET DERIVED

STATE-FORCED INTRINSIC ACTION:
    NOT YET DERIVED
```

---

## 14. Descriptive and Orthad boundaries

This pass does not reopen the descriptive-layer audit.

```text
D1 DOMAIN-PROPER EFFECTIVE INVARIANT: NOT YET DERIVED
HIGHER-ORDER DESCRIPTIVE L: NOT YET DERIVED
```

The Orthad lane remains pairing-first and open at the exact value level:

```text
EXACT PRIMARY PAIRING TYPE: NOT YET DERIVED
EXACT PRIMARY PAIRING SEED: NOT YET DERIVED
EXACT B/Q/L VALUE RECURRENCE: NOT YET DERIVED
EXACT CHART MAPS: NOT YET DERIVED
EXACT DIRECTED TRANSFERS: NOT YET DERIVED
```

---

# PROVED

- `PPGRL` decomposes into a strict dependency chain.
- The source forces a two-slot architectural skeleton and primary-to-four-descendant direction.
- A mere equation `D=R(P)` is vacuous.
- Pointwise restriction through state-fixed placements is a non-vacuous factorization interface.
- The primary generation direction excludes external choice and circular descendant dependence.
- The architectural `B/Q/L` signatures stated above follow from the primary authority.
- The Domain-0 local descendant recurrence and endpoint are exact.
- The abstract action-equivalence criterion must preserve the repaired factorization.

# CERTIFIED FINITELY

- Exact Domain-0 custody word and prefix trace.
- Exact local descendant endpoint `i/4895`.
- Finite constant-map smuggling control and pointwise-restriction rejection.
- Deterministic notebook execution, manifest verification, and archive rebuild.

# OBSERVED

- No complete all-prefix pairing realization is currently available for comparison.
- Pullback, regeneration, and value mutation remain presentation candidates rather than proved distinct laws.

# OPEN

- Exact state-indexed argument-and-placement realization.
- Exact pairing carrier, codomain, and evaluator.
- Exact QBL restriction constructor realization.
- Primary seed.
- Intrinsic `B`, `Q`, and `L` value recurrences.
- Typed star law.
- Exact action-equivalence category.
- Complete-model independence.
- One state-forced intrinsic action.
