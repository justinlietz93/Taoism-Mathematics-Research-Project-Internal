# Assumption Lock

- Primary authority: `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md`.
- Incoming audit boundary: `p5-b3-v8_audit-package_20260711T223909.zip`.
- The State-Forced Derivation Principle is binding:
  - determine how the complete retained state constrains every unresolved continuation;
  - reject external choices, lost retained structure, circular descendant dependence, and transitions that do not close the next coupled state;
  - treat algebraic forms as representations of an intrinsic update, not options selected by evolution;
  - when multiple complete laws survive, identify the exact missing retained distinction.
- Primitive selection factors only through custody under strict `B > Q > L` priority.
- Pairing, chart restrictions, and transfers mutate after primitive selection and cannot select, permit, block, or veto it.
- The local ordered pairing interface has two slots and four ordered plus/minus placements.
- One primary object is generative for both chart restrictions and both directed transfers.
- No global `Pair(-,-)` bifunctor, vector space, field, scalar codomain, adjoint, star, Hermitian law, or gauge group is assumed.
- `i/4895` is the completed Domain-0 old-axis local descendant only.
- `L` retains old architecture, preserves two-slot arity, and adds one architectural axis.
- A descendant equation is not a dependency certificate if its restriction map can encode an independently selected target.
- `PPGRL` is an umbrella research target, not the earliest missing datum.
- Independence requires complete all-prefix recurrences and an inequivalence proof.
- No projection occurs during custody evolution.
