# p5-b3-v9_ppgrl-decomposition_20260711T224358

This package decomposes the primary-pairing generation and restriction program, repairs the primary-to-descendant factorization, proves the constant-map smuggling boundary, and records the strongest currently forced `B/Q/L` signatures.

## Rebuild

From the extracted package directory:

```bash
python scripts/20260711T224358_build_package.py --root . --archive-parent ..
```

The command regenerates exact outputs, traces, the executed notebook, figures, manifest, deterministic ZIP, and ZIP SHA-256.

## Primary result

```text
EARLIEST MISSING DATUM:
STATE-INDEXED ARGUMENT-AND-PLACEMENT REALIZATION

NON-VACUOUS RESTRICTION FACTORIZATION INTERFACE: PROVED
STATE-FORCED INTRINSIC ACTION: NOT YET DERIVED
```
