[CURRENT STATE]

Use `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md` as the primary authority.

Use the State-Forced Derivation Principle as a binding constraint.

Use `p5-b3-v8` as the accepted incoming boundary, with this correction:

```text
PPGRL AS AN UMBRELLA RESEARCH TARGET: ADOPT
PPGRL AS THE FIRST MISSING DATUM: REJECT
```

Keep these results:

```text
CUSTODY-ONLY PRIMITIVE SELECTION: PROVED
LOCAL ORDERED TWO-SLOT ARCHITECTURE: PROVED
ONE PRIMARY OBJECT GENERATES FOUR DESCENDANTS: SOURCE-FORCED DIRECTION
B/Q FIX ARCHITECTURAL RANK: PROVED
L RETAINS OLD ARCHITECTURE: PROVED
L PRESERVES TWO-SLOT ARITY: PROVED
L ADDS ONE ARCHITECTURAL AXIS: PROVED
DOMAIN-0 COMPLETED OLD-AXIS DESCENDANT i/4895: PROVED
TYPED STAR LAW: NOT YET DERIVED
STATE-FORCED INTRINSIC ACTION: NOT YET DERIVED
```

The equation

\[
D_{ab,t+1}=R_{ab,t+1}(P_{t+1})
\]

is not sufficient when `R_ab` may encode an independently chosen descendant as a constant map.

[STRATEGIC QUESTION]

Determine the minimal non-vacuous dependency chain from retained state to the primary pairing and from the primary pairing to its four descendants.

Then determine how far that repaired dependency chain forces the intrinsic `B/Q/L` action.

[REASONING TASK]

1. Decompose `PPGRL`.

   Separate these obligations:

   ```text
   argument-object architecture
   chart placements or embeddings
   primary pairing carrier and codomain
   restriction constructor
   primary seed
   B action
   Q action
   L old/new extension
   action-equivalence category
   ```

   Determine their dependency order.

   Identify the earliest missing datum or theorem.

   Do not name the entire unresolved recurrence as the first missing datum.

   Report: `PPGRL_DEPENDENCY_DECOMPOSITION.md`.

2. Repair the descendant factorization.

   Introduce only the weakest abstract structure needed to express

   \[
   D_{ab,t}=\rho_t(P_t,\iota_{a,t},\iota_{b,t}).
   \]

   Do not assume vector spaces, a global bifunctor, or an adjoint.

   Require the retained state to fix the placement data `iota_+` and `iota_-`, or name the exact missing datum that would fix them.

   Require the realization to fix `rho`, or name the exact missing datum that would fix it.

   Require identical `P`, `rho`, and placement data to produce identical descendants.

   Exclude restriction maps that encode independently selected target descendants.

   Report: `NONVACUOUS_DESCENDANT_FACTORIZATION.md`.

3. Prove the constant-map smuggling boundary.

   Show that an arbitrary descendant can satisfy `D=R(P)` through a constant `R`.

   State the exact additional condition that rejects this construction.

   Use this as a negative control, not as a lawful model.

4. Determine what the retained state forces about argument architecture.

   Work from:

   ```text
   two orientation hands
   four ordered placements
   exact word history
   fixed rank under B/Q
   old/new sector extension under L
   one new active axis at L
   retained old architecture
   ```

   Determine whether these facts force one abstract argument-object family up to action equivalence.

   Report either the forced structure or the first missing retained distinction.

5. Reassess the primary update input.

   Determine whether `P_{t+1}` is forced by:

   ```text
   the advanced custody state and exact word alone
   the prior primary pairing plus the advanced custody state
   the complete prior lifted state
   ```

   Use the primary authority's statement that the pairing is generated from retained state and ordered word.

   Reject any input signature that introduces an external choice or circular descendant dependence.

6. Resume the intrinsic `B` derivation.

   Determine the strongest state-forced action on the active sector while preserving all latched sectors.

   Test whether pullback, regeneration, and value mutation are distinct intrinsic laws or coordinate presentations.

   Use complete coupled transitions only.

   Report the exact law or the first missing datum.

7. Resume the intrinsic `Q` derivation.

   Determine the weakest abstract order-four orientation action forced by the retained phase and exact word.

   Keep the exact `+i` law scoped to the Domain-0 local descendant.

   Determine whether the two orientation hands and four descendants force where the quarter-turn acts.

   Do not type the star before the restriction constructor is typed.

8. Resume the intrinsic `L` derivation.

   Preserve two-slot arity.

   Express each argument architecture in old and new sectors.

   Keep

   ```text
   old-old retained
   old-new open
   new-old open
   new-new open
   ```

   Determine whether retained state, exact history, and the two orientation hands force any mixed or new-new relation before a scalar realization is chosen.

9. Reevaluate star semantics after Steps 1 through 8.

   Determine whether the repaired restriction constructor induces the star law.

   Determine whether `Q` and star arise from one prior action type.

   Do not adopt `STAR_SEMANTICS_AXIOM` without a complete independence certificate.

10. Define action equivalence with the repaired factorization.

    Preserve:

    ```text
    custody and exact word
    primitive case maps
    primary pairing
    placement data
    restriction constructor
    all four descendants
    old/new sector structure
    seed and every prefix
    no projection
    ```

    Determine whether any surviving presentations are coordinate-equivalent.

11. Use complete models only for independence claims.

    A lawful model must compute the complete coupled state at every prefix.

    It must use custody-only primitive selection.

    It must generate all four descendants through the repaired non-vacuous factorization.

    It must reproduce the accepted Domain-0 descendant and first-`L` retention.

    If two complete laws survive, prove inequivalence under Step 10.

    Otherwise leave independence open.

12. Strengthen the executable and formal companions.

    Add a test that rejects constant-map descendant smuggling.

    Encode the dependency layers separately.

    Do not treat a reflexive embedding theorem as proof of old-old retention.

    Compile Lean when available.

    Otherwise report:

    ```text
    LEAN SOURCE PRESENT; PROOF AND COMPILATION NOT VERIFIED
    ```

13. Preserve the descriptive boundary.

    Keep:

    ```text
    D1 DOMAIN-PROPER EFFECTIVE INVARIANT: NOT YET DERIVED
    HIGHER-ORDER DESCRIPTIVE L: NOT YET DERIVED
    ```

14. Report these statuses separately.

    ```text
    PPGRL UMBRELLA TARGET:
    EARLIEST MISSING DATUM:
    ARGUMENT-OBJECT ARCHITECTURE:
    CHART PLACEMENT MAPS:
    NON-VACUOUS RESTRICTION FACTORIZATION:
    PRIMARY PAIRING SEED:
    INTRINSIC B ACTION:
    INTRINSIC Q ACTION:
    INTRINSIC L ACTION:
    STAR LAW:
    ACTION-EQUIVALENCE CATEGORY:
    COMPLETE-MODEL INDEPENDENCE:
    STATE-FORCED INTRINSIC ACTION:
    ```

    For each `PROVED` claim, give the exact bridge.

    For each open claim, name the first missing datum.

15. Package the work.

    Write:

    `QBL_PRIMARY_PAIRING_PPGRL_DECOMPOSITION_v1.md`

    Build:

    `p5-b3-v9_ppgrl-decomposition_<timestamp>.zip`

    Include source and executed notebooks, scripts, outputs, traces, formal sources, source map, assumption lock, findings, and manifest.
