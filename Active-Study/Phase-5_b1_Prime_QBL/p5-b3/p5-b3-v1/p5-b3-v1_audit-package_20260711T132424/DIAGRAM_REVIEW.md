# Orthad Diagram Review

## Disposition

The diagram is highly useful and is substantially aligned with `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md`.

It correctly shows:

- the retained custody state before every primitive step;
- strict state selection of `B`, `Q`, or `L`;
- the Orthad mutating with the selected letter rather than selecting it;
- pairing-first construction;
- both chart restrictions and both directed transfers inside one retained object;
- no projection during custody;
- `L` retaining the completed axis and appending one new active orthogonal axis;
- the sphere as a schematic projection-side aid rather than the retained object.

## Load-bearing relevance to this audit

Panels 3, 4a, 4b, 6, and 7 show why the arithmetic carry factor cannot disprove a higher-order `L` recurrence. The new independent axis belongs to the primary pairing and Orthad lift. Their exact all-depth recurrences remain under derivation.

The agent's scalar factor

\[
\pi(S_A^-)=\lambda j_A+\beta-\nu(q_A)
\]

ignores `P`, `Omega+`, `Omega-`, and both transfers. A failure to find an independent scalar carry coordinate is therefore not a certificate that no independent higher-level distinction exists in the complete lifted object.

## Research-status update

The lower-right research-status line should now distinguish the proved and open scopes:

```text
Affine 7/8/9 canonical boundary-orbit semiconjugacy:
    proved from custody arithmetic

Full affine-language / Orthad factor:
    not yet derived
```

The diagram's architecture itself does not need to be redrawn for this audit.
