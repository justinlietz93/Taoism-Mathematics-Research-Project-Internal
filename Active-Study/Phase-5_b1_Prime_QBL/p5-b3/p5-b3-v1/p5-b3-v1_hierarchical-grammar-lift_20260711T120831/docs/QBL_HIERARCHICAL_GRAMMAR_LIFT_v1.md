# QBL Hierarchical Grammar Lift

**Step:** `p5-b3-v1`  
**Status:** theorem-bearing boundary-factor analysis  
**Primary authority:** `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md`  
**Accepted inputs:** Branch 1 affine-language closure and Branch 2 global exact-threshold theorem  
**Older canonical ledgers:** provenance only

## 1. Disposition

### Proved abstractly

```text
QBL-TO-AFFINE INTERNAL FACTOR MAP: PROVED
FACTOR SCOPE: INDUCED PRE-L DOMAIN-BOUNDARY CUSTODY DYNAMICS
PRIMITIVE-ALPHABET SYMBOL MAP: NOT LICENSED
FIVE-VALUED DEFECT: FIRST-DIFFERENCE FACTOR ONLY
EXACT ARITHMETIC FACTOR: PROVED
STRUCTURAL RECURRENCE WITHOUT IDENTITY: PROVED
```

The load-bearing invariant is not the fact that both alphabets contain three symbols. It is the exact **boundary slack cocycle** on the retained pair

\[
(j,b)\longmapsto(2j+6,\,2b+c),
\qquad c\in\{7,8,9\}.
\]

Here `j` is the carried global phase-position index and `b` is the Fibonacci-corridor index recovered from the retained pair, equivalently the cumulative number of primitive `B` operations in the exact word prefix.

### Structural boundary

```text
HIGHER-ORDER L IDENTITY: FALSE UNDER THE FOUR-PART CRITERION
HIERARCHICAL DEPTH RECURRENCE: COUNT ALIGNMENT ONLY
EXACT INTERNAL SELF-RECURRENCE: NOT PROVED
```

The affine grammar satisfies saturation, retention, and resumed-law requirements, but it does not append a new independent state axis. Its carry is a quotient label derived from two retained boundary states. It is therefore an exact arithmetic factor and a structural recurrence, not a second `L` theorem.

### Open

```text
SPECIFIC-ORBIT EQUIDISTRIBUTION: NOT PROVED
GAUGE/FQM MAP FROM d_A=±1: NOT YET DERIVED
ORTHAD ALL-DEPTH CHART/TRANSFER RECURRENCE: NOT YET DERIVED
```

## 2. Source map

| Class | Source | Use in this pass |
|---|---|---|
| **Primary** | `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md` | Exact primitive state, `B>Q>L`, capacities, floor crossing, carry across `L`, and Orthad exclusions. |
| **Accepted** | `QBL_CARRY_AFFINE_FOLLOWER_STRUCTURE_v5.md` | Exact affine recurrence, half-open coding, complexity, entropy, non-soficity, mixing, and no finite Markov order. |
| **Accepted** | `QBL_GLOBAL_EXACT_THRESHOLD_BRIDGE_v2.md` | Global identity `T_A=ceil(y_A)` and exact Fibonacci threshold definition. |
| **Contextual** | `CF03_Hierarchical_Tachyonic_Interfaces.pdf` | Distinguishes active depth from raw component multiplicity; an active depth counts scales with newly resolved structure. |
| **Conjectural target** | `STRONG-EXPLICIT-NOTES` self-recurrence observation, as preserved in the accepted Branch 2 handoff | Motivates the test; contributes no theorem premise. |
| **Provenance only** | Older Phase 5 ledgers and recovered v7 packages | Historical context only; they do not override the primary law. |

The self-recurrence note asks whether the `7/8/9` grammar is an internal recurrence of `L`. This document treats that as a question, not an assumption.

## 3. Exact boundary states

Let `W_t` be the exact ordered primitive word already executed. Define

\[
b(W):=\#_B(W),
\qquad q=(u,v).
\]

The current authority carries

\[
X=(A,q,\theta,k,j,W)
\]

inside the full coupled state

\[
\widehat X=(X,P,\Omega^+,\Omega^-,T^{+\to-},T^{-\to+}).
\]

The Orthad coordinates are retained in the boundary state but are not assigned formulas here.

For Domain `A`, write

\[
N_A=6\,2^A,
\qquad
J_A:=\sum_{r=0}^A N_r=6(2^{A+1}-1).
\]

### 3.1 Pre-`L` boundary

`S_A^-` is the complete retained state immediately before the unique `L` that closes Domain `A`:

\[
S_A^-=
\left(
\begin{array}{l}
A,\ q_A,\ \theta_A,\ k_A=N_A-1,\ j_A=J_A,\ W_A^-,\ b_A;\\
P_A^-,\Omega_A^{+,-},\Omega_A^{-,-},
T_A^{+\to-,-},T_A^{-\to+,-}
\end{array}
\right),
\]

where

\[
b_A=b(W_A^-),
\]

and `W_A^-` is the unique word prefix generated by the primitive law up to, but not including, that closing `L`.

Every earlier domain-closing `L` is already inside `W_A^-`; the current closing `L` is not. The cumulative number of `Q` letters is

\[
Q_A^{\rm cum}
=\sum_{r=0}^A(N_r-1)
=J_A-(A+1),
\]

so, for initial phase `theta_0`,

\[
\theta_A=\theta_0+\frac{\pi}{2}\bigl(J_A-(A+1)\bigr).
\]

The prefix length is therefore

\[
|W_A^-|=b_A+Q_A^{\rm cum}+A=b_A+J_A-1.
\]

### 3.2 Post-`L` boundary

Immediately after the closing `L`,

\[
S_A^+=
\left(
\begin{array}{l}
A+1,\ q_A,\ \theta_A,\ 0,\ J_A+1,\ W_A^-L,\ b_A;\\
P_A^+,\Omega_A^{+,+},\Omega_A^{-,+},
T_A^{+\to-,+},T_A^{-\to+,+}
\end{array}
\right).
\]

The pair, phase, word history, and cumulative `B` count are retained. The local position restarts at zero. The Orthad law requires the completed axis to be latched and one new active orthogonal axis to be appended, but its exact all-depth matrices remain open.

The affine carry `c_(A+1)` is **not emitted by the instantaneous `L`**. It is obtained only after the entire next domain evolves:

\[
S_A^-\xrightarrow{L}S_A^+
\xrightarrow{\text{self-selected B/Q word}}S_{A+1}^-.
\]

For reference, the generated domain segments begin

```text
A=0: BQQBBBQBQBBQBBL
A=1: BQBQBBQBQBBQBQBQBBQBQBBQBQBBBL
A=2: QBQBBQBQBBQBQBBQBQBBQBQBQBBQBQBBQBQBBQBQBBQBQBQBBQBQBBQBBL
```

Each displayed segment includes its closing `L`.

## 4. Fibonacci corridor theorem

Define

\[
q_0=(1,1)=(F_1,F_2),
\qquad B(u,v)=(v,u+v).
\]

### Theorem 4.1

After exactly `b` primitive refinements,

\[
\boxed{q_b=(F_{b+1},F_{b+2})}.
\]

**Proof.** At `b=0`, the identity is `(1,1)=(F_1,F_2)`. Assume

\[
q_b=(F_{b+1},F_{b+2}).
\]

Then

\[
q_{b+1}
=B(q_b)
=(F_{b+2},F_{b+1}+F_{b+2})
=(F_{b+2},F_{b+3}).
\]

The result follows by induction. ∎

The pair therefore determines the cumulative `B` count on the reachable corridor. Define the corridor-index function

\[
\nu(F_{b+1},F_{b+2})=b.
\]

It is not a search over candidate states; it is the unique index of the retained reachable pair.

## 5. The floor count is exactly `T_A`

At the final phase position of Domain `A`, the primitive threshold is

\[
\Delta(J_A)=2^{2J_A}.
\]

Before that final position, all admitted products are below a capacity no larger than `2^(2J_A)`. At the final position, `B` continues from the current product while it remains below `2^(2J_A)`, and the last admitted refinement is the first one that reaches or crosses that capacity. Hence the floor pair is the first Fibonacci pair whose product is at least the final capacity:

\[
\begin{aligned}
b_A
&=\min\left\{b\ge0:F_{b+1}F_{b+2}\ge2^{2J_A}\right\}\\
&=T_A.
\end{aligned}
\]

Thus

\[
\boxed{q_A=(F_{T_A+1},F_{T_A+2}),\qquad b_A=T_A.}
\]

This is an exact theorem of the custody law plus the accepted global threshold theorem, not a finite match.

## 6. Capacity/exponent identity

The final global phase-position index is

\[
J_A=\sum_{r=0}^A6\,2^r=6(2^{A+1}-1).
\]

Therefore the Branch 2 exponent is

\[
\boxed{m_A=12(2^{A+1}-1)=2J_A=2\sum_{r=0}^A N_r.}
\]

The genuinely carried QBL quantity is `j=J_A`. The exponent `m_A` is its fixed law-derived image `2j`; it is not a second independent counter.

The next-boundary identities are

\[
J_{A+1}=2J_A+6,
\qquad
m_{A+1}=2m_A+12.
\]

## 7. State-internal factor map

Set

\[
\lambda:=\frac{\log2}{\log\varphi},
\qquad
\beta:=\frac{\log5}{2\log\varphi}-\frac32.
\]

Define on reachable pre-`L` boundary states

\[
\boxed{
\pi(S^-):=\lambda j+\beta-\nu(q).
}
\]

Equivalent retained forms are

\[
\pi(S^-)=\lambda j+\beta-\#_B(W)
\]

and, after naming the derived boundary coordinate `b`,

\[
\pi(S^-)=\lambda j+\beta-b.
\]

The map uses:

1. the carried global position `j`;
2. the retained pair `q`, or equivalently the exact word prefix `W`;
3. fixed constants of the law.

It does not read `A` and import a precomputed table. At `S_A^-`,

\[
\begin{aligned}
\pi(S_A^-)
&=\lambda J_A+\beta-T_A\\
&=\frac{2J_A\log2+\log5}{2\log\varphi}-\frac32-T_A\\
&=y_A-T_A\\
&=E_A.
\end{aligned}
\]

Hence

\[
\boxed{\pi(S_A^-)=E_A.}
\]

### 7.1 Why the index-only map is weaker

The map

\[
A\mapsto y_A-T_A
\]

is a trivial index projection because it presupposes both closed formulas. The internal map above instead reconstructs the error from the state coordinates whose evolution produced the floor: `j` and the Fibonacci-corridor index carried by `q`/`W`.

A map using only `j` also fails to close the recurrence: it has no retained integer term that can produce `-c`.

## 8. Commuting boundary law

Define the complete pre-`L` boundary transition

\[
\mathcal R:S_A^-\longmapsto S_{A+1}^-.
\]

It includes the closing `L` and every self-selected primitive step in the next domain.

From the retained coordinates,

\[
j_{A+1}=2j_A+6
\]

and

\[
\boxed{
c_{A+1}
:=\nu(q_{A+1})-2\nu(q_A)
=b_{A+1}-2b_A
=T_{A+1}-2T_A.
}
\]

Let

\[
\gamma:=6\lambda-\beta
=\frac{6\log2}{\log\varphi}+\frac32-\frac{\log5}{2\log\varphi}
=8.4702446042907841279349898711327462\ldots.
\]

Then

\[
\begin{aligned}
\pi(S_{A+1}^-)
&=\lambda(2j_A+6)+\beta-(2b_A+c_{A+1})\\
&=2(\lambda j_A+\beta-b_A)+(6\lambda-\beta)-c_{A+1}\\
&=2\pi(S_A^-)+\gamma-c_{A+1}.
\end{aligned}
\]

Therefore the diagram commutes:

\[
\boxed{
\pi\circ\mathcal R
=F\circ\pi,
\qquad
F(E)=2E+\gamma-c,
\quad c\in\{7,8,9\}.
}
\]

The carry alphabet follows because `E_A in (-1,0)` globally and

\[
c_{A+1}=\lceil2E_A+\gamma\rceil.
\]

```text
QBL-TO-AFFINE INTERNAL FACTOR MAP: PROVED
```

**Scope.** This is an exact factor of the induced pre-`L` domain-boundary custody dynamics. The map is defined on the full retained boundary state but ignores Orthad coordinates. Consequently, it remains valid under every lawful Orthad completion of the same custody transition. It does not derive the missing Orthad recurrence and is not a gauge/FQM factor theorem.

## 9. Relation between the two three-symbol alphabets

| Alphabet | Scale | What a symbol does |
|---|---|---|
| `B,Q,L` | One primitive tick | Mutates pair, phase-position, or domain/rank custody. |
| `7,8,9` | One complete boundary return | Records the integer cocycle `b_(A+1)-2b_A`. |

There is no licensed symbol-by-symbol identification:

- `7`, `8`, and `9` are not primitive operators;
- a carry symbol depends on two successive floor states;
- one carry interval contains many different exact `B/Q/L` domain words;
- `L` occurs once per boundary transition regardless of whether the carry is `7`, `8`, or `9`.

The exact relation is therefore:

```text
EXACT BOUNDARY-RETURN QUOTIENT/COCYCLE
NO PRIMITIVE-ALPHABET MORPHISM OR SYMBOL IDENTIFICATION
```

The shared cardinality three is incidental to the theorem.

## 10. Five-valued defect layer

For `A>=2`, define

\[
d_A=c_A-c_{A-1}.
\]

Since each carry lies in `{7,8,9}`,

\[
\boxed{d_A\in\{-2,-1,0,1,2\}.}
\]

This is the image of two consecutive boundary edges under the difference map

\[
(c_{A-1},c_A)\longmapsto c_A-c_{A-1}.
\]

It is a scalar two-edge factor and loses edge information. It is not a five-coordinate retained object, a five-axis Orthad, or a five-variable state tuple.

```text
FIVE-VALUED DEFECT: EXACT FIRST-DIFFERENCE ALPHABET ONLY
```

## 11. Four-part higher-order `L` test

| Requirement | Verdict | Certificate |
|---|---|---|
| 1. Lower descriptive layer saturates | **PASS** | `S_A^-` is exactly the state where `B` and `Q` are blocked and `L` is forced. |
| 2. Completed structure is retained | **PASS** | `S_A^+` carries `q`, `theta`, `W`, `b`, and all prior Orthad state; only the local position restarts. |
| 3. One new independent distinction or axis becomes available in the affine grammar | **FAIL** | `c_(A+1)` is derived from `q_A,q_(A+1)` or `b_A,b_(A+1)`. No independent affine coordinate is appended at `L`. |
| 4. The same governing law resumes | **PASS** | The next boundary obeys the same `j,b,E,c` recurrence for every `A`. |

The affine grammar therefore reproduces three structural features of `L`, but fails the independent-extension requirement.

```text
HIGHER-ORDER L IDENTITY: FALSE UNDER THE FOUR-PART CRITERION
STRUCTURAL RECURRENCE WITHOUT IDENTITY: PROVED
```

A later all-depth Orthad theorem could reveal a separate independent axis correlated with the carry, but count data cannot supply that theorem.

## 12. Hierarchical-boundary test

Branch 1 proves that the number of realizable affine words of length `n` is

\[
p(n)=2^{n+1}-1.
\]

At matched index `n=A`, the QBL cumulative phase-position budget satisfies the exact arithmetic identity

\[
\boxed{J_A=6(2^{A+1}-1)=6p(A).}
\]

This is stronger than a loose inequality, but it is still a multiplicity identity. It does not construct a map from affine cylinders

\[
C(w)=\{E:\text{the first }|w|\text{ carry symbols equal }w\}
\]

to QBL retained distinctions such as exact word-prefix fibers, phase positions, latched axes, or Orthad chart sectors.

The missing theorem would require a canonical refinement-preserving family

\[
\iota_A:\mathcal C_A\longrightarrow\mathcal H_A
\]

from affine length-`A` cylinders to a defined family `H_A` of QBL retained distinctions, satisfying compatibility under cylinder refinement and primitive/`L` extension. No such `H_A` and `iota_A` are derived by the current sources.

CF03 explicitly counts active hierarchy by scales at which new structure appears, not by raw numbers of pieces. Its extraction protocol subtracts coarser boundaries and records one depth contribution only when the new multiplicity at a scale is nonzero. Therefore `J_A=6p(A)` cannot by itself prove one new active grammar depth per `L`.

```text
HIERARCHICAL DEPTH RECURRENCE: COUNT ALIGNMENT ONLY
```

## 13. Correct strength of self-recurrence

| Candidate statement | Status |
|---|---|
| Exact internal self-recurrence of primitive `L` at a higher descriptive layer | **NOT PROVED; fails the independent-axis test for the affine factor itself** |
| Exact arithmetic factor of QBL boundary evolution | **PROVED** |
| Structural recurrence without identity | **PROVED** |
| Numerical/cardinality resemblance | **TRUE BUT NON-LOAD-BEARING** |

The accepted theorem is therefore not “a fractal `L`” or “higher-dimensional `L`.” It is:

> The exact QBL pre-`L` boundary return system has a state-internal one-dimensional arithmetic factor. Its normalized threshold slack evolves by the globally exact non-sofic `7/8/9` affine grammar.

## 14. Stronger route: boundary renormalization cocycle

The original formulation emphasizes the carry alphabet. The stronger invariant is the affine lattice extension

\[
(j,b,E)\longmapsto
(2j+6,\,2b+c,\,2E+\gamma-c).
\]

The coordinate

\[
E=\lambda j+\beta-b
\]

is the normalized mismatch between:

- the logarithmic refinement depth licensed by the carried capacity index `j`; and
- the exact Fibonacci refinement depth carried by `q`/`W`.

The carry `c` is the unique integer cocycle that returns this mismatch to its fundamental interval. This explains the finite alphabet and the doubling dynamics without comparing alphabet cardinalities.

A genuinely stronger future hierarchy theorem should lift this cocycle into a refinement-preserving functor on complete word-built Orthad boundary states. That route would require the currently open all-depth pairing, chart, and transfer recurrences.

## 15. Orthad boundary

The current authority requires exact word-built pairing, two chart restrictions, and two directed transfers at every primitive prefix. Those recurrences remain open. Counts and the factor coordinate do not determine:

- a chart matrix;
- a gauge value or isometry class;
- holonomy;
- an FQM class;
- a Weil projection.

```text
GAUGE/FQM MAP FROM d_A=±1: NOT YET DERIVED
```

The factor theorem neither weakens nor fills that hold.

## 16. Formal and computational companions

The package contains:

- a deterministic derivation script that checks the symbolic dependencies and finite exact regressions;
- source and executed no-I/O notebooks with one figure and one PASS/FAIL result per claim cell;
- exact boundary-state and transition tables;
- negative controls for index-only, altered-capacity, and altered-refinement maps;
- Lean theorem surfaces for the Fibonacci pair, capacity identity, boundary carry, and factor commutation.

Lean and Mathlib were unavailable in the build environment:

```text
LEAN SOURCE PRESENT; COMPILATION NOT VERIFIED
```

## 17. Final status

```text
QBL-TO-AFFINE INTERNAL FACTOR MAP: PROVED
FACTOR SCOPE: INDUCED PRE-L DOMAIN-BOUNDARY CUSTODY DYNAMICS
EXACT ARITHMETIC FACTOR: PROVED
STRUCTURAL RECURRENCE WITHOUT IDENTITY: PROVED
PRIMITIVE-ALPHABET SYMBOL MAP: NOT LICENSED
FIVE-VALUED DEFECT: FIRST-DIFFERENCE FACTOR ONLY
HIGHER-ORDER L IDENTITY: FALSE UNDER THE FOUR-PART CRITERION
HIERARCHICAL DEPTH RECURRENCE: COUNT ALIGNMENT ONLY
EXACT INTERNAL SELF-RECURRENCE: NOT PROVED
SPECIFIC-ORBIT EQUIDISTRIBUTION: NOT PROVED
GAUGE/FQM MAP FROM d_A=±1: NOT YET DERIVED
LEAN SOURCE PRESENT; COMPILATION NOT VERIFIED
p5-b3 BRANCH STATUS: CLOSED
```
