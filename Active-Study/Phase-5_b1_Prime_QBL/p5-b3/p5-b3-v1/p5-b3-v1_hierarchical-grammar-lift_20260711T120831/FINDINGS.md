# Findings

## Decisive result

The affine carry grammar is an exact internal arithmetic factor of the retained QBL pre-`L` boundary return dynamics:

\[
\pi(S_A^-)=\frac{\log2}{\log\varphi}j_A+
\left(\frac{\log5}{2\log\varphi}-\frac32\right)-\nu(q_A)=E_A.
\]

With `j_(A+1)=2j_A+6` and `nu(q_(A+1))=2nu(q_A)+c_(A+1)`, this gives the exact commuting recurrence.

The result is not a symbolwise map from `B/Q/L` to `7/8/9`, and it is not an Orthad/gauge factor.

## Higher-order L test

```text
saturation:            PASS
retention:             PASS
independent extension: FAIL
resumed law:           PASS
```

Therefore the affine grammar is a structural recurrence without identity to primitive `L`.

## Hierarchy

`J_A=6p(A)` is exact, but CF03 active depth requires newly resolved structure and a refinement-preserving map. No canonical affine-cylinder-to-QBL-distinction map is derived.

```text
HIERARCHICAL DEPTH RECURRENCE: COUNT ALIGNMENT ONLY
```

## Holds

```text
SPECIFIC-ORBIT EQUIDISTRIBUTION: NOT PROVED
GAUGE/FQM MAP FROM d_A=±1: NOT YET DERIVED
LEAN SOURCE PRESENT; COMPILATION NOT VERIFIED
```
