# p5-b3-v1_hierarchical-grammar-lift_20260711T120831

This package proves the exact QBL-to-affine factor on induced pre-`L` domain-boundary custody dynamics and separates it from a literal higher-order `L` identity.

## Main results

```text
QBL-TO-AFFINE INTERNAL FACTOR MAP: PROVED
HIGHER-ORDER L IDENTITY: FALSE UNDER THE FOUR-PART CRITERION
HIERARCHICAL DEPTH RECURRENCE: COUNT ALIGNMENT ONLY
GAUGE/FQM MAP FROM d_A=±1: NOT YET DERIVED
p5-b3 BRANCH STATUS: CLOSED
```

## One-command rebuild

From the extracted package directory:

```bash
python scripts/20260711T120831_build_package.py \
  --package-root . \
  --archive-out ../p5-b3-v1_hierarchical-grammar-lift_20260711T120831.zip
```

The builder runs the derivation checks, validates the fixed source/executed notebooks, regenerates `MANIFEST.json`, writes the deterministic archive, and writes the external `.sha256` file.

## Proof boundary

The written theorem and SymPy identities are complete for the arithmetic factor. Lean source is present but compilation was unavailable.
