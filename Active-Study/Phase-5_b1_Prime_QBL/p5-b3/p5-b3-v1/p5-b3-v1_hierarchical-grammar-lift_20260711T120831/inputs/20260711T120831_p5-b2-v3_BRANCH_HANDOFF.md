# Branch Handoff

## Closed work

```text
p5-b1: affine carry language structure closed
p5-b2: global exact-threshold bridge closed
```

Accepted results now include:

\[
p(n)=2^{n+1}-1,
\qquad
h_{\mathrm{affine}}=\log2,
\]

non-soficity, mixing, no finite Markov order, and

\[
T_A=\left\lceil
\frac{12(2^{A+1}-1)\log2+\log5}{2\log\varphi}-\frac32
\right\rceil
\]

for every `A>=0`.

## Next branch

`p5-b3-v1` should test the observation preserved in `STRONG-EXPLICIT-NOTES`:

> Does complete QBL domain evolution project internally to the affine `7/8/9` grammar, and is the appearance of that next grammar a higher-order recurrence of the `L` saturation-and-extension law?

The branch must distinguish three outcomes:

```text
EXACT INTERNAL FACTOR MAP
ARITHMETIC COUNT SHADOW ONLY
STRUCTURAL ANALOGY WITHOUT DERIVED COMMUTATION
```

Cardinality matches alone are not enough. The decisive result is a state-derived commuting map at successive `L` boundaries.
