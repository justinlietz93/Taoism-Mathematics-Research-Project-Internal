# Assumption Lock

1. `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md` is the primitive authority.
2. Branch 1 results are accepted: exact affine language, complexity, entropy, non-soficity, mixing, and no finite Markov order.
3. Branch 2 result is accepted: `T_A = ceil(y_A)` for all `A>=0`.
4. The Fibonacci convention is `F_0=0`, `F_1=1`.
5. The factor map may read retained custody coordinates and fixed constants, but may not import a lookup table indexed only by `A`.
6. A count-only coincidence is not an Orthad theorem.
7. The `STRONG-EXPLICIT-NOTES` observation is conjectural until a commuting map and structural criteria are proved.
8. CF03 active depth is not raw multiplicity; a depth claim requires a scale/refinement map.
9. Older ledgers and recovered v7 results are provenance only unless explicitly rederived under the current law.
10. Projection, gauge, FQM, and Weil claims remain outside this pass.
