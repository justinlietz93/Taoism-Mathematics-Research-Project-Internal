# Lab Journal

## 2026-07-11

1. Read the `p5-b3-v5` audit and preserved all adopted orbit results.
2. Recomputed the malformed prior archive hash and confirmed the missing final hexadecimal digit.
3. Expanded D0 from one-step transitions to its exact path category using retained word prefixes.
4. Defined the pre-`L` section and first-return category internally to D0.
5. Constructed the D1-to-D0 interpretation on states and return paths.
6. Proved fullness and faithfulness onto the boundary-return subcategory and inverse segmentation at boundary hits.
7. Classified the carry alphabet as an information-losing path-observable factor.
8. Added two controls: a variable-length return alphabet that remains same-layer and a genuine old-description fiber split.
9. Separated local Domain-A saturation, complete D0 saturation, and D1 saturation.
10. Retained the canonical orbit-closure and Orthad obligations as open.
