# Assumption Lock

## Primary authorities

1. `CF000_Primitive_Distinguishability.pdf`
   - a genuinely higher determination adds internal determination rather than renaming;
   - a new domain is irreducible to the exhausted current domain;
   - same-domain saturation is exhaustion of genuinely new lawful articulation in that domain;
   - a newly admitted domain inherits the prior invariant stack and adds a domain-proper effective invariant.

2. `QBL_PRIMITIVE_CUSTODY_AND_ORTHAD_LAW_v2.md`
   - custody state `X=(A,q,theta,k,j,W)`;
   - strict primitive priority `B>Q>L`;
   - exact ordered word prefix is retained;
   - local Domain-A saturation occurs exactly when `B` and `Q` are blocked;
   - `L` is already a native transition of the complete all-domain custody law;
   - Orthad pairing, chart, and transfer recurrences remain separate open obligations.

## Accepted prior results

- canonical QBL-to-affine boundary-orbit semiconjugacy;
- exact boundary-return cocycle;
- global exact threshold theorem;
- canonical carry itinerary aperiodic;
- canonical symbolic orbit closure infinite and transitive;
- `n+1 <= p_can(n) <= 2^(n+1)-1`;
- `0 <= h(X_can) <= log 2`;
- full affine language has complexity `2^(n+1)-1`, entropy `log 2`, is non-sofic, mixing, and has no finite Markov order;
- canonical orbit closure equality with the full affine system remains open.

## Contamination exclusions

- failure of a one-letter symbol map is not an irreducibility proof;
- local Domain-A saturation is not saturation of the complete D0 all-domain custody layer;
- an induced return map or new alphabet is not automatically a new CF000 descriptive domain;
- an information-losing factor cannot establish a new independent determination;
- the symbolic orbit closure is not identified with a lawful enlarged QBL state family without a construction;
- no geometric axis, primary pairing, chart map, transfer, gauge value, FQM class, or Weil projection is used to decide the descriptive question.
