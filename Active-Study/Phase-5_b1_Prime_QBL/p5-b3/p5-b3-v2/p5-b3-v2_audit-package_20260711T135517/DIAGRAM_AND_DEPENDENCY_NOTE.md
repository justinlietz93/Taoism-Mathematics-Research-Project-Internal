# Diagram and Dependency Note

The supplied `orthad-diagram-v5.png` is consistent with the written custody law on the load-bearing architecture:

```text
retained custody state
-> self-selected primitive letter
-> primary pairing
-> two chart restrictions
-> two directed transfers
-> fully retained lifted object
-> terminal projection only after evolution
```

The diagram also makes the present obstruction visible. The arithmetic factor uses custody coordinates `j` and `q`. It does not yet act on the primary pairing or the Orthad blocks where `L` latches the completed axis and appends a new active axis.

The next derivation should therefore begin at Panel 3, not at the projection-side sphere or a finite carrier downstream.

The written authority controls two notation points:

1. `j` is the global phase-position index and does not increment on every primitive step. It changes with `Q` and is reset to the next domain start by `L`.
2. The current state is `X=(A,q,theta,k,j,W)`, while the fully retained lifted object also contains `P`, two chart restrictions, and two transfers.
