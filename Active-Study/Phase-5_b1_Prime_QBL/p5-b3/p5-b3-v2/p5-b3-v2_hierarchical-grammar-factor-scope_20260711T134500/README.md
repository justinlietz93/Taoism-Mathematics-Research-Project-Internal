# p5-b3-v2 Hierarchical Grammar Factor Scope

Rebuild from the package root:

```bash
python scripts/20260711T134500_build_package.py --package-root . --archive-out ../p5-b3-v2_hierarchical-grammar-factor-scope_20260711T134500.zip
```

The builder runs the derivation script, executes the no-I/O notebook, extracts figures, regenerates outputs and traces, writes the manifest, builds a deterministic ZIP, and writes its SHA-256 file.
