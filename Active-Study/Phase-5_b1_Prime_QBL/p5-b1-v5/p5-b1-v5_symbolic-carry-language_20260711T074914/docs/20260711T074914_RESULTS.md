# Results

## Proved abstractly

- `J` and `P` remain exact one-step Lebesgue objects.
- `989` is forbidden despite pairwise support.
- The actual affine length-three language has 15 words.
- The affine word complexity is `2^(n+1)-1`.
- The affine coding entropy is `log(2)`.
- The pairwise edge-envelope entropy is `log(1+sqrt(2))`.

## Certified finitely

- Trace coverage: `A=0..10000`, exactly once each.
- Carry alphabet: `{7,8,9}` only.
- Transition count: 9999.
- Direct cylinder enumeration: lengths 1 through 12.
- Outward interval boundary certificate: `A=0..10000`.
- Markov-order counterexamples: orders 1 through 10.

## Observed

- Empirical edges are closer to `J` than to envelope `K`.
- State and defect frequencies are close to Lebesgue values.

## Open

```text
ACTUAL CARRY LANGUAGE PRESENTATION: NOT YET DERIVED
ACTUAL CARRY LANGUAGE MIXING: NOT YET DERIVED
SPECIFIC-ORBIT EQUIDISTRIBUTION: NOT PROVED
GLOBAL T_A=ceil(y_A) BRIDGE: NOT YET PROVED
GAUGE/FQM MAP FROM d_A=±1: NOT YET DERIVED
```
