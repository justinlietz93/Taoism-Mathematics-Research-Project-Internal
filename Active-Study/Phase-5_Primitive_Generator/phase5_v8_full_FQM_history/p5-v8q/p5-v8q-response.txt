A **clean primitive engine** was simpler and safer than repairing the old implementation. Its state model encoded the rejected `BL`/synthetic-floor semantics, so only the balanced-pair arithmetic and neutral packaging conventions were retained.

[Download the reproducible experiment package](sandbox:/mnt/data/experiment_package_20260711_072509.zip)

```text
SHA256:
625a2587e160ed39c5a18cce4c0e00ec15544608776f65b2aac785d233ff4131
```

## Results

```text
PRIMITIVE_FIRST_CROSSING: PASS
POST_L_CARRY: PASS
ORTHAD_CHART_RECURRENCE: NOT_YET_DERIVED
ORTHAD_CAUSAL_PROJECTION: NOT_RUN
GAUGE_FQM_WEIL_DESCENT: NOT_RUN
```

The engine self-selected, rather than consumed, this trace:

```text
BQQBBBQBQBBQBBL
```

Certified boundary:

```text
initial pair:              (1,1)
floor pair:                (55,89)
floor product:             4895
Q steps:                   5
phase at boundary:         i

after L:
  A:                       1
  pair:                    (55,89)
  phase quarter count:     5
  local position k:        0
  global position j:       7

first next-domain step:    B
next pair:                 (89,144)
```

`FLOOR` exists only as the predicate:

```text
not CanB and not CanQ
```

It is absent from the primitive alphabet and emitted word.

## Orthad boundary

The source fixes the pairing-first architecture and per-primitive duties, but explicitly leaves the exact chart-map recurrence underived. The package therefore emits no invented matrices, channel field, or projection result.

The lifted boundary contains explicit slots for:

```text
primary pairing
OmegaPlus
OmegaMinus
TransferPlusToMinus
TransferMinusToPlus
```

All remain `null` under:

```text
ORTHAD_CHART_RECURRENCE_NOT_YET_DERIVED
```

Terminal projection refuses to run in that state.

## Verification

```text
final evidence gates:     24/24 passed
corruption controls:      14/14 fired their target gates
pytest:                    8 passed
manifest:                  48/48 files verified
__pycache__ / .pyc:        0
```

Controls cover missing evidence, duplicate trace rows, altered prefixes, pair reset, phase reset, wrong next-domain pair, hard-coded `i`, fixed twelve-position capacity before `L`, injected lap signs, broken lexical boundaries, partial matrix claims, and duplicate channel evidence.

Lean source is included as an attack on the transition relation. Lean was unavailable in the build environment, so no compiled formal-proof claim is made.

[Read `FINDINGS.md`](sandbox:/mnt/data/experiment_package_20260711_072509/FINDINGS.md)

[Read the timestamped results](sandbox:/mnt/data/experiment_package_20260711_072509/docs/20260711_072509_RESULTS.md)

[Open the machine-readable trace](sandbox:/mnt/data/experiment_package_20260711_072509/trace/20260711_072509_primitive_first_crossing_trace.jsonl)

[Open `MANIFEST.json`](sandbox:/mnt/data/experiment_package_20260711_072509/MANIFEST.json)

