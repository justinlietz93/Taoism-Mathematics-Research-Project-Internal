# p5_v8u Agent Instructions

[CURRENT STATE]

Keep the accepted primitive results:

```text
BQQBBBQBQBBQBBL
floor pair (55,89)
five Q steps
pair and phase carried across the first L
first next-domain B gives (89,144)
```

Keep `i/4895` only as a local active-axis shorthand.

Keep the Domain-0 doubled cyclic surface `Z/12Z` as a supported carrier-level result.

Keep the first-L structural axis-block change `1 -> 2`.

Treat `Z/12Z × Z/24Z` only as a candidate first-L product index set. The clean first-L carrier recurrence is not yet derived.

Treat `s_D(x)=x+1 mod D` only as a fixed historical descendant until it is derived as the clean native successor.

The `p5_v8t` package found no clean successor recurrence. It named `Phi_L^S` too early. The seed and the earlier `B/Q` updates are still open.

The next interaction ID is `p5_v8u`.

[STRATEGIC QUESTION]

Determine whether the clean Domain-0 retained state forces a native successor before the first `L`.

Compare these cases:

1. `S_empty` is the cyclic shift on `Z/12Z`, and every pre-L `B/Q` tick forces a successor update or covariance law.
2. The cyclic shift is only a downstream historical operator, and the clean successor seed is missing.
3. The canon's “native successor” names a different word-built object that must be recovered from the pairing-first architecture.

Assess all three. Propose a stronger interpretation when the sources force one.

[REASONING TASK]

1. Preserve the accepted baseline.

   Reuse the `p5_v8t` package and the accepted primitive evidence.

   Recompute one custody sanity check.

   Report: exact baseline ZIP hashes and reused paths.

2. Separate the carrier claims.

   Determine which of these are source-derived:

   ```text
   Domain-0 doubled surface Z/12Z
   first-L axis-block count 1 -> 2
   newborn Domain-1 surface Z/24Z
   direct product Z/12Z × Z/24Z
   prefix-by-prefix doubled carrier address
   ```

   Consider direct product, group extension, single-carrier block presentation, and another coupled construction.

   Report: `carrier_claim_disposition.md` and a machine-readable claim table.

3. Determine the clean successor seed.

   Let `S_empty` act on the supported Domain-0 carrier or index set.

   Determine whether the sources force:

   ```text
   S_empty(x) = x + 1 mod 12
   ```

   Distinguish a historical fixed cyclic identity from a clean QBL derivation.

   If the seed is not forced, state the first exact missing equation.

   Report one status:

   ```text
   NATIVE_SUCCESSOR_SEED: DERIVED
   ```

   or:

   ```text
   NATIVE_SUCCESSOR_SEED: NOT_YET_DERIVED
   ```

4. Work through every pre-L primitive prefix.

   Determine the successor update or covariance under each `B` and `Q` in:

   ```text
   BQQBBBQBQBBQBB
   ```

   Start with the first `B`. Do not jump to `L`.

   For each prefix, report:

   ```text
   carrier/index set
   successor before
   primitive
   successor after
   source or derivation
   status
   ```

   Report: `pre_L_successor_trace.jsonl`.

5. Classify the possible `B/Q` laws.

   Assess whether `B` and `Q`:

   ```text
   change the successor
   conjugate the successor
   change only its eigencharacters
   leave a fixed successor unchanged while changing another retained object
   ```

   Require a source-derived reason for the selected case.

   Report: `BQ_successor_covariance.md`.

6. Attempt the first-L successor extension only after the pre-L trace closes.

   Determine whether `L` produces:

   ```text
   one global cycle
   commuting factor successors
   a coupled group extension
   a block successor on one retained carrier
   another forced construction
   ```

   Do not assume `Z/12Z × Z/24Z`.

   Report:

   ```text
   FIRST_L_SUCCESSOR_EXTENSION: DERIVED
   ```

   or the first exact missing map.

7. Correct the v7e first-birth assessment.

   Confirm that zero Q-depth is lawful in the historical extractor.

   Determine whether a newborn axis with no pre-latch history belongs to the extractor's input type.

   Consider whether the first `L` has a lawful boundary-incidence analogue.

   Do not promote v7e to a modern pairing or transfer law.

   Report: `v7e_first_birth_type_assessment.md`.

8. Type the spectral layer honestly.

   Determine the minimum coefficient ring or field and representation space needed to state eigenvectors, eigenforms, and the successor-to-pairing bridge.

   Keep this layer separate from primitive custody.

   Report one status:

   ```text
   AMBIENT_SPECTRAL_MODULE_ROLE:
   REQUIRED_FOR_EIGENBASIS_AND_PAIRING_FORMALIZATION
   ```

   or a stronger source-derived result.

9. Keep the pairing and charts blocked until the successor is settled.

   Derive no primary pairing from a guessed successor.

   Emit no chart or transfer matrix from the candidate carrier alone.

   Report separate statuses for:

   ```text
   NATIVE_SUCCESSOR_SEED
   PRE_L_BQ_SUCCESSOR_RECURRENCE
   FIRST_L_SUCCESSOR_EXTENSION
   PRIMARY_PAIRING_RECURRENCE
   CHART_RESTRICTIONS
   MIXED_TRANSFER_RECURRENCE
   ```

10. Distinguish evidence classes in the verifier.

   Label each gate as:

   ```text
   MECHANICALLY_RECOMPUTED
   SOURCE_DERIVED
   CANDIDATE_FORMALIZATION
   ```

   A candidate formula must not pass a gate named as a derived theorem.

   Add controls that promote the candidate product carrier and historical shift to clean-derived status. Both controls must fail.

11. Keep downstream layers closed.

   Run no terminal projection, gauge quotient, FQM construction, or Weil descent unless the complete first-crossing successor, pairing, and transfer chain is derived.

12. Package the work.

   Name the root and ZIP:

   ```text
   p5_v8u_orthad-successor-seed-and-prefix-covariance_<YYYYMMDD_HHMMSS>
   p5_v8u_orthad-successor-seed-and-prefix-covariance_<YYYYMMDD_HHMMSS>.zip
   ```

   Use the `p5_v8u` prefix exactly.

   Follow the reproducible experiment-package format.

   Compute the final SHA-256 from the exact ZIP linked in the response.

   Report: the ZIP, exact hash, status lines, and the first derivation file to open.
