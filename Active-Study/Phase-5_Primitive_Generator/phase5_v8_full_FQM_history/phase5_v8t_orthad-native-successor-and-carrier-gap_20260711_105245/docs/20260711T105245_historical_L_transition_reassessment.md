# Historical L transition reassessment

Two facts coexist:

- the global pair and phase carry across L;
- the newborn local active axis begins at identity.

Therefore the historical scalar ratio is internally consistent in its narrow model:

T(L) = lens_newborn/lens_latched = 1/(i/4895) = 4895*i^3.

It is not a modern pairing or cross-chart transfer law. It contains no successor extension, inherited mixed block, chart embedding, or directed transfer equation.
