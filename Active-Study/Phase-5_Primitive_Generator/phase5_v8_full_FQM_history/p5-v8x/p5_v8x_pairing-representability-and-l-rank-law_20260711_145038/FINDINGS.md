# FINDINGS

## Proved abstractly

1. A two-slot pairing system with pullback in both arguments types all four ratified chart/transfer expressions without requiring a scalar object or a representing dual.
2. Representability as `P:H->D(H)` requires an additional natural representation axiom: `Pair(A,H) ≅ Hom(A,D(H))`.
3. One-sided orthogonality does not force both mixed blocks to vanish for a nonsymmetric pairing. The explicit matrix `[[1,1],[0,1]]` has `P(new,old)=0` and `P(old,new)=1`.
4. If both mixed blocks are zero, the two-by-two presentation is block diagonal.
5. Block size `1 -> 2` does not force algebraic rank `+1`; `diag(1,0)` has size two and rank one.

## Certified finitely

- The accepted primitive engine independently reproduces `BQQBBBQBQBBQBBL`.
- The first floor pair is `(55,89)`, product `4895`, with five Q steps and phase witness `i`.
- `L` carries the pair and phase, resets `k=0`, sets `j=7`, and the first next-domain `B` gives `(89,144)`.
- Six real corruption controls mutate a temporary package copy, run the actual verifier, and fire the intended semantic gate.
- The executed notebook has one no-I/O code cell per claim and one single-axes figure per claim.

## Observed

- The source uses the schematic star in both chart pullbacks and gauge re-expression, but does not define it as adjoint, involution, transpose, or conjugation.
- The source phrase “new orthogonal active axis” is side-ambiguous before a symmetry law or typed orthogonality relation exists.
- The source phrase “pairing rank rises by one” is not linked to a specific rank function.

## Open

- exact primary pairing type;
- representability and the dual object `D(H)`;
- coefficient/scalar realization, involution, and scalar variance;
- left/right/two-sided orthogonality at the first `L`;
- argument-object dimension, pairing morphism rank, and nondegenerate rank;
- lawful gauge subgroup;
- exact primary pairing seed and B/Q/L pairing recurrences;
- chart values, transfer values, `Xi_hat_t`, projection, gauge quotient, FQM, Weil, affine, and MHD layers.

## Retired path

- `P:H->D(H)` as a source-derived minimum;
- ordinary-versus-conjugate scalar variance as the earliest type fork;
- automatic block diagonalization at `L`;
- algebraic pairing-rank `+1` inferred from one appended axis;
- quotient by all `Aut(H)` without a source-authorized gauge subgroup;
- status certification from self-reported capability booleans.

## Status

```text
PAIRING_FIRST_REALIGNMENT: PASS
SOURCE_FORCED_PAIRING_INTERFACE: DERIVED
DUALITY_MORPHISM_MODEL: ADMISSIBLE_CANDIDATE
PAIRING_REPRESENTABILITY: NOT_YET_DERIVED
SCALAR_VARIANCE_STATUS: DOWNSTREAM
FIRST_L_RIGHT_MIXED_BLOCK: NOT_YET_DERIVED
FIRST_L_LEFT_MIXED_BLOCK: NOT_YET_DERIVED
FIRST_L_PAIRING_RANK_LAW: NOT_YET_TYPED
EXACT_PRIMARY_PAIRING_TYPE: NOT_YET_DERIVED
EXACT PRIMARY PAIRING SEED: NOT_YET_DERIVED
Xi_hat_t VALUES: NOT_INSTANTIATED
REAL CORRUPTION CONTROLS: PASS
TERMINAL PROJECTION: NOT_RUN
```
