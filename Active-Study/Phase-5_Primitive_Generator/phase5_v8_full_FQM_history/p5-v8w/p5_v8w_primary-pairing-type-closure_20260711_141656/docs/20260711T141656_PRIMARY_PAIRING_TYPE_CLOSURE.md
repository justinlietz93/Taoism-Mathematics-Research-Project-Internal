# p5_v8w Primary Pairing Type Closure

## Executive result

The pairing-first architecture forces an abstract duality morphism `P_t:H_t->D(H_t)`. It does not yet force ordinary versus conjugate scalar variance. That is the earliest exact type fork.

## Source-derived closure

The chart equations are all instances of `D(iota_a) P iota_b`; the gauge action is `D(U) P U`; the Q witness requires a quarter-turn-capable realization; and the first `L` requires an orthogonal biproduct extension.

The first-L orthogonality law forces the mixed birth blocks to vanish. This is stronger than leaving an arbitrary `C_t` in the block template. The new-axis diagonal remains open.

## Candidate elimination

- General duality morphism: derived minimal interface.
- Bilinear and sesquilinear realizations: admissible, not forced.
- Hermitian specialization: admissible, not forced.
- Quadratic refinement directly as `P_t`: ruled out without polarization.
- Polarized quadratic and operator-valued objects: require extra maps before they can play the primary role.

## Type fork

```text
SCALAR_VARIANCE_AXIOM: specify whether D(lambda*id_H)=lambda*id_DH or D(lambda*id_H)=lambda^**id_DH; equivalently whether P(lambda*x,y)=lambda*P(x,y) or lambda^**P(x,y).
```

## Seed boundary

Raw and model-level gauge classes are nonunique under the unconstrained interface, but retained-seed nonuniqueness is not yet derived because seed admissibility and normalization are absent.

## Downstream boundary

No pairing values, chart values, transfers, cocycle residuals, projection rows, gauge quotient, FQM, Weil, affine, or MHD result are emitted.

## Status

```text
PAIRING_FIRST_REALIGNMENT: PASS
MINIMAL_PAIRING_INTERFACE: DERIVED
EXACT_PRIMARY_PAIRING_TYPE: NOT_YET_DERIVED
SURVIVING_TYPE_FORK: ordinary-dual bilinear; conjugate-dual sesquilinear
EARLIEST_MISSING_AXIOM: SCALAR_VARIANCE_AXIOM
RAW_PAIRING_PRESENTATION_NONUNIQUENESS: PROVED
RETAINED_GAUGE_CLASS_SEED_NONUNIQUENESS: NOT_YET_DERIVED
EXACT_PRIMARY_PAIRING_SEED: NOT_YET_DERIVED
B_PAIRING_TYPE_SIGNATURE: DERIVED
Q_PAIRING_TYPE_SIGNATURE: DERIVED
L_PAIRING_TYPE_SIGNATURE: DERIVED
Xi_hat_t VALUES: NOT_INSTANTIATED
TERMINAL_PROJECTION: NOT_RUN
```
