# Primary Pairing Seed Closure

`Xi_0` and `W_0` do not select a gauge class of pairing values. After the interface and admissibility law are fixed, the missing map is

```text
eta_P : (Xi_0,W_0) -> Pair(H_0,D(H_0))/Aut(H_0).
```

Normalization, signature, orientation, and gauge equivalence remain separate obligations.

```text
EXACT_PRIMARY_PAIRING_SEED: NOT_YET_DERIVED
MISSING_MAP_OR_AXIOM: eta_P after SEED_ADMISSIBILITY_AXIOM
```
