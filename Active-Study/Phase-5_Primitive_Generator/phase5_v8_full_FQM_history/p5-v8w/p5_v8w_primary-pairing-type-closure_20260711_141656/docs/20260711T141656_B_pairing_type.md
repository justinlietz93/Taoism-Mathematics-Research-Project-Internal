# B pairing type signature

Derived at the minimal interface level:

```text
Phi_B^P : (Xi_t, Xi_(t+1), W_(t+1), P_t in Hom(H_t,D(H_t)))
          -> P_(t+1) in Hom(H_t,D(H_t))
```

`H_t` and architectural rank are unchanged. The exact value recurrence is open.

```text
B_PAIRING_TYPE_SIGNATURE: DERIVED
B_PAIRING_VALUE_RECURRENCE: NOT_YET_DERIVED
```
