# Lifted-State Schema Boundary

The package emits `lifted_state_schema`, not `Xi_hat_t`. `Xi_hat_t` is reserved for an actual tuple containing instantiated `P_t`, both chart restrictions, and both directed transfers.

All five Orthad-value fields remain null here, so the fully retained lifted state is not instantiated and no projection is run.
