# Chart Maps

```text
CHART_MAPS: NOT_YET_DERIVED
DIRECTED_TRANSFERS: NOT_YET_DERIVED
```

Required direction:

```text
Omega_plus  = iota_plus*  P iota_plus
Omega_minus = iota_minus* P iota_minus
T_plus_to_minus = iota_minus* P iota_plus
T_minus_to_plus = iota_plus* P iota_minus
```

The chart modules, embeddings, overlap domain, and pairing type are not fixed. No matrix values are emitted.
