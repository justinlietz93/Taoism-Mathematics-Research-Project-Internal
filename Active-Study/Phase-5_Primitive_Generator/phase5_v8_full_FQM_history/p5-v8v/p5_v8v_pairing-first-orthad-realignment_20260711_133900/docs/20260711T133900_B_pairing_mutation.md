# B Pairing Mutation

```text
B_PAIRING_MUTATION: NOT_YET_DERIVED
MISSING_MAP: B_pairing : (P_t,Xi_t,Xi_{t+1},W_{t+1}) -> P_{t+1}
```

The local descendant is exact:

```text
phase retained
(u,v) -> (v,u+v)
1/(uv) -> 1/(v(u+v))
```

This does not determine how every retained pairing component changes.
