# Figures

The architecture diagram is preserved in `inputs/` as the supplied layout master. Notebook claim cells emit inline single-axes figures. No figure is used as proof of an underived pairing, chart, or transfer value.
