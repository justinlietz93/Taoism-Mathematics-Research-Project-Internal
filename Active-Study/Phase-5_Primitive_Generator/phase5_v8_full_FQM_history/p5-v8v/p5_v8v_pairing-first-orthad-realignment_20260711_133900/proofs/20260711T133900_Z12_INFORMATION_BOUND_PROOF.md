# Z/12Z Information Bound

The free monoid `{B,Q,L}*` contains words of every finite length and is infinite. `Z/12Z` contains twelve elements. An injective map from an infinite set into a finite set does not exist. Therefore a `Z/12Z` coordinate cannot retain every exact QBL word without additional retained state.
