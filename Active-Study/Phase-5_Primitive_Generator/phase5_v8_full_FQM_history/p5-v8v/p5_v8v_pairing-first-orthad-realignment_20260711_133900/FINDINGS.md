# FINDINGS

## Proved abstractly

### Custody alone does not determine a pairing seed

On the one-dimensional rational module, `P_1(x,y)=xy` and `P_2(x,y)=2xy` are distinct bilinear forms. Both can be paired with the same custody tuple `Xi_0` because the custody law contains no seed equation. Therefore an explicit seed map is necessary:

```text
eta_P : (Xi_0,W_0,D_P) -> P_0.
```

This proves nonuniqueness under the currently stated custody-only axioms. It does not rule out a future source that supplies `eta_P`.

### Z/12Z cannot retain the exact full word

The exact word space `{B,Q,L}*` is infinite and `Z/12Z` has twelve elements. No map from all exact words into `Z/12Z` can be injective. The finite doubled surface is not the fully retained lifted state.

## Certified finitely

The independent primitive implementation and oracle agree on:

```text
word: BQQBBBQBQBBQBBL
floor pair: (55,89)
floor product: 4895
Q steps: 5
phase at boundary: 5*pi/2
phase witness: i
after L: A=1, pair=(55,89), phase_quarters=5, k=0, j=7
first next-domain B: (89,144)
```

The local active-axis shorthand reaches `i/4895`. It is certified only as a local descendant.

## Observed

The primary law and canon constrain `P_t` to be primary, retained from the first primitive tick, rank-preserving under `B/Q`, rank-extending under `L`, and generative of both chart restrictions and both directed transfers.

The source set does not specify the coefficient object, retained axis object, duality or involution, argument variance, or symmetry/adjoint law. The older Hermitian `H=M+iJ` tensor, scalar `T_ab` cochain, finished FQM form, and cyclic successor are downstream or historically contaminated reconstructions. None supplies the clean pairing seed and per-letter recurrence.

At the first `L`, old-block retention and one new orthogonal axis are architectural obligations. If a future scalar-valued form type gives `orthogonal` its standard meaning, the mixed birth block vanishes in a compatible basis. This remains a conditional formalization, not an emitted matrix.

## Open

```text
PRIMARY_PAIRING_TYPE: NOT_YET_DERIVED
PRIMARY_PAIRING_SEED: NOT_YET_DERIVED
B_PAIRING_MUTATION: NOT_YET_DERIVED
Q_PAIRING_MUTATION: NOT_YET_DERIVED
L_PAIRING_EXTENSION: NOT_YET_DERIVED
CHART_MAPS: NOT_YET_DERIVED
DIRECTED_TRANSFERS: NOT_YET_DERIVED
OVERLAP_DOMAIN: NOT_YET_DERIVED
COCYCLE: NOT_RUN
TERMINAL_PROJECTION: NOT_RUN
GAUGE_FQM_WEIL_DESCENT: NOT_RUN
QBL_TO_AFFINE_FACTOR_MAP: NOT_YET_DERIVED
MHD_ORTHAD_READINESS: NOT_READY
```

The first exact missing object is:

```text
PAIRING_TYPE_DATUM
D_P=(K,H_0,duality_or_involution,variance,symmetry_law).
```

After that object is fixed, the next missing map is:

```text
eta_P : (Xi_0,W_0,D_P) -> P_0.
```

## Retired path

```text
alpha_empty as the first Orthad gap
S_empty as the first Orthad gap
spectral module as an established predecessor of P_t
Z/12Z shift as a pairing generator
Z/12Z x Z/24Z as the full retained carrier
historical H=M+iJ as the clean P_0
historical T_ab as the modern mixed transfer
pair_c as a primary-pairing recurrence
```

The finite successor results remain valid downstream coordinate mathematics and a negative control.

## Required status block

```text
FIRST TRUE GAP:
    PRIMARY PAIRING TYPE, SEED, AND PER-LETTER MUTATION

NATIVE SUCCESSOR ON Z/12Z:
    DOWNSTREAM COORDINATE QUESTION

ORTHAD EXISTS FROM FIRST PRIMITIVE TICK:
    ARCHITECTURAL LAW

PRIMITIVE CUSTODY STATE:
    Xi_t

FULLY RETAINED LIFTED STATE:
    Xi_hat_t

ORTHAD:
    ⌞Xi_hat_t⌝

EXACT PRIMARY PAIRING TYPE:
    NOT_YET_DERIVED

EXACT PRIMARY PAIRING SEED:
    NOT_YET_DERIVED

EXACT PRIMARY PAIRING RECURRENCE:
    NOT_YET_DERIVED

EXACT CHART MAPS:
    NOT_YET_DERIVED

EXACT DIRECTED TRANSFERS:
    NOT_YET_DERIVED

QBL_TO_AFFINE FACTOR MAP:
    NOT_YET_DERIVED

MHD_ORTHAD_READINESS:
    NOT_READY
```
