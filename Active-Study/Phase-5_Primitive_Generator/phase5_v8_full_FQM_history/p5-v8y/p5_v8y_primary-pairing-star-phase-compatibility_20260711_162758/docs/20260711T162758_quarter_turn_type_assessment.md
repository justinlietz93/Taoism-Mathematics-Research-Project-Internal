# Quarter-turn type assessment

The primary authority states that `Q` rotates the active pairing data by a quarter-turn witness denoted `i`. It separately records the older local shorthand `exp(i theta)/(uv)` and explicitly denies that the shorthand is the complete Orthad.

The corpus does not determine whether `i` is:

- scalar multiplication in a complex coefficient object;
- a complex structure `J` with `J^2=-1` acting on arguments;
- an orientation operator;
- a local descendant label carried by the old shorthand.

Therefore:

```text
Q QUARTER-TURN ACTION TYPE: NOT_YET_DERIVED
local i/(uv): LOCAL_DESCENDANT_ONLY
```
