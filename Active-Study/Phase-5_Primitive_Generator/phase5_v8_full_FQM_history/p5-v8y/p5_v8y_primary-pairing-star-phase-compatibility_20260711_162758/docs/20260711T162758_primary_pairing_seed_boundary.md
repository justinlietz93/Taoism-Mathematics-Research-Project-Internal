# Primary pairing seed boundary

No source-bound row derives a primary seed from `Xi_0` and `W_0`. The following remain downstream or untyped and may not be promoted to `P_0`:

- local `i/(uv)`;
- historical `H=M+iJ`;
- the doubled carrier `Z/12Z`;
- a finished FQM polarization;
- imported matrix normalization.

```text
EXACT PRIMARY PAIRING SEED: NOT_YET_DERIVED
```
