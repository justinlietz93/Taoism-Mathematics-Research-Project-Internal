# Primary pairing star and phase compatibility

## Result

No single concrete pairing type is forced by the source-bound corpus.

The same glyph `*` occurs in chart pullbacks, schematic gauge re-expression, and the candidate first-`L` reverse relation. The historical `H=M+iJ` uses conjugate transpose to establish Hermitianity, but it is a downstream overlap reconstruction rather than the clean primary pairing.

A transpose-based complex bilinear model, a conjugate-transpose-based sesquilinear model, an operator-valued star model, and an abstract dual-pullback model all remain compatible with the currently ratified local obligations. They differ in how `*`, `i`, orthogonality, and the first-`L` reverse relation are typed.

## Earliest missing axiom

```text
STAR_SEMANTICS_AXIOM:
Specify one typed operation * with its domain, codomain, variance,
coefficient action, and involution/adjoint law, and require the same
operation to govern iota* P iota, U* P U, and the reverse first-L relation.
```

Only after this axiom is fixed can `Q_ACTION_AXIOM` state whether the quarter turn is scalar multiplication, conjugation by a complex-structure operator, orientation action, or another mutation.

```text
EXACT PRIMARY PAIRING TYPE: NOT_YET_DERIVED
SURVIVING MODELS: complex bilinear symmetric; complex bilinear nonsymmetric; complex sesquilinear non-Hermitian; Hermitian; skew-Hermitian; operator-valued pairing; abstract dual-pullback model
EARLIEST MISSING AXIOM: STAR_SEMANTICS_AXIOM
```
