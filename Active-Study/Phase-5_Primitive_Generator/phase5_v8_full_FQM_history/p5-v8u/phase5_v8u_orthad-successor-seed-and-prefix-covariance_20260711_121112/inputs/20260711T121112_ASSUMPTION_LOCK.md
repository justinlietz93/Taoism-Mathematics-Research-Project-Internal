# Assumption Lock — p5_v8u

## Accepted custody facts

- Strict self-selection `B > Q > L`.
- First crossing `BQQBBBQBQBBQBBL`.
- Floor pair `(55,89)`, five `Q` ticks.
- Pair and phase carry through first `L`.
- First next-domain `B` gives `(89,144)`.
- `i/4895` is only a local active-axis shorthand.

## Licensed carrier facts

- Domain-0 local phase capacity is 6.
- Both orientation hands are retained, supporting the Domain-0 doubled surface `Z/12Z`.
- First `L` changes the structural axis-block count `1 -> 2`.
- Domain-1 has 12 local phase positions, so `Z/24Z` is licensed only as its local doubled surface.

## Forbidden promotions

- `Z/12Z × Z/24Z` as the clean retained carrier.
- A prefix address obtained by copying `k` into the doubled carrier.
- Historical `s_D(x)=x+1` as the clean native successor seed.
- Any primary pairing, chart, transfer, projection, gauge, FQM, or Weil output derived from a guessed successor.

## Forbidden imports and methods

- No `R/S/T`, fixed-window scheduler, synthetic floor operator, candidate search, post-hoc Orthad, or projection before halt.
- No constant chart matrices or hand-inserted transfer law.
