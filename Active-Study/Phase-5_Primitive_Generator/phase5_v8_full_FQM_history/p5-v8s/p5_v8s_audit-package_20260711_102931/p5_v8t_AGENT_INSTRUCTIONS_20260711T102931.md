# p5_v8t Agent Instructions

[CURRENT STATE]

Keep the accepted primitive results:

```text
BQQBBBQBQBBQBBL
floor pair (55,89)
five Q steps
pair and phase carried across the first L
first next-domain B gives (89,144)
```

Keep `i/4895` only as a local active-axis shorthand.

Keep the historical findings that `O` is a derived overlap record, its clean per-tick schedule is open, and `pair_c` is not a ratified modern coupling law.

The `p5_v8s` package did not yet isolate the earliest Orthad gap. It proposed an ambient module functor but did not examine the canon's earlier statement that the pairing is the eigenbasis of the native successor.

Use the clean QBL law for custody. Use the canonical ledger for Orthad architecture. Treat historical packages as evidence to test.

The next interaction ID is `p5_v8t`.

[STRATEGIC QUESTION]

Determine whether the clean retained state already fixes a concrete carrier, basis, and native successor.

Compare these cases:

1. The native successor is defined and forces the pairing basis.
2. The retained carrier is defined, but the native successor law is missing.
3. An ambient module functor is genuinely required before either object can be stated.

Critique all three. Use a stronger case when the sources support it.

[REASONING TASK]

1. Preserve the accepted primitive baseline.

   Reuse the `p5_v8s` package and its accepted `p5_v8r` custody evidence.

   Recompute one sanity check only.

   Report: the exact baseline ZIP hashes and reused evidence paths.

2. Recover the native-successor source lineage.

   Search every supplied canon and historical artifact for:

   ```text
   native successor
   successor operator
   eigenbasis
   eigen-chart
   generated eigen-chart
   pairing-primary generative direction
   ```

   Locate the pre-ledger source when it is available.

   Name each unavailable source exactly.

   Report: `native_successor_source_inventory.csv`.

3. Define the concrete retained carrier before adding an abstract module.

   Work from the clean state and exact word prefix.

   Determine what is already fixed by:

   ```text
   retained axis stack
   domain counter
   local and global phase positions
   doubled orientation carrier Z/(2N)Z
   lap/parity hand
   B/Q/L rank schedule
   ```

   State the concrete basis or index set at every first-crossing prefix.

   Report: `retained_carrier_and_basis.md` and a machine-readable prefix table.

4. Determine the native successor.

   Let `S_t` denote the native successor acting on the retained carrier at prefix `t`.

   Determine its type, seed, and update under `B`, `Q`, and `L`.

   Require every update to follow from the clean retained state and exact word prefix.

   Report one result:

   ```text
   NATIVE_SUCCESSOR_RECURRENCE: DERIVED
   ```

   with a complete first-crossing trace, or:

   ```text
   NATIVE_SUCCESSOR_RECURRENCE: NOT_YET_DERIVED
   ```

   with the first exact missing map.

5. Reassess the ambient module proposal.

   Determine whether

   ```text
   H : (X_t,W_t) -> (R,H_t,basis_t,Phi_B,Phi_Q,Phi_L)
   ```

   is:

   ```text
   DERIVED
   REQUIRED_BUT_NOT_YET_DERIVED
   OPTIONAL_FORMAL_PRESENTATION
   UNLICENSED_IMPORT
   ```

   Do not call it the earliest gap by assumption.

   Report: `ambient_module_role.md`.

6. Derive the pairing only after the successor is settled.

   Use the canon statement that the pairing is the forced eigenbasis or eigenform of the native successor.

   Determine the exact relation between `S_t` and `P_t`.

   If the source does not define that relation, state that missing equation before introducing chart modules.

   Report: `successor_to_pairing_bridge.md`.

7. Derive the charts and transfers only when the pairing is typed.

   Determine both chart restrictions and both directed transfers from the one primary pairing.

   Confirm that neither chart is independently invented.

   Stop at the first missing dependency.

   Report separate statuses for pairing, chart restrictions, and transfer recurrence.

8. Correct the historical `L` assessment.

   Keep these two facts separate:

   ```text
   global pair and phase carry across L
   new local active axis begins at identity
   ```

   Reassess `T(L)=lens_newborn/lens_latched` using that distinction.

   Judge whether it is valid only in the historical local scalar model or whether any part survives into the modern transition law.

   Report: `historical_L_transition_reassessment.md`.

9. Strengthen the underdetermination theorem.

   Use two nondegenerate symmetric bilinear forms with the same diagonal restrictions and different mixed terms.

   One valid pair over `Q` is:

   ```text
   P0 = [[1,0],[0,1]]
   P2 = [[1,2],[2,1]]
   ```

   State the exact admissible class covered by the theorem.

   Compile the Lean proof when Lean is available.

   Report: the human proof, machine witness, and compile status.

10. Use honest lineage statuses.

   Report:

   ```text
   SPECIFIED_PHASE5_ARTIFACT_LINEAGE
   EXTERNAL_OVERSET_SOURCE_CORPUS
   ```

   Do not call the full cited corpus complete while `orthad_overset_grids.zip` is unavailable.

11. Remove stale semantic tests.

   Delete or quarantine tests that assert:

   ```text
   tau_0 is the smallest gap
   O is rejected because it is outside Q/B/L
   ```

   Add a gate that fails when a current test asserts a withdrawn claim.

   Report: current tests, provenance-only tests, and exact counts.

12. Execute real corruption controls.

   For each control, mutate a temporary package copy and run the actual verifier.

   Record:

   ```text
   mutation
   target gate
   verifier exit code
   observed failure
   evidence path
   ```

   Compute the response count from these executions.

   Report: `corruption_controls.jsonl` and a summary.

13. Make the verifier directly runnable.

   Disable bytecode creation inside the verifier.

   Prevent pytest cache creation.

   Verify the archive in a fresh temporary extraction.

   Confirm this exact command works on a normal extraction without environment setup:

   ```text
   python scripts/<TS>_verify.py <exact-zip-path>
   ```

14. Repair package presentation.

   Remove control characters from equations.

   Use the required `FINDINGS.md` headings:

   ```text
   Status
   Main result
   Why interesting
   Scope
   ```

   Separate structural axis-block count from the algebraic rank of an underived pairing.

15. Keep downstream layers closed.

   Run no terminal projection, gauge quotient, FQM construction, or Weil descent unless the full first-crossing pairing and transfer recurrence is derived.

   Report:

   ```text
   CONCRETE_RETAINED_CARRIER
   NATIVE_SUCCESSOR_RECURRENCE
   AMBIENT_MODULE_FUNCTOR_ROLE
   PRIMARY_PAIRING_RECURRENCE
   CHART_RESTRICTIONS
   MIXED_TRANSFER_RECURRENCE
   FIRST_L_ORTHAD_EXTENSION
   ORTHAD_CAUSAL_PROJECTION
   GAUGE_FQM_WEIL_DESCENT
   ```

16. Package the work.

   Name the root and ZIP:

   ```text
   p5_v8t_orthad-native-successor-and-carrier-gap_<YYYYMMDD_HHMMSS>
   p5_v8t_orthad-native-successor-and-carrier-gap_<YYYYMMDD_HHMMSS>.zip
   ```

   Use one compact internal timestamp.

   Follow the standard reproducible experiment-package layout.

   Compute the final SHA-256 from the exact ZIP linked in the response.

   Report: the ZIP, exact hash, status lines, and the first derivation file to open.
