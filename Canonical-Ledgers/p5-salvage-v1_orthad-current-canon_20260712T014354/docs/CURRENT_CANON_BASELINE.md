# Current Canon Baseline Used for Salvage

This package classifies legacy Phase 5 work against the current custody and Orthad understanding. It does not promote a legacy result merely because the old package called it canon.

## Retained custody law

- The custody state is `X=(A,q,theta,k,j,W)` with exact ordered word `W`.
- The next primitive is selected only by custody under strict `B > Q > L`.
- The Orthad cannot select, permit, block, or veto the primitive.
- The first exact word from `(1,1)` is `BQQBBBQBQBBQBBL`.
- The first floor pair is `(55,89)`, product `4895`, after five `Q` steps.
- `L` carries the pair and phase, sets the new local position to zero, and opens the next domain.
- The first next-domain `B` gives `(89,144)`.

## Retained coupled architecture

The accepted coupled state is

```text
Xhat_t = (X_t, P_t, Omega_t+, Omega_t-, T_t+->-, T_t-->+)
```

The dependency order is

```text
primitive custody
-> custody-selected primitive
-> primary pairing mutation
-> two chart restrictions
-> two directed transfers
-> complete retained lifted state
-> terminal projection
```

The four descendants must be generated from one primary object. They may not be independently selected. No projection occurs during evolution.

## Current exact and open boundaries

Accepted exactly:

- Domain-0 local trace `a=i^(#Q(W))/(uv)` before the first `L`;
- `B: a -> a*u/(u+v)` on that path;
- `Q: a -> i*a` on that path;
- completed Domain-0 old-axis descendant `i/4895`.

Still open:

- exact primary pairing type, seed, and per-letter recurrence;
- exact chart maps and directed-transfer recurrences;
- the typed meaning of star/adjoint/duality;
- first-`L` mixed and newborn relations;
- exact algebraic rank law;
- a current-canon bridge from the retained Orthad to legacy FQM/Weil outputs.

## Salvage rule

A legacy result is current-canon only when its inputs are generated by this chain. Internally correct mathematics without that provenance is retained as a conditional theorem, calibration set, obstruction, or external tool.
