# Superseded and Rejected Claims

These claims must not be imported into new work without a fresh derivation.

1. Phase 5 is closed.
2. A fixed cyclic successor generates the current primary pairing.
3. A finite doubled cyclic surface is the fully retained lifted state.
4. The first crossing is `BL` from `(34,55)`.
5. `FLOOR` is a primitive or emits an intermediate field.
6. The local `i/4895` value is a current pairing diagonal or Hermitian self-value.
7. Both first-`L` mixed blocks are zero merely because the new axis is called orthogonal.
8. Pairing rank rises by one merely because architectural axis count rises.
9. `P:H->D(H)` or a global `Pair(-,-)` bifunctor is already forced.
10. Stored booleans, status strings, prose labels, or toy matrices are proof certificates.
11. Legacy T records, family-F forms, or FQM presentations are current Orthad outputs without a clean provenance bridge.
12. Quotient group shapes of degenerate forms are automatically realized by orthogonal complement subgroups.
